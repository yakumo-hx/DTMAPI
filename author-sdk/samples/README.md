# Author SDK demand samples

This root contains bounded samples that exercise public API demand without becoming platform
features or first-party products. The `api-demand` subtree is source evidence for API consumers;
its entries remain `NeverPublish` unless a later, explicit product decision changes their Catalog
role and physical ownership.
