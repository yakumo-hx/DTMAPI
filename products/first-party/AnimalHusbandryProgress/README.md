# AnimalHusbandryProgress

Managed Advanced ProductNative CodeMod for the native animal bell viewer. It reads existing husbandry progress, owns four atomic viewer Hooks and only creates/removes its own cloned progress rows. It never mutates animal state, AI, breeding, host membership or save data.
