# 20260811-0001: MoreEquipmentSlots 1.0.0 Direct Replacement

## Metadata

- Update ID: `20260811-0001`
- Date: `2026-08-11`
- Lifecycle Status: `verified`
- Validation Level: `docs, source, unit, runtime`
- Runtime Validation: `passed`
- Related Issue State: `none`
- Source: 用户批准 MoreEquipmentSlots 1.0.0 在 Workshop `3744059735` 原位替换 0.3.1，并要求三个位置保持旧版帽子/饰品语义且 UI 不得重叠。生命周期中曾采用固定入口与无人机模态抽屉并完成其 exact 候选验收；用户随后依据实机截图重新决策为“自然挂在当前已解锁官方槽之后并随官方 `1 -> 2` 动态顺延”，明确删除全部展开栏设计且不把旧设计从 Update 历史中抹去。发布前玩家复测又证明两个饰品袋会把官方被动槽扩为三个，而旧审查上限会隐藏 Product 三槽；用户据此阻断发布，随后把有界兼容目标定为最多四个袋子（官方被动槽总数五、再接 Product 三槽，总计八个被动饰品 UI）。用户现已手动提交更新，并要求以重新下载的 Steam 订阅目录确认同步后更新发布权威。

## Summary

- 以单一 Product 生命周期关闭当前 public 1.00.03 上的动态 UI、导航、监听隔离、装备语义和 incoming withdrawal outcome-unknown；当前 UI 采用事件驱动的官方尾槽顺延，不增加逐帧轮询。
- 保持 Product-v3 sidecar、迁移、存档指纹、cold recovery、typed shield provider、Workshop/UniqueID 和 244 exact Author policy。
- focused 自动门、双分辨率 `NoNativeSave` UI 验收、disposable 保存/cold recovery、完整 Release、候选包、上传准备和发布后订阅制品核验全部由本 Update 跟踪。旧入口/抽屉与仅审查一至两个官方被动槽的动态顺延候选都保留为精确历史证据；后者已被玩家在三个官方被动槽下的发布前复测否决。新 exact 候选已经按官方一至五槽通过 UI、功能、正常保存、cold recovery 和 clean from-start 完整 Release；Steam 重新下载的八文件订阅树又逐字匹配授权树，故当前生命周期为 `verified`，上传例外已消费。

## User-Visible Impact

- 目标版本把三个完整 `112 × 112` Product 装备槽直接接在当前最后一个已解锁官方槽之后；审查范围为官方被动槽总数一至五（基础一槽加零至四个饰品袋），每增加一槽时同一 Product 行只横移一个原生 `112 + 12` 步距，不缩放、不换行、不改官方数组/槽池，也不接管无人机交互。超过五个官方被动槽暂不承诺兼容并继续明确 fail-closed。
- 三槽接受旧版语义下的普通帽、盾帽和两类带技能被动饰品；Product 帽子不改变人物外观。
- public `24650773 / 1.00.03` 上的 exact Product 候选 `1AD74EBD…C65444A` 曾通过入口/抽屉方案的第三存档 `NoNativeSave`、disposable 正常保存、三进程 cold recovery 和 clean from-start 完整 Release，并曾准备到官方上传目录但没有提交。用户随后否决该 UI，故该哈希、其五张抽屉截图和准备树均只是历史证据。后继动态顺延候选 `0F4BD87B…7527F` 也曾通过官方一至两槽范围的全部自动门并作为 `PreparedNotSubmitted` 放入官方目录，但 `2026-08-11 14:49` 玩家复测在三个官方被动槽时精确触发其旧上限并隐藏 Product 三槽；该候选现已失去发布资格，Catalog 上传例外已撤销，Steam 订阅制品始终仍是 0.3.1。
- 当前 Steam 订阅制品是同一 Workshop 项上的 `1.0.0` 直接替换；它要求 Runtime `0.6.0`，只含一个 Product 入口 DLL，不含旧 0.3.1 DLL、Compatibility Host 产品字节或重复入口。精确 manifest、树和观测时间只由 Catalog 与 current subscription manifest 持有。

## Changed Files

- Lifecycle/docs: this Update, `docs/updates/INDEX-2026-08.md`, the MoreEquipment 1.0 rebaseline/root-cause Review, `docs/debug/regressions/smoke-matrix.md`, `docs/debug/issues/ISSUE-021-20260805-moreequipment-native-placement-save-quarantine.md`, `docs/hook-map/focused/MoreEquipmentSlots.md`, and `products/first-party/MoreEquipmentSlots/README.md`.
- Product UI/config: `products/first-party/MoreEquipmentSlots/src/ModEntry.cs`, `src/Native/EquipmentSlotContracts.cs`, `src/Native/MoreEquipmentSlotsCallbacks.cs`, `src/Native/MoreEquipmentSlotsHookInstaller.cs`, `src/Native/MoreEquipmentSlotsHookOwnership.cs`, `src/Native/MoreEquipmentSlotsNativeRuntime.cs`, `src/Native/MoreEquipmentSlotsUiLayoutPolicy.cs`, plus `i18n/english.json` and `i18n/schinese.json`.
- Product semantics/transactions: `products/first-party/MoreEquipmentSlots/src/Native/MoreEquipmentSlotsItemAdmissionPolicy.cs` and `src/Native/EquipmentSlotNativePlacement.cs`.
- Catalog projection: `tools/release/dtmapi-product-catalog.json` retains the mechanical Schema-5 production-source count and managed admission, adds the exact Steam-published `1.0.0` artifact, and returns `releaseStop` to zero upload authorization after consuming the `E2EBF0C…0CFE8 / BFB01CC9…F9B5C0` exception. `tools/release/current-subscription-manifest.json` records the new installed/latest manifest observation; `tools/scripts/check-product-catalog.ps1` binds the new current artifact while preserving the separate 0.3.1 rollback record.
- Physical/source tests: `tests/DTMAPI.UnitTests/DTMAPI.UnitTests.csproj`, `MoreEquipmentSlotsProductTests.cs`, `EquipmentSlotsCompatibilityHostTests.cs`, `Fixtures/EquipmentSlotsHarmonyOwnerFixture/Program.cs`, `Fixtures/EquipmentSlotsNativeFixture/NativeTypes.cs`, `Fixtures/EquipmentSlotsNativeFixture/UnityUiTypes.cs`, and the new `EquipmentSlotsLegacyNativeFixture` / `EquipmentSlotsLegacyTargetFixture` projects.
- Bounded game acceptance route: `src/DTMAPI.GameBridge.DolocTown.QA/QaHostParticipant.cs`, `Scenarios/DolocTownGameBridge.G4Fixtures.cs`, `Scenarios/Fixtures/MoreEquipmentSlots100UiFixtureCase.cs`, `tools/scripts/run-game-smoke.ps1`, and `tools/scripts/test-game-smoke-save-modes.ps1`.
- Runtime evidence/status: this Update, `docs/updates/INDEX-2026-08.md`, and `docs/debug/regressions/smoke-matrix.md`.
- Evidence retention: generated `docs/debug/evidence-retention-allowlist.json` now retains the accepted UI/NoNativeSave, normal-save and aggregate cold-recovery identities referenced by this lifecycle.
- Release orchestration portability: `tools/scripts/test-runtime-workshop-installer-061.ps1`, `cleanup-duplicate-runtime-evidence.ps1`, `steam-appmanifest-identity.ps1`, `test-workshop-download-markers.ps1`, and `test.ps1` now capture intentional native-command failures without aborting, use the repository SHA-256 helper at the affected portable boundaries, and expose a clearly non-authoritative `PostRuntimeInstaller` diagnostic tail. The default remains an unchanged from-start Release run.
- External release state: the old drawer candidate and the later `0F4BD87B…7527F / D02C035B…AC85` one-to-two-slot candidate remain non-releasable historical bytes. The official local folder remains the submitted `E2EBF0C…0CFE8 / BFB01CC9…F9B5C0` tree with its original `workshop.json`; Steam independently redownloaded the same eight-file tree under the new installed/latest manifest.

## Validation

- `DTMAPI.UnitTests.csproj -c Release`: PASS, zero errors; the ten warnings are pre-existing nullable warnings in unrelated DebugConsole sources. The managed test-session entry runs and asserts both `EquipmentSlotsHarmonyOwnerFixture: OK` and `EquipmentSlotsLegacyTargetFixture: OK` rather than allowing unmanaged direct fixture execution.
- `DTMAPI_UNIT_TEST_FOCUS=moreequipment-product`, `moreequipment-cold-host`, `compatibility-host`, and `moreequipment-acceptance-routing`: PASS; every managed test session was removed by the tracked retention protocol.
- `DTMAPI.GameBridge.DolocTown.QA.csproj -c Release`: PASS, zero warnings/errors.
- Repository Release build with `tools/scripts/build.ps1 -Configuration Release -SkipTests`: PASS. All projects build; only the same ten unrelated DebugConsole nullable warnings appear in the Unit aggregate.
- `tools/scripts/test-game-smoke-save-modes.ps1`: PASS. PowerShell AST parsing of `run-game-smoke.ps1`: PASS.
- The no-launch routing projection for `QaObserveEquipmentSlotsUi + AutoExerciseMoreEquipmentSlotsNoNativeSave + AutoExerciseTitleButtonLifecycle`, third slot, `NoNativeSave`, `Local11`, and isolated official Mods: PASS. It selects the Product 1.0 UI route and exactly seven UI evidence files (canonical image/summary plus five dual-resolution state images).
- Exact source comparison between Author reference `24456188_test_E861E0` and current public observation `24650773_public_76C24E`: PASS, `10/10` identical for `AccessoriesBar`, `AccessorySlot`, `EquipmentBarPanel`, `EquipmentBarWidget`, `EquipmentBarUiState`, `AgentEquipmentManager`, `ItemPassive`, `ItemHat`, `ItemFunctionPassive`, and `ItemFunctionHerbPackage`.
- Focused source scan: Product code contains no `passiveItems`, `SetPassiveSlotCount`, `passiveItemPool.Instances`, or native `hatItem` write.
- `check-product-catalog.ps1`: PASS (`27 / 11 / 22 / 48`) after the mechanical Schema-5 source count moved from `287` to `289`; `generate-managed-product-admission-registry.ps1 -Check`: PASS with the same 12 admission rows.
- `check-doc-governance.ps1`: PASS (`6673` checks); `git diff --check`: PASS.
- `build-evidence-retention-allowlist.ps1 -Check` and `test-runtime-evidence-retention.ps1`: PASS after regeneration (`516` source files, `976` smoke runs, `62` runtime identities, `35` durable roots).
- Clean commit `123dbcbd` built twice through the Catalog Author SDK against the generated exact `doloctown-24456188-moreequipmentslots-v1` reference fixture. Both pre-hover-fix ZIPs were byte-identical at SHA-256 `6ACD0DBD1AA553F52F42FC7530D63674CFDE52E7D5AE1D774276CF665D52E32E`; the seven-file package had one `206,848`-byte Product DLL, version `1.0.0`, minimum Runtime `0.6.0`, and the exact Advanced policy/owner receipt. The SDK first rejected ambient public build `24650773` with `SDK202`; no policy was relaxed or regenerated. Runtime hover correction changes Product bytes, so this hash is preserved only as failed-candidate evidence and is no longer releasable.
- The first locked UI transaction stopped before process launch because the late independent-route gate still counted the intentional title-lifecycle and `MoreEquipmentSlotsNoNativeSave` participants as unrelated cases. Its atomic wrapper restored the original local package tree exactly (`B47EA3E2F57BD1FA28CC1D6E155A2049C26778C716A5D8CBC0E70A8699D46FE3`) and released the shared lock. The gate now admits only the exact three-part 1.0 combination; PowerShell syntax and the complete save-mode focused suite pass after the correction.
- The second locked transaction launched public `24650773 / 1.00.03` against the unchanged candidate and retained [GAME-SMOKE/20260811-043639](../../../debug/evidence/GAME-SMOKE/20260811-043639). Runtime logs prove the exact local candidate loaded under the approved drift boundary, all five Hooks installed, and three Product clones rendered. The first non-empty `1920 × 1080` collapsed image shows the fixed entrance to the right of the two-slot official passive maximum without intersecting backpack, close button, or drone controls. Before any cleanup or restoration, `player-save-unchanged-before-cleanup.json` and `committed-sidecar-unchanged-before-cleanup.json` both passed; the process exited, profile and exact original Product tree were restored, and the shared lock was released.
- That run stopped after the first screenshot because the QA fixture treated Unity's asynchronous screenshot write as synchronous, while the actual stage independently rebuilt a stale two-file evidence allowlist instead of the seven-file route projection. The valid PNG appeared after the false-negative and therefore exposed both harness defects. The fixture now waits up to ten seconds for a non-empty screenshot before admitting the next runner input, and stage construction receives the same five dual-resolution evidence paths as route projection. That correction enabled the later pointer root-cause run; neither attempt is acceptance.
- A later clean-foreground attempt retained [GAME-SMOKE/20260811-050642](../../../debug/evidence/GAME-SMOKE/20260811-050642). Its pointer-enter/exit stacks prove the entrance Rect and raycast were reached, then identify Product defects: cloned `DolocUiObject.rectTransform` was null inside `AccessorySlot.ShowEquipmentItemViewer -> HoverBoxGroup.UpdateAnchorPosition`, and name-only `GetMethod("HideHoverBox")` was ambiguous across the public build's three overloads. The Product now binds every cloned slot's runtime `rectTransform` to its owned transform and selects only zero-parameter `DolocAPI.HideHoverBox()`; the physical fixture models both real failure modes. The runner also terminates the combined route immediately after a failed toggle phase instead of entering another title-lifecycle timeout.
- Post-fix `DTMAPI_UNIT_TEST_FOCUS=moreequipment-product`: PASS, including initialized entrance/slot hints and the exact zero-parameter pointer-exit overload. QA Release build remains `0 warning / 0 error`; PowerShell AST and `test-game-smoke-save-modes.ps1` pass with the new fail-fast assertion.
- Clean `8024e9e3` rebuilt through the unchanged exact Author policy as SHA-256 `21AC1FDF222AD5D8B2CFD5FA306BC1282D93FE4BDB32B3AFDCC4183F1C6EA212`, tree digest `3C70477C5AD9E2827DC0667244A06B2D50BEB01E58EF99EFA7CC232595A56632`. Locked [GAME-SMOKE/20260811-054506](../../../debug/evidence/GAME-SMOKE/20260811-054506) proved both hover fixes, real entrance click and `drawer expanded`; the QA then failed closed because the activated drawer escaped the native drone panel. Public `EquipmentBarWidget.prefab` confirms that `drone_panel` owns an enabled `HorizontalLayoutGroup` with `spacing=12` and forced child width/height, while Product had applied `ignoreLayout` only to the entrance. The drawer root now also acquires `LayoutElement.ignoreLayout=true`; `moreequipment-product` focused Unit/physical tests pass and explicitly require both roots to remain outside native layout. The `21AC…A212` bytes are failed-candidate evidence and are not releasable.
- The `054506` fail-fast route exited in about 66 seconds, again proved player archives and committed sidecar unchanged before restoration, restored the original `B47EA3E2…46FE3` Product tree, and released the shared lock. Its incomplete seven-file QA stage remains retained under an exact recovery receipt and must be consumed before the next deployment.
- Clean `47ac1d83` rebuilt through the same exact Author policy as SHA-256 `F546A51F0AD1478E669BB38F14F222D65AA9B2F8ED6D7A833E19DB4827DE6F63`, transaction tree digest `2EDDCF21A578E9210E33E5385BD519F18563843872261180F51C5F7E0C4ACDA9`, and Product DLL SHA-256 `66407B0E8C60086764DF7F541F03F1B8C1C7BFC8D48DB5F16D052680531C9AF9`. The package still contains exactly seven files, one 1.0.0 Product entry, minimum Runtime 0.6.0 and the unchanged `doloctown-24456188-moreequipmentslots-v1` receipt.
- Locked [GAME-SMOKE/20260811-055523](../../../debug/evidence/GAME-SMOKE/20260811-055523) produced all five images. Manual image inspection and the first four Rect reports pass both `1920 × 1080` and `1024 × 768` collapsed/expanded geometry, official passive maximum `2`, backpack capacity `40`, navigation, drone modal isolation/restoration and config disable/re-enable. The fifth reopen PNG instead shows the world while its stale native root reports off-screen Rects, so the run is diagnostic `partial`, not acceptance. The same run then failed gameplay readiness because the fixture counted the frozen Host's four targets instead of the current Product's five Hooks. Player archives and committed sidecar passed the pre-cleanup invariant; the complete QA stage, Product tree, profile and lock all cleaned exactly.
- The QA authority now shares the current five target identities across UI/gameplay/no-save/cold/owner-deactivation readiness and derives all expected counts from that array. Reopen must keep the entrance and drone owner inside the viewport for five consecutive stable frames; every snapshot additionally requires all official, backpack, close-button, drone and Product Rects inside the current viewport and asserts both Product roots remain `ignoreLayout`. QA Release build and the full `DTMAPI.QaUnitTests` pass with zero QA warnings/errors; `DTMAPI_UNIT_TEST_FOCUS=moreequipment-acceptance-routing` also passes (the aggregate Unit project retains ten unrelated DebugConsole nullable warnings). Managed test sessions were removed by the tracked cleanup protocol.
- A first post-commit launch was externally interrupted after process start because its parent command had a five-second orchestration timeout. [GAME-SMOKE/20260811-060929](../../../debug/evidence/GAME-SMOKE/20260811-060929) is non-acceptance: no UI evidence was produced. It nevertheless proved the player archives, committed sidecar and Local11 profile unchanged. The exact retained QA files and interrupted Product transaction were recovered only after matching their receipt hashes plus original `B47EA3E2…46FE3` and candidate `2EDDCF21…CDA9` tree digests; process, QA stage, transaction root and Runtime lock returned to zero.
- Locked [GAME-SMOKE/20260811-061243](../../../debug/evidence/GAME-SMOKE/20260811-061243) passed the complete UI subgate. All seven expected evidence files archived exactly; five screenshots and every native/Product Selectable Rect pass `1920 × 1080`, `1024 × 768`, official passive maximum `2`, Product slots `3`, backpack `40`, modal drone isolation/restoration, navigation, config disable/re-enable and stable close/reopen. The new fifth image visibly contains the reopened equipment panel, and the runtime published `Smoke.EquipmentSlotsUiObservation=verified` only after five stable in-viewport frames.
- The same `061243` combination then failed before its first native withdrawal: the real `grandmas_button` is `ItemFunctionPassive` with a non-empty skill, but Product compared its CLR full name against `DolocTown.Config.ItemFunctionPassive` rather than the real `DolocTown.Config.Item.ItemFunctionPassive`. Static review confirms the `.Item` namespace is unchanged across tracked 237, 242, 244, 245 and 246 builds; the physical fixture had mirrored the same wrong namespace. Product constants, direct policy tests and the native fixture now use the real Passive/HerbPackage full names while preserving active, cooldown and skill-less rejection. `moreequipment-product` and `moreequipment-acceptance-routing` pass; managed sessions cleaned. Since Product bytes changed, `F546…DE6F63` is historical failed-candidate evidence, not releasable.
- `061243` again proved player current/prev/bak and committed sidecar unchanged before cleanup; the complete QA stage, profile, original Product tree and Runtime lock restored exactly. The DebugConsole give occurred only in memory and the admission failure preceded `CostItem`, so no durable sidecar or native-save mutation was admitted.
- Clean `8c4344f5` rebuilt through the unchanged exact Author policy as SHA-256 `1AD74EBDCE492A35C9659144C635742F0C9E72D864E61E4FE8D60EA45C65444A`, transaction tree digest `8509BE312F4724F98FD76BE6B2831E251A17F4C4D790928BE89CB176DE833F97`, and Product DLL SHA-256 `D911EB0558B00196C454D39FD02AA335C3227059E537D3AF11B7F54F0385394F`. It retains exactly seven files, one 1.0.0 entry, minimum Runtime 0.6.0 and the same `doloctown-24456188-moreequipmentslots-v1` receipt.
- Locked [GAME-SMOKE/20260811-062040](../../../debug/evidence/GAME-SMOKE/20260811-062040) again published the complete UI subgate as `verified`, then proved the namespace correction under public native data: `grandmas_button` passed Product admission/equip and `box_hat` passed give/replacement into a live shield Working entry. QA stopped before shield damage because its helper searched the frozen four-parameter `BodyController.OnAttacked`; tracked 237–246 actually expose `(float,bool,Vector2,AttackProperties,out bool)`. This is QA drift, not a reason to restore the old Host attack prefix or add a sixth Product Hook.
- The QA helper now selects exactly the five-parameter method, requires `DolocTown.AttackProperties`, supplies its public static `Physical` value, and validates `arguments[4]` as the out-bool death result. This exercises the current real chain `BodyController -> AgentEquipmentManager.TryGetShieldItem -> Product IAgentEquipmentShieldItem` without owning the native attack tail. QA Release build and full `DTMAPI.QaUnitTests` pass with zero warnings/errors; source assertions lock the five parameters, `Physical` and fifth out-bool index. Product source was not changed, so the next Author build must remain byte-identical to `1AD7…544A`.
- `062040` again proved player archive and committed sidecar unchanged before cleanup; all seven QA evidence files, profile, original `B47EA…46FE3` Product tree and Runtime lock restored exactly. The failure preceded the attack call's native mutation and no native save was entered.
- QA-only commit `ac627f70` rebuilt the Product byte-identically as `1AD74EBD…C65444A`; entry DLL, seven-file package and exact reference receipt were unchanged.
- Locked [GAME-SMOKE/20260811-062611](../../../debug/evidence/GAME-SMOKE/20260811-062611) passed the entire bounded third-save `NoNativeSave` combination from the start. Manual inspection of all five images confirms no non-drone overlap at either resolution and a visible stable reopen; every state reports exact viewport containment/navigation and only the expanded drawer occupies the drone owner. Runtime terminal facts are UI `verified`, gameplay `verified`, G5 `verified`, title lifecycle `verified`, five Hooks `5/5`, passive `grandmas_button` equip, shield replacement, damage `80 → 40`, break, post-break equip/unequip, native counts restored, Committed unchanged and `nativeSaveRequested=false`.
- Before any restoration, current/prev/bak and both committed-sidecar paths were unchanged. The seven-file QA archive, stage membership, Local11 profile, original `B47EA…46FE3` Product tree, process and Runtime lock all cleaned exactly. This closes the current candidate's UI and player-third-save NoNativeSave subgates without borrowing any earlier candidate evidence.
- Disposable normal-save attempt [GAME-SMOKE/20260811-064033](../../../debug/evidence/GAME-SMOKE/20260811-064033) stopped before Product load because the first outer helper omitted the redirected fixture's `MODS` and `mod_infos.json`; no Product mutation or cold phase occurred. A clean replacement fixture then loaded the exact candidate and five Hooks in [GAME-SMOKE/20260811-064433](../../../debug/evidence/GAME-SMOKE/20260811-064433), but the legacy QA route incorrectly required a durable sidecar before its first Product mutation. Both runs exited and restored the original Product/profile/lock; they are non-acceptance.
- A missing Product-v3 file is a valid first-run baseline. The QA now reads the runtime's empty current Committed document only for the pre-mutation slot choice; every post-`SaveSaved` assertion still requires `TryReadValidated(sidecarPath, scope, ...)` from disk. QA Release build is zero-warning/zero-error, full QA Unit and the save-mode focused suite pass. This is QA-only, so the frozen Product ZIP remains `1AD74EBD…C65444A`; the disposable normal-save/cold route must rerun from the start.
- [GAME-SMOKE/20260811-065006](../../../debug/evidence/GAME-SMOKE/20260811-065006) then loaded the exact Product/five Hooks and passed the corrected empty-sidecar precondition, but the QA inventory helper found neither a DebugConsole Product inside the isolated fixture nor a Runtime release manifest for its frozen-Host fallback. It failed before `GiveItem`, withdrawal, sidecar write or native save. The next fresh fixture adds the current DebugConsole Product solely as the QA native-item helper; the frozen EquipmentSlots Host remains outside the 1.0 normal-save gate.
- [GAME-SMOKE/20260811-065512](../../../debug/evidence/GAME-SMOKE/20260811-065512) loaded the exact Product plus current DebugConsole helper, successfully gave both native items, equipped `grandmas_button`, replaced it with `box_hat`, and retained five-Hook `5/5`; it then failed before the first native save because the normal-save QA helper still required `BodyController.OnAttacked` to return `handled=true`. That attack-tail return belongs neither to the Product five-Hook set nor to Product-v3 save semantics and was already excluded with the frozen Host boundary.
- The normal-save QA now calls the real `AgentEquipmentManager.TryGetShieldItem(out IAgentEquipmentShieldItem)` and requires the provider to come from the exact Product assembly with the expected positive shield value/percent. It then saves the equipped shield, unequips the shield hat through Product, equips/unequips the passive item, saves the empty sidecar and removes both QA-staged items back to their exact native baselines. Neither normal-save phase calls `BodyController.OnAttacked`; the accepted `062611` NoNativeSave run remains the independent real damage/break evidence. QA Release build and full QA Unit pass with zero warnings/errors; Product source and frozen candidate bytes remain unchanged. A fresh disposable normal-save/cold run is still required.
- [GAME-SMOKE/20260811-070554](../../../debug/evidence/GAME-SMOKE/20260811-070554) then passed the disposable normal-save route for the unchanged candidate: exact Product provider and five Hooks, two real native saves, full-shield then empty Product-v3 commits, exact QA-item cleanup, marked-fixture deletion and clean process/lock exit all passed.
- The fresh independent cold route failed at [GAME-SMOKE/20260811-070648](../../../debug/evidence/GAME-SMOKE/20260811-070648) only after `ColdPrepare` completed its real Sleep save and staged the cold seed. The intentionally disabled Product correctly demanded the preserved frozen recovery Host, but the broker could not see the installed `release-manifest.json` because smoke redirects `runtime.Paths.DtmApiPath` to the disposable fixture. The runner now acquires the shared lock, validates the installed 0.6.1 manifest's single frozen-Host receipt and the referenced component's exact path/length/hash, then projects only the unchanged manifest bytes into the fixture. It records both hashes and `CopiedComponentBytes=false`; Product, Host, schema and candidate bytes are unchanged. All three cold phases must rerun from a fresh fixture.
- Fresh aggregate [GAME-SMOKE/20260811-072147](../../../debug/evidence/GAME-SMOKE/20260811-072147) completed `ColdPrepare -> ColdCommit -> ColdObserve` from the start. `072005` and `072056` each used one foreground-verified Enter and observed a real native save; the Product-disabled middle process passed frozen Host demand/destination and recovered exactly one `grandmas_button`; `072147` used `NoNativeSave`, retained exactly that one native item, observed empty terminal Product-v3 authority and zero replay/session state, then passed archive/committed-sidecar unchanged gates before deleting the whole marked fixture. The trust projection records installed Runtime `0.6.1` / `db5e518a6d7f`, manifest SHA-256 `460960EC…DC04A`, component SHA-256 `63853864…F89F` and `CopiedComponentBytes=false`. Original `B47EA3E2…46FE3`, candidate `8509BE31…33F97`, process and shared lock restored exactly.
- The first complete Release attempt on clean `e3ad056c` rebuilt successfully but stopped in aggregate Unit before QA/Doctor and every later gate. Both rejected EquipmentSlots routing cases were independently reproduced under Windows PowerShell 5.1 with exit `1`; only the Unit assertion still expected pre-1.0 error wording. The assertion now requires the current no-Host rejection plus the exact bounded-observer rejection naming `AutoExerciseNewContentApis`; runner, Product and frozen candidate bytes are unchanged. This run is non-acceptance and cannot be resumed or counted as a complete Release PASS.
- After the assertion correction, aggregate Unit and `moreequipment-acceptance-routing` focused Unit pass; tracked cleanup reports zero retained sessions. Catalog `27 / 11 / 22 / 48`, generated registry `12` rows, document governance, `git diff --check` and the frozen candidate hash all pass. The still-unreached Release gates remain pending their one diagnostic tail.
- The unreached diagnostic tail passed QA Unit, Doctor, Runtime package/installer/evidence-retention, Author SDK and the Runtime/install transaction matrices, then exposed the frozen Candidate11 replay still resident in the default suite. That replay owns the historical 0.6 `9 source + 2 retained` Local11 transaction and cannot remain a current-product gate as `currentPublishedArtifact` changes after this replacement. Its helper, dedicated test, evidence and syntax check remain available, but the complete replay is removed from default `test.ps1`; current Catalog/subscription/release-artifact/SDK/ABI gates remain unchanged. No game, shared lock or Product bytes were involved.
- The continued tail passed Catalog, historical receipt-only Batch6 checks, current Advanced products, exact reference fixtures and deterministic current-published package/release-contract gates, then found the Batch4 inventory still matching the pre-`b0aa552e` AnimalViewer close tokens. The runner already requires two sequenced `new-data` renders followed by one Product-owned native-close/rows/clones-zero receipt and verifies that close occurs after the last render. Inventory patterns/order and its stale negative sample now encode that same atomic protocol; focused semantic boundary passes `25/25` contracts and `149/149` patterns, and all `12 + 1 + 6` meta-negative matrices pass. No runtime or candidate source changed.
- The remainder of the non-authoritative tail then reached exit `0`: Batch5/6 plan/static checks, no-demand/deadline, synthetic and retained ABI, Runtime floor, test-artifact governance and final document governance all pass. Together with the earlier QA/Doctor/package/SDK/install/Catalog/reference/package-contract segment, every gate that the first failed complete run had not reached has now been exercised once. These partial results are diagnostic only; the exact candidate remains `1AD74EBD…C65444A`, and one clean from-start complete Release on the final HEAD is still mandatory.
- The authoritative final run then executed `tools/scripts/test.ps1 -Configuration Release` from the start on clean HEAD `eae8c424b1fe691a9cdb4b0a6adbe643c70eccd1` and passed with exit `0` in `957.321` seconds (`2026-08-11T08:13:59+08:00` to `08:29:56+08:00`). `complete-release.result.json` records identical HEAD and empty Git status before/after, identical candidate SHA-256 `1AD74EBD…C65444A` before/after, `DolocTownProcessCountAfter=0`, and `RuntimeLockExistsAfter=false`. The only stderr line was the managed-session cleanup-pending notice emitted during Unit execution; the tracked cleanup manifest subsequently reported zero candidates and the session path absent. This is the sole complete Release PASS and closes the pre-upload repository gate without claiming Steam publication.
- Read-only upload preflight found the existing official folder at `C:/Users/Administrator/AppData/LocalLow/RedSawGames/DolocTown/MODS/DTMAPI_MoreEquipmentSlots` with its exact 33-byte `workshop.json`, SHA-256 `EF4FD946…6543D`, naming Workshop `3744059735`. Combining that byte-preserved control file with the seven candidate files yields eight files, `212,189` bytes and `DTMAPI-Retained-SHA256SUMS-v1` tree `2081E5F6BE0F91F755DBAD6DE45ADD27703C2BAA0301C1F56556FD1959509EB4`; this exact tree alone is now the temporary Catalog mutation entrypoint.
- The first locked upload-preparation attempt stopped during read-only target validation because the ephemeral helper used invalid PowerShell object-array addition. It created no staging sibling and moved no target; a direct audit reconfirmed nine old files, old DLL SHA-256 `EC00CD03…E0A48`, preserved `workshop.json`, zero transients, zero game process and a released lock before retry.
- The corrected locked transaction expanded only candidate `1AD74EBD…C65444A` into a fresh direct sibling, wrote the captured original `workshop.json` bytes, required the exact eight-file set/tree plus one `207,360`-byte DLL `D911EB05…5394F`, manifest `DTMAPI.MoreEquipmentSlotsMod / 1.0.0 / minimum 0.6.0`, Advanced policy `doloctown-24456188-moreequipmentslots-v1`, then atomically exchanged the official folder and removed the previous sibling only after the final audit passed. Result: `PreparedNotSubmitted`, eight files, `212,189` bytes, tree `2081E5F6…509EB4`, zero transients, zero game process and a released lock.
- Independent read-only post-check reconfirmed that exact upload tree and preserved `workshop.json`. The actual Steam subscription `D:/steam/steamapps/workshop/content/2285550/3744059735` remains the untouched nine-file `0.3.1-dtmapi` tree `E0854CEE…E0CA6`; this proves preparation did not masquerade as publication or alter the player artifact.
- The user then reviewed the real equipment screenshot and replaced the entrance/drawer decision with direct dynamic continuation after the last active official slot. This does not retroactively erase the earlier design, failures, accepted runtime evidence, complete Release PASS or upload-preparation transaction; it makes all of those exact `1AD74EBD…C65444A` UI/package results historical and non-authoritative for the new source. The Catalog upload authorization was therefore revoked before any Steam submission.
- Current source owns one `LayoutElement.ignoreLayout` Product row and three retained `112 x 112` clones. `RenderPassiveItems` reads the actual final two official Rects after native layout, requires reviewed `112 x 112` geometry and `12` spacing, and places the same row `12` units after the current tail; official passive growth `1 -> 2 -> 3 -> 4 -> 5` moves that row by exactly `124` per step without recreating roots. `ClearCallBack` hides it, the next render reuses it, and config disable/title/owner deactivation still destroy Product roots. A sixth unreviewed official passive count, unexpected reviewed geometry, intersection with official slots or the read-only drone boundary hides the Product row. Stable viewport、背包、关闭按钮和全部 native Selectable 相交检查由 QA 在连续五帧稳定后执行，不在原生面板动画中的 render callback 扫描整棵 UI 树。
- The Product does not add `Update`, frame events or per-frame polling. Geometry and navigation are recalculated only from the existing native `RenderPassiveItems` callback and UI lifecycle/config boundaries; hover/click callbacks remain input-driven. The read-only drone Rect is retained solely as a collision boundary—no drone selectable, raycast or navigation state is changed.
- After the player blocker, `DTMAPI_UNIT_TEST_FOCUS=moreequipment-product`, QA Release build, `DTMAPI_UNIT_TEST_FOCUS=moreequipment-acceptance-routing`, and `tools/scripts/test-game-smoke-save-modes.ps1` pass for the revised source. The physical fixture proves native passive `1 -> 2 -> 3 -> 4 -> 5`, exact `112 + 12` continuation, three root identities retained across every render/close/reopen, inherited-listener removal, full native-prefix-plus-three navigation, unchanged drone state and fail-closed count `6`. The QA-only range fixture likewise exercises every supported count before capturing maximum five; a new exact-binary public `24650773` game run remains required, so prior two-slot runtime evidence is not reused as acceptance.
- Product Catalog `27 / 11 / 22 / 48`, generated managed admission registry `12` rows, document governance `6678` checks, evidence-retention tests and `git diff --check` pass through the transient-validator correction. These checks validate repository contracts and the revoked old upload authority; they do not create a new upload authorization.
- Under the shared Runtime lock, the superseded eight-file `2081E5F6…509EB4` official upload tree was replaced atomically with the retained private 0.3.1 payload: `9` files, `539,565` bytes, tree `E0854CEE…E0CA6`. The exact original `workshop.json` SHA-256 `EF4FD946…6543D` was preserved, final transient sibling count and `DolocTown.exe` count were zero, and the lock was released. The Steam subscription, player saves and Product-v3 sidecars were not touched; the removed drawer candidate remains reproducible from its recorded source/Author evidence but is no longer locally submit-ready.
- Clean `fa2fdc47` built through the unchanged exact `doloctown-24456188-moreequipmentslots-v1` fixture as Product ZIP SHA-256 `902890E86F04E16207FF88ACF42DFA9A958E88FA148931FC331FCAB6911695E3`, entry DLL SHA-256 `F9BAC345860FB1ADD18646D40D59A7639B3EFC2BFDBFAE803FEC5E8331591EE2` (`204,800` bytes), and eight-file staged transaction tree `5189F8035C805E888BFC746B9A14AB9C024D1B0D4B23F5A6B7B75B2754F4796B`. The SDK first rejected ambient public `24650773` with `SDK202`; the exact 244 policy was not relaxed or regenerated.
- Locked [GAME-SMOKE/20260811-100346](../../../debug/evidence/GAME-SMOKE/20260811-100346) is a diagnostic `partial`, not acceptance. Its stable reports and inspected images show the natural row clear at both resolutions: `1920 x 1080` Product `[851,616 -> 1211,728]` after official tail X=`839`, and `1024 x 768` Product `[444.873,482.25 -> 666.576,551.224]` after official tail X=`437.483`; neither intersects official equipment, 40-slot backpack, close control or drone owner. Config disable/re-enable first produced a transient whole-Selectable collision warning and recovered on the next native render; close/reopen then sampled the native tween while the panel root was off-screen, produced a transient viewport warning, hid the Product row and left QA waiting for a visible stable reopen until the outer timeout. The layout evidence is good; the run as a whole is not.
- Before forced-exit recovery, `player-save-unchanged-before-forced-exit.json` and `committed-sidecar-unchanged-before-forced-exit.json` both passed for the third-save current/prev/bak family and committed Product paths. The process was then stopped, the exact retained 0.3.1 Product tree and original Local11 profile were restored, QA/activation/transient roots returned to zero, and the shared lock was released under `dynamic-row-timeout-recovery.json`.
- Root cause is validation ownership, not a need for a frame loop: `RenderPassiveItems` is a stable owner for official tail/count/slot geometry, but it can run while the surrounding equipment panel is still tweening. The correction removes render-time equipment-panel containment and whole-native-Selectable enumeration, retaining only reviewed official geometry plus official-slot/drone-boundary collision checks. QA still fails release on any settled viewport escape, backpack/close/all-native intersection or unstable reopen after five stable frames. Product source still contains no `Update`, `LateUpdate`, `FixedUpdate`, frame-event subscription or per-frame polling.
- Clean `efd7d252` rebuilt through the same exact 244 policy as seven-file ZIP SHA-256 `0F4BD87B5F899FC74648A202EFF1430F17D9AEF8AA149C7C19D03B11EB97527F` (`76,213` bytes), entry DLL SHA-256 `8CC36D1F73DDCDA497182B91A271C424B515FDE05ED08E4A47F73E2E1ADAF566`, Advanced receipt SHA-256 `AC1E4346E13B2A6A239D3969578ADE531CBF97B83B4614869035152DEEC704DD`, and exact eight-file local transaction tree `D02C035B2C0731415243E01B3E042E5A77A81C496E472B5D0ECC9C38DC89AC85` (`206,305` bytes including the preserved `workshop.json`). It remains `DTMAPI.MoreEquipmentSlotsMod / 1.0.0 / minimum Runtime 0.6.0` with no bundled native dependency or old 0.3.1 DLL.
- Locked [GAME-SMOKE/20260811-103047](../../../debug/evidence/GAME-SMOKE/20260811-103047) passed from the start on public `24650773`. The QA status is `verified`: `1920 x 1080` and `1024 x 768` both show two official passive slots followed immediately by three Product slots; the stable reopen deliberately changes official passive count to `1`, and the same retained Product roots move left by one native step. Every snapshot reports `Intersections=none`, `NativeTailOrder=exact`, `NavigationProjection=native-plus-three`, `ViewportContainment=exact`, `IgnoreLayout=true` and `PerFramePolling=none`; configuration disable/re-enable is `0/0/0 -> 5/1/3` and close/reopen reuses the roots. Manual inspection of all three non-duplicate PNGs confirms the row stays above the 40-slot backpack and clear of the close/drone surfaces.
- The same run passed real `grandmas_button` equip, `grandmas_button -> box_hat` replacement, typed shield `80 -> 40`, shield break, post-break equip/unequip, native inventory restoration and five Hook `5/5`; it requested no native save and left Committed `generation=0/occupied=0`. Before runner cleanup, the complete third-save current/prev/bak family and both committed-sidecar paths were byte/hash/mtime unchanged. QA stage, Local11 profile (`0247C4C4…09D`), process and lock cleaned exactly; the wrapper restored the official Product to retained 0.3.1 `9 / 539,565 / E0854CEE…E0CA6` and left zero transaction roots.
- The first exact-binary disposable attempt [GAME-SMOKE/20260811-104209](../../../debug/evidence/GAME-SMOKE/20260811-104209) is non-acceptance. The outer fixture copied `SAVE`/`DTMAPI` but initially omitted its redirected `MODS` roots and exact fixture enablement, so the native source snapshot marked MoreEquipmentSlots disabled and QA stopped at `productAssemblyLoaded=false` before give, withdrawal, sidecar write or native save. This repeats the already documented isolation boundary rather than exposing a Product regression; process, live profile, retained 0.3.1 tree and lock restored exactly, and the failed fixture was deleted after diagnosis.
- Fresh [GAME-SMOKE/20260811-104433](../../../debug/evidence/GAME-SMOKE/20260811-104433) then loaded only the exact `0F4BD87B…7527F` Product and current DebugConsole inventory helper inside a marked AutoCloud-isolated fixture. It passed Product provider/five Hook readiness, two real native saves, full-shield then empty Product-v3 commits, Product-owned shield/passive unequip, exact QA-item cleanup, fixture deletion, title lifecycle, process exit, live profile restoration and retained 0.3.1 restoration. No frozen attack-tail behavior was reintroduced into this gate.
- Fresh aggregate [GAME-SMOKE/20260811-104904](../../../debug/evidence/GAME-SMOKE/20260811-104904) completed `ColdPrepare/104716 -> ColdCommit/104810 -> ColdObserve/104904` from the start against the same exact seven-file payload. Two real Sleep saves seeded and then terminally cleared the Product-v3 journal; the Product-disabled middle process demanded only the frozen recovery Host and recovered exactly one `grandmas_button`; final `NoNativeSave` observation proved the item remained, terminal authority/replay/session state were empty, archive and committed sidecar were unchanged before cleanup, and the whole marked fixture was deleted. Installed Runtime `0.6.1 / db5e518a6d7f` manifest SHA-256 `460960EC…DC04A` was projected byte-exactly, frozen component SHA-256 `63853864…F89F` was verified in place, and `CopiedComponentBytes=false`. The wrapper restored retained 0.3.1, process and lock to zero.
- The first dynamic-candidate complete Release orchestration on clean `a31676ca` was interrupted outside `test.ps1` because a temporary wrapper promoted the known managed-session cleanup stderr notice to a terminating error. A corrected from-start invocation then passed build, aggregate Unit, QA Unit, Doctor, Catalog and Runtime package/source-boundary gates before `test-runtime-workshop-installer-061.ps1` stopped at its intentional lock-contention case: the mutex correctly rejected the second install, but script-wide `ErrorActionPreference=Stop` converted the expected native stderr into a terminating `NativeCommandError` before the assertion could inspect its non-zero exit. This was a test capture defect, not a live installer lock, Product failure or resumable Release PASS.
- The installer helper now scopes only the BAT invocation to `Continue`, restores the caller policy in `finally`, and still asserts the exact non-zero code/message. A fresh focused Runtime package plus complete `0.6.1` installer matrix passed. The diagnostic tail also exposed that an early temporary wrapper was using Windows PowerShell 5.1 even though deterministic Author SDK ZIP creation explicitly requires PowerShell 7; final orchestration now uses the current `pwsh`, while WinPS 5.1 remains only the required syntax/player-installer child host. Exact evidence-cleanup, Steam-manifest and download-marker hash sites were moved to `Get-DtmApiFileSha256` so those bounded portable helpers no longer depend on an optional cmdlet.
- `tools/scripts/test.ps1 -Configuration Release -StartAt PostRuntimeInstaller` then passed the entire previously unreached tail with exit `0` in `685.868` seconds (`2026-08-11T11:09:36+08:00` to `11:21:02+08:00`). It covered evidence retention, Windows PowerShell child checks, Author SDK, Runtime/install transactions, Catalog/current release contracts, exact reference fixtures, nine-product deterministic double builds, Batch4/5/6, no-demand/deadline, synthetic and retained ABI, Runtime floor, artifact governance and document governance. The runner labels this path non-authoritative; no game or shared Runtime lock was used, and candidate SHA-256 remained `0F4BD87B…7527F`. One clean from-start complete Release on the committed final HEAD is still required.
- The authoritative replacement run then executed default `tools/scripts/test.ps1 -Configuration Release` from the start on clean HEAD `e42a284f50d243f0c5821adab43fc5a9e8afb1d6` and passed with exit `0` in `940.285` seconds (`2026-08-11T11:23:01+08:00` to `11:38:41+08:00`). The receipt records identical HEAD and empty tracked status before/after, identical candidate SHA-256 `0F4BD87B…7527F`, `DolocTownProcessCountAfter=0`, and `RuntimeLockExistsAfter=false`. This single run re-executed every gate from build/Unit through installer, Author SDK, deterministic current release contracts, Batch4/5/6, ABI and final document governance; it did not resume or splice the diagnostic tail. The sole stderr line was the known managed Unit-session cleanup-pending notice; tracked cleanup then reported zero candidates and the named session path absent.
- Read-only preparation preflight revalidated the frozen seven-file candidate (`206,272` bytes) and the existing official folder's byte-preserved 33-byte `workshop.json` (`EF4FD946…6543D`). Their only authorized composition is eight files, `206,305` bytes, tree `D02C035B…AC85`, one `201,728`-byte entry DLL `8CC36D1F…AF566`, manifest `DTMAPI.MoreEquipmentSlotsMod / 1.0.0 / minimum 0.6.0`, and exact policy `doloctown-24456188-moreequipmentslots-v1`. Catalog now authorizes this package/tree only; the official folder and Steam subscription remain untouched until the locked preparation transaction.
- The first locked preparation attempt stopped before creating a stage because a StrictMode diagnostic eagerly accessed `.Name` on an empty transient-sibling array. The second stopped after building but before moving the target because the same diagnostic pattern accessed `.Stream` on an empty ADS array; its catch verified the exact candidate tree and deleted the owned stage. After each attempt, independent read-only checks proved the official and subscription trees both remained `9 / 539,565 / E0854CEE…E0CA6`, with zero transaction siblings, zero game process and a released lock. These are no-mutation orchestration failures, not Product/package failures.
- The corrected locked transaction at `2026-08-11T11:45:34+08:00` verified clean authorization commit `69bdced5`, the retained official/subscription baselines and byte-exact `workshop.json`, expanded the frozen ZIP into a same-volume direct sibling, rejected reparse points/ADS/path traversal, and required the exact file/tree/DLL/manifest/receipt set before mutation. It atomically renamed the retained target to a backup, renamed the stage to the official folder, re-audited the target and unchanged subscription, then removed the validated backup. Result: `PreparedNotSubmitted`, eight files, `206,305` bytes, tree `D02C035B…AC85`, one DLL `8CC36D1F…AF566`, original `workshop.json` `EF4FD946…6543D`, zero transient siblings, zero game process and a released lock.
- An independent post-check reconfirmed the official dynamic tree and identity while the actual Steam subscription remained nine files, `539,565` bytes, tree `E0854CEE…E0CA6`, entry DLL `092807CC…71ED`. No Steam content path or current public/subscription authority was changed, and no submit action was invoked.
- The player's `2026-08-11 14:49` pre-publication retest then invalidated `0F4BD87B…7527F`: with two official accessory bags, `RenderPassiveItems` supplied three passive sprites and Product logged `The native accessories bar must expose one or two reviewed passive slots`, retaining `clones=3` but publishing `visible=False layoutBlocked=True`. Functional equip/save observations remain useful history, but the candidate and all prior PASS claims are insufficient for native counts three through five.
- Commit `be917f29` changed this lifecycle back to `in-progress/partial/open` and returned Catalog `releaseStop` to `ActiveNoUploadAuthorization` with zero public mutation entrypoints before any Steam submission. Catalog, generated admission registry, document governance (`6678`) and `git diff --check` passed for that revocation.
- The retained rollback transaction then verified all ten private Product archives, acquired the shared Runtime lock, and atomically replaced the invalid official eight-file tree `D02C035B…AC85 / 206,305` with the retained 0.3.1 nine-file tree `E0854CEE…E0CA6 / 539,565`. The byte-preserved `workshop.json` remained `EF4FD946…6543D`; the Steam subscription independently remained `E0854CEE…E0CA6`; transient siblings, `DolocTown.exe` and the released lock all ended at zero.
- A subsequent user screenshot sets the new bounded product promise: support four official accessory bags, meaning five official passive slots followed by three Product slots. The screenshot also shows the native horizontal row can continue beyond that bound and approach/escape the right viewport, so counts above five remain explicitly unsupported rather than being inferred safe.
- Clean `5c0a1765` rebuilt through the unchanged exact 244 policy as seven-file ZIP SHA-256 `E2EBF0C34DDB500422E906756B0BA1B1AB6864D820962A738BC34AED8620CFE8` (`76,228` bytes), entry DLL SHA-256 `699E95BC05E79F67EE45D83C89D8119EB2BE723FF342CF2DA0CD0A2C7DBC8E31` (`201,728` bytes), and Advanced receipt SHA-256 `07ED617F1E87902B6A9668358F2CBA718EA753FD830111D457D3D293938AC920`. It still contains one `DTMAPI.MoreEquipmentSlotsMod / 1.0.0 / minimum Runtime 0.6.0` entry, no old DLL or Host bytes, and the exact `doloctown-24456188-moreequipmentslots-v1` receipt.
- Locked [GAME-SMOKE/20260811-151705](../../../debug/evidence/GAME-SMOKE/20260811-151705) passed from the start on public `24650773`. The same retained Product row exercised official passive `1 -> 1 -> 2 -> 3 -> 4 -> 5 -> 1`; at maximum it exposed five official plus three Product passive slots, `AccessoriesSelectables=10`, five Hooks, `NativeTailOrder=exact`, `NavigationProjection=native-plus-three`, `Intersections=none`, `ViewportContainment=exact`, `IgnoreLayout=true`, and `PerFramePolling=none`. At `1920 x 1080` the Product row was `[1223,616 -> 1583,728]`; at `1024 x 768` it was `[673.966,482.25 -> 895.669,551.224]`, remained inside the viewport, and ended about `9.854` pixels above the drone boundary. Manual inspection of both maximum screenshots and the one-slot reopen confirms the complete equipment panel, 40-slot backpack and close control remain visible without overlap.
- The same `151705` process passed real passive/hat/shield equip, replacement, damage/break, unequip, configuration disable/re-enable and title cleanup with no native save. Before any runner cleanup, the third-save current/prev/bak family and both committed-sidecar paths were byte/hash/mtime unchanged; QA/profile/process cleanup passed. The outer transaction then restored the exact nine-file subscription-identical old entry. Its outer `info.json` already says `1.0.0`, but the actual loaded manifest remains `0.3.1-dtmapi`; release facts therefore continue to follow the internal entry and tree rather than the premature Workshop metadata string.
- Fresh [GAME-SMOKE/20260811-152206](../../../debug/evidence/GAME-SMOKE/20260811-152206) loaded only exact candidate `E2EBF0C…0CFE8` and the current DebugConsole inventory helper inside an AutoCloud-isolated fixture. Product provider/five-Hook readiness, two real native saves, full-shield then empty Product-v3 commits, Product-owned shield/passive unequip, exact QA-item cleanup, fixture deletion, title lifecycle, process exit and live-profile restoration all passed.
- Fresh aggregate [GAME-SMOKE/20260811-152639](../../../debug/evidence/GAME-SMOKE/20260811-152639) completed [ColdPrepare/152457](../../../debug/evidence/GAME-SMOKE/20260811-152457) -> [ColdCommit/152549](../../../debug/evidence/GAME-SMOKE/20260811-152549) -> `ColdObserve/152639` from the start against the exact seven-file SDK payload. Two real Sleep saves seeded and terminally cleared the journal; the Product-disabled middle process demanded the frozen recovery Host and recovered exactly one `grandmas_button`; final `NoNativeSave` proved the item remained, terminal authority/replay/session state were empty, archive and committed sidecar were unchanged before cleanup, and the marked fixture was deleted. Installed Runtime `0.6.1 / db5e518a6d7f`, manifest `460960EC…DC04A`, frozen component `63853864…F89F`, and `CopiedComponentBytes=false` were reverified.
- One preceding cold orchestration stopped before acceptance because its temporary local mount incorrectly combined the exact seven-file SDK package with upload-only `workshop.json`; the existing cold preflight correctly rejected that eight-file set. No Product/save defect was observed. The rerun kept the two authorities separate: seven files for package/cold validation, and eight files only when a future authorized upload tree preserves `workshop.json`.
- The authoritative repository gate then ran `tools/scripts/test.ps1 -Configuration Release` from the start on clean HEAD `296b83be4136fe74b4907b57cec57f4de63229c7` and passed with exit `0` in `990.487` seconds (`2026-08-11T15:32:36.0342822+08:00` to `15:49:06.5214401+08:00`). It re-executed build, aggregate Unit/QA/Doctor, installer/package, Author SDK, Catalog/current product contracts, Batch4/5/6, retained ABI, artifact governance and final document governance. Postflight found zero managed cleanup candidates, no named Unit session, zero `DolocTown.exe`, a free Runtime lock, clean Git state, and unchanged package SHA-256 `E2EBF0C…0CFE8`; this is the sole complete Release PASS for the official-one-through-five source.
- Read-only upload preflight combined only the frozen seven-file SDK package with the official folder's byte-preserved 33-byte `workshop.json` (`EF4FD946…6543D`). The authorized composition is exactly eight files, `206,305` bytes, tree `BFB01CC9F8965673A9057DB969E2A5B0E61E822E17701F6C828066CE08F9B5C0`, one `201,728`-byte DLL `699E95BC…C8E31`, manifest `DTMAPI.MoreEquipmentSlotsMod / 1.0.0 / minimum 0.6.0`, and policy `doloctown-24456188-moreequipmentslots-v1`. Catalog grants no other public mutation entrypoint; this line records authorization, not preparation or publication.
- The locked preparation transaction at `2026-08-11T15:53:36.2627819+08:00` required clean authorization HEAD `bcd2b1ded2d5dce7d9ab10ed6baae5d9910aec5d`, exact retained official/subscription baselines `E0854CEE…E0CA6`, exact candidate `E2EBF0C…0CFE8`, and byte-identical `workshop.json`. It expanded the seven-file package into a fresh same-volume direct sibling, rejected path traversal, reparse points and ADS, required the authorized manifest/receipt/DLL/tree, atomically exchanged the official folder, re-audited the target and unchanged Steam subscription, and removed the verified backup. Result: `PreparedNotSubmitted`, eight files, `206,305` bytes, tree `BFB01CC9…F9B5C0`, entry DLL `699E95BC…C8E31`, zero transient siblings, zero game process and a released Runtime lock.

## Post-Publication Subscription Closeout

- At `2026-08-11T08:14:13Z`, the native Steam manifest at `<SteamLibrary>/steamapps/workshop/appworkshop_2285550.acf` reported `NeedsUpdate=0`, `NeedsDownload=0`, and identical installed/latest manifest `288769556214437710` for Workshop `3744059735`; its item size and update epoch were `206,305` and `1786435662` (`2026-08-11T08:07:42Z`). The complete ACF observation was 51 installed/detail members, `83,937,475` bytes on disk, source SHA-256 `54E210FA…1743D`, and no pending item state.
- The downloaded player artifact at `<SteamLibrary>/steamapps/workshop/content/2285550/3744059735` contains exactly eight files and `206,305` bytes. Under the upload-authorization `DTMAPI-Retained-SHA256SUMS-v1` ordering it is exactly `BFB01CC9…F9B5C0`; under the current-published `DTMAPI-Published-SHA256SUMS-v1` ordinal ordering it is `19443C39…CAA8F`. The different aggregate hashes reflect the two already-defined path-ordering algorithms, not different files.
- The subscription manifest is `DTMAPI.MoreEquipmentSlotsMod / 1.0.0 / minimum Runtime 0.6.0`; the package binding and Advanced receipt point to the same sole `201,728`-byte entry DLL `699E95BC…C8E31` and policy `doloctown-24456188-moreequipmentslots-v1`. The tree has zero reparse points, alternate streams, unexpected executables, old 0.3.1 names, Compatibility Host product bytes or duplicate entries.
- Because every downloaded path and byte matches the exact candidate already used by the official-one-through-five UI, normal-save, 0.3.1 migration/conservation and cold-recovery evidence, no new game launch or save mutation was required for publication identity. This closeout is a read-only subscription audit: no Runtime lock, official upload folder, player save, committed sidecar or Steam-managed subscription byte was changed.
- Catalog now records the current published artifact, the current subscription manifest names this Update as its latest release authority, and the one-shot upload exception is consumed. The retained 0.3.1 artifact remains only as rollback/migration evidence and does not describe current subscription bytes.
- Final validation passed the Product Catalog checker in both PowerShell 7 and Windows PowerShell 5.1, generated-registry consistency (`12` rows), document governance (`6681` checks), `git diff --check`, and a second independent subscription snapshot that recomputed the same manifest, eight-file tree, entry DLL and zero-reparse result. No full Release rerun was needed because neither Runtime/Product source nor the frozen candidate bytes changed.

## Evidence

- [MoreEquipmentSlots 1.0 direct-replacement rebaseline Review](../../reviews/code/2026/20260810-0001-moreequipment-100-current-compatibility-and-code-reaudit.md).
- [Public 1.00.03 / build 24650773 baseline-difference audit](../../reviews/code/2026/20260811-0001-public-10003-baseline-difference-audit.md).
- Player screenshot fact is preserved as text in the rebaseline Review; the temporary screenshot itself is not a repository asset.
- [Partial public-build UI smoke evidence](../../../debug/evidence/GAME-SMOKE/20260811-043639) preserves the valid first screenshot, logs, pre-cleanup save/sidecar invariants, cleanup diagnosis, and exact manual-recovery receipt for the retained QA stage.
- [Public-build hover root-cause evidence](../../../debug/evidence/GAME-SMOKE/20260811-050642) proves real pointer reach, preserves both Product exception stacks and unchanged pre-cleanup player state, and records exact profile/Product/QA recovery.
- [Public-build drawer-layout root-cause evidence](../../../debug/evidence/GAME-SMOKE/20260811-054506) proves successful hover/click/expand followed by native HorizontalLayoutGroup Rect drift, unchanged pre-cleanup player state, exact Product restoration and retained-stage recovery authority.
- [Dual-resolution diagnostic evidence](../../../debug/evidence/GAME-SMOKE/20260811-055523) proves the four core geometry states and exact cleanup, while preserving the invalid off-screen reopen capture and obsolete four-target readiness failure that prevented acceptance.
- [Complete UI and native-admission root-cause evidence](../../../debug/evidence/GAME-SMOKE/20260811-061243) verifies all five dual-resolution UI states and exact cleanup, then preserves the real passive-function namespace failure that invalidated `F546…DE6F63`.
- [Corrected admission and current native attack-route evidence](../../../debug/evidence/GAME-SMOKE/20260811-062040) verifies the complete UI subgate plus real passive/盾帽 replacement, then preserves the stale four-parameter QA attack lookup and exact cleanup.
- [Bounded UI and NoNativeSave acceptance](../../../debug/evidence/GAME-SMOKE/20260811-062611) preserves all five accepted screenshots, exact Rect report, passive/hat/shield transaction, five-Hook status and pre-cleanup unchanged-state proofs.
- [First dynamic-tail diagnostic](../../../debug/evidence/GAME-SMOKE/20260811-100346) preserves the two clean-resolution images/Rects, transient animation false-positive logs, unchanged-before-recovery proofs and exact recovery receipt; it is not acceptance for either the superseded `fa2fdc47` bytes or the corrected source.
- [Corrected dynamic-tail UI and NoNativeSave acceptance](../../../debug/evidence/GAME-SMOKE/20260811-103047) preserves the three inspected screenshots, stable Rect inventory, official `2 -> 1` movement, real passive/hat/shield route, five-Hook status, unchanged-before-cleanup save/sidecar proofs, exact QA/profile cleanup and process exit.
- [Drawer-candidate normal-save acceptance](../../../debug/evidence/GAME-SMOKE/20260811-070554) and [drawer-candidate aggregate cold-recovery acceptance](../../../debug/evidence/GAME-SMOKE/20260811-072147) remain historical exact-binary evidence for the rejected `1AD74EBD…C65444A` UI; they do not authorize the dynamic-row package.
- [Current dynamic-row normal-save acceptance](../../../debug/evidence/GAME-SMOKE/20260811-104433), [current aggregate cold-recovery acceptance](../../../debug/evidence/GAME-SMOKE/20260811-104904), and its [aggregate receipt root](../../../debug/evidence/MOREEQUIPMENT-COLD-RECOVERY/20260811-104714-dynamic-row) close the `0F4BD87B…7527F` save/commit/recovery runtime matrix without borrowing the old candidate.
- [Four-bag player blocker and bounded-resolution Review](../../reviews/manual-qa/2026/20260806-0001-moreequipment-official-slot-growth-review.md), [official-one-through-five UI/NoNativeSave acceptance](../../../debug/evidence/GAME-SMOKE/20260811-151705), [exact-candidate normal-save acceptance](../../../debug/evidence/GAME-SMOKE/20260811-152206), [exact-candidate aggregate cold-recovery acceptance](../../../debug/evidence/GAME-SMOKE/20260811-152639), and its [aggregate receipt root](../../../debug/evidence/MOREEQUIPMENT-COLD-RECOVERY/20260811-152456-five-passive) own the current `E2EBF0C…0CFE8` runtime result. Earlier two-slot rows remain historical and cannot authorize this package.
- [Missing disposable Product root diagnosis](../../../debug/evidence/GAME-SMOKE/20260811-064033), [first-run sidecar QA diagnosis](../../../debug/evidence/GAME-SMOKE/20260811-064433), and [isolated inventory-helper ownership diagnosis](../../../debug/evidence/GAME-SMOKE/20260811-065006) are retained non-acceptance runs; none is spliced into the pending normal-save result.
- The current five-Hook physical fixture covers singleton dynamic-row roots, official passive `1 -> 2` tail movement, inherited-listener removal, native-plus-three navigation, unchanged drone interaction, repeated render/close reuse, config/title/owner cleanup, item admission, current shield behavior, incoming outcome-unknown, save veto/non-replay, and both Product/Host owner orders.

## Rollback Notes

- The retained 0.3.1 artifact remains rollback and migration evidence only. Steam's current subscription must never be edited locally; reverting the published Workshop item would require a new user-authorized upload, a new bounded lifecycle Update, and a subsequently observed Steam manifest.
- A source rollback may revert this Update's Product/UI/test/package changes as one unit, but it cannot rewrite the published fact. Catalog and subscription authority must continue to describe observed 1.0.0 bytes until Steam actually delivers a different manifest.
- Do not restore the rejected second-row layout, modify official `passiveItems`/pool state, or replace the typed shield provider with a copied native attack tail.

## Follow-Up

- This 1.0.0 publication lifecycle is closed. Catalog is back at `ActiveNoUploadAuthorization`; any future MoreEquipmentSlots upload requires a new bounded Update and a new exact existing-Workshop exception.
- Continue to preserve the 0.3.1 migration path and monitor the explicitly unsupported sixth native passive slot. No further publication action is required by this Update.
