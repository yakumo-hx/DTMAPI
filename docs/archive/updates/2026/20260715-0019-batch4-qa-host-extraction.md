# 20260715-0019 Batch 4 Optional QA Host Extraction

- Lifecycle Status: `verified`
- Validation Level: `docs,source,unit,runtime`
- Runtime Validation: `passed`
- Related Issue State: `open`
- Area: batch4/qa-host/smoke/bootstrap/core/gamebridge/runner/release
- Source: user requested the post-Doctor Batch 4 extraction; the 2026-07-16 detailed acceptance review reopened the premature final closure and the 2026-07-17 corrective G8 closed it
- Primary review: `docs/reviews/code/2026/20260715-0011-batch4-qa-extraction-dependency-map.md`
- Primary route: `docs/reviews/code/2026/20260712-0003-dtmapi-full-boundary-audit.md`

## 2026-07-16 Acceptance Correction

The G0-G7 commits and their passing tests/runs remain valid behavior, activation, recovery and package-exclusion evidence, but the completion inference was wrong. Detailed source review `docs/reviews/code/2026/20260716-0001-batch2-batch3-batch4-detailed-acceptance-review.md` found 25 files / 12,733 physical lines of QA scenario code still compiled into production GameBridge, versus 15 files / 3,227 lines in the optional QA source. Production retains case dispatch, scenario state, mutations/assertions, status/evidence composition, screenshots, forced GC, save/load and process control; those are not narrow native fixture seams.

`DtmApiRuntime` also exposes two public fixture controls, including owner-root suppression, and the final G7 runs all staged the optional participant. Earlier no-receipt/no-QA controls predate the final G7 production changes. Batch 4 therefore remains in progress and requires corrective G8 extraction, a semantic source/IL absence gate, removal of public fixture authority, and a final post-G7 no-QA/no-receipt player run.

Clean committed HEAD `7e62d050` passed the full tracked Release suite in 582.1 seconds, and a fresh five-DLL Runtime candidate passed the isolated Workshop/PowerShell 5.1 matrix with zero blockers. Those results prove build/test consistency and package exclusion; they do not prove that QA case bodies left the player GameBridge.

## Objective

Extract the embedded Smoke/Hook test surface into an explicitly staged optional `netstandard2.0` QA host without adding a sixth player Runtime assembly, a production-to-QA reference, an ordinary-player settings poll, or any authority over unknown player files. Preserve the runner protocol and scenario evidence while migrating dependency-closed groups, then delete the embedded harness only after the extracted path passes.

## Frozen Boundaries

- Player Runtime remains the same five game-loaded assemblies; QA DLL/PDB/settings/receipts are never part of the Workshop/player package.
- Runner activation is explicit, versioned, hash-bound, once-only, and fail-closed. Missing, damaged, stale, or mismatched QA never falls back to partial or dual execution.
- QA references only narrow production-owned internal host/native facades. Production contains no QA scenario policy or case bodies; corrective G8 makes this a source/IL-gated Release invariant.
- Title/shutdown cleanup removes QA update/lifecycle/listener/Hook ownership before the runner restores any save, enablement, configuration, or staged payload.
- ISSUE-010 and ISSUE-011 remain open. Extraction and minute-scale Smoke are not GC evidence and do not replace the independent AutoFishing and ActionSpeed ladders.

## Migration Groups

- G0 freezes the exact post-Doctor eleven-public-product enabled/disabled Steam baseline and five-DLL/no-QA inventory.
- G1 adds the optional host, trusted activation receipt, staging, exactly-one-owner rule, close protocol, and player-package exclusion.
- G2 moves the settings/result protocol, all four performance probes, and the AutoFishing performance orchestrator as one dependency-closed unit.
- G3 moves low-coupling CustomEntity, diagnostics, FishRoe and lifecycle notifications; it does not own SaveSlots UI.
- G4 moves Bootstrap/UI/SaveSlots/Animal/audio/camera observation after separating production UI repair.
- G5 moves action/equipment/world mutation, including ChestLocator, with setup/cleanup receipts.
- G6 moves fishing, owner/save/load, long-trend, GC and native-Hook probes.
- G7 deletes the old embedded scheduler and removes QA project/static references, payload and ordinary-player polling, but the 2026-07-16 correction found that it did not finish physical case-body extraction.

## Changed Files

G1 adds the optional host and its first reviewable boundary:

- `src/DTMAPI.GameBridge.DolocTown.QA/**`: optional `netstandard2.0` QA assembly, fixed factory, settings protocol, and participant.
- `src/DTMAPI.BepInExBootstrap/QaHostActivationLoader.cs` and `BootstrapPlugin.cs`: fixed-path receipt validation and hash-bound in-memory activation before bridge composition.
- `src/DTMAPI.GameBridge.DolocTown/QaHost/**`, `DolocTownGameBridge*.cs`, and `AssemblyInfo.cs`: narrow participant/facade contract, Runtime-started lifecycle publication, one-shot update/listener ownership, and fail-closed shutdown.
- `src/DTMAPI.GameBridge.DolocTown/Smoke/SmokeHarness.cs`: missing ordinary-player settings are probed once instead of every frame during the G1 compatibility lane.
- `DTMAPI.sln`, `Directory.Build.props`, `tools/scripts/build.ps1`, and `tests/DTMAPI.UnitTests/Program.cs`: QA build/version projection and activation/lifecycle/close-failure tests.
- `tools/scripts/run-game-smoke.ps1`: fixed run-root staging transaction, activation-last receipt, six-state runId-scoped lifecycle gate, exit-first exact cleanup, and recovery receipts.
- Catalog, package, install, status, preflight, release-contract, and upgrade-transaction scripts: recursive QA exclusion plus five-production-DLL/zero-QA-reference gates.
- This Update, `docs/debug/regressions/smoke-matrix.md`, and `docs/debug/evidence-retention-allowlist.json`: G1 lifecycle, real-game evidence, and the generated retention inventory.

G2 adds the first dependency-closed extraction unit:

- `src/DTMAPI.GameBridge.DolocTown.QA/Diagnostics/**` and `Performance/**`: the runtime-memory trend, same-thread allocation, Unity memory and fishing-performance probes plus their DTO/result/serializer authority now live only in the optional QA assembly.
- `src/DTMAPI.GameBridge.DolocTown/QaHost/**`, `Smoke/Cases/AutoFishingSmokeCase.cs`, and the QA participant/settings: production retains only the action driver and scalar snapshot seam; QA owns performance settings, measurement, result export and terminal statuses. Positive FishLoop now consumes the runner target/warm-up values and stays active until the QA target completes.
- `Smoke/SmokeHarness.cs`: only the boolean action/measurement pairing gate remains in production; the five profile/target/duration fields have a single QA settings owner.
- `DolocTownGameBridge.cs` and `.Hooks.cs`: EnabledNoRod arms a restoration receipt before its first native selection mutation and verifies idempotent restoration on failure, ReturnedToTitle and shutdown.
- `tests/DTMAPI.QaUnitTests/**`, UnitTests, `build.ps1`, `test.ps1`, and `run-game-smoke.ps1`: direct QA tests run in a separate process so the G1 activation test remains compile-time independent; protocol 2, `100/5` positive-target completion, failed-close/write retry, PowerShell staging and player-package exclusion are gated.

G3 adds the low-coupling read-only/observation unit:

- `src/DTMAPI.GameBridge.DolocTown.QA/G3/**`, `QaHostSettings.cs`, `QaHostFactory.cs`, and `QaHostParticipant.cs`: protocol 3 owns an owner-bound in-memory CustomEntity registration/disposal contract, transient FishRoe tooltip observation, diagnostics snapshot/export, and SaveLoaded/SaveSaved/Workshop-reload observations. CustomEntity exposes no runtime verbs; FishRoe is never placed in inventory/world state; diagnostics require non-empty mod rows and explicit ready feature/Hook identities.
- `src/DTMAPI.GameBridge.DolocTown/QaHost/**`: the production facade exposes only the narrow owner-bound registration, tooltip observation, and diagnostics seams needed by the optional host. The CustomEntity fixture session retries one failed owner cleanup and verifies all four registries return to zero.
- `Hooking/DolocTownHookCallbacks.cs`: production persistence/Runtime callbacks retain their ordering and independent failure isolation; QA notifications run only after their required production receipts succeed. A false Workshop capture receipt remains expected degradation rather than a duplicate Error.
- `Smoke/SmokeHarness.cs` and `tools/scripts/run-game-smoke.ps1`: G3 cases are mutually exclusive with their embedded rollback lane, require an explicit positive save slot, and use a pure G3 SaveLoaded route. Smoke auto-exit now closes the participant synchronously before `Application.Quit`; the close and quit paths are exception-isolated so even a failed close cannot suppress the exit request.
- `tests/DTMAPI.UnitTests/Program.cs` and `tests/DTMAPI.QaUnitTests/Program.cs`: lifecycle order, pre-SaveLoaded title continuation, deterministic pre-quit close, failed-close still-quits, shutdown idempotence, owner cleanup retry, diagnostics settings, callback isolation, and executed runner routing are covered.

G4 adds the Bootstrap/UI/content/audio/camera observation unit:

- `src/DTMAPI.GameBridge.DolocTown.QA/G4/**`, `QaHostParticipant.cs`, and `QaHostSettings.cs`: protocol 4 owns title/official/pause/DebugConsole/SaveSlots/Animal/audio/Hatch/camera/content observations, runner-marker arbitration, and continuous post-save HomePage terminals. DebugConsole and audio behavior remain real product/player actions; QA only observes breadcrumbs and captures evidence.
- `Features/UiDiagnostics/NativeUiLayoutRepairService.cs`, `ReflectedTitleMenuSettingsUi.cs`, `WorkshopContentInputUi.cs`, and the native UI callbacks: repair stays in production while optional QA receives read-only observations. The obsolete diagnostics service was replaced by the narrow repair owner.
- `QaHost/DolocTownGameBridge.G4*.cs`, `GameBridgeFixtureAccess.cs`, and `G4CameraPlayableFixtureScenario.cs`: bounded owner/receipt seams cover native UI, SaveSlots continuation, read-only Animal rows, audio/Hatch receipts, content absence, and independent `AgentPosition` plus native `CameraController.position2d` capture/restore. Deterministic camera QA uses the native owner's unconstrained `ForceSetPosition` and never changes the production Camera API.
- `Smoke/SmokeHarness.cs` and `tools/scripts/run-game-smoke.ps1`: every migrated G4 case is mutually exclusive with its embedded rollback lane. The external-input route requires Steam, no HookProbe, no install, an exact Workshop YConsole artifact, OS-level input, token-bound READY/terminal handshakes, atomic same-directory marker publication, byte-identical save/profile restoration, and a fixed marker directly under the explicitly authorized runner evidence root.
- `tools/scripts/common.ps1`, ordinary UnitTests, and QA UnitTests: process-aware log waits, exact evidence routing/cleanup, title generation order, failure-close retry, UI ownership, native camera state, PowerShell syntax, and all G4 route constraints are gated.

G5 adds the action/equipment/world-mutation unit:

- `src/DTMAPI.GameBridge.DolocTown.QA/**`, `QaHost/DolocTownGameBridge.G5Fixtures.cs`, and the narrow fixture facade: protocol 5 owns an exact ordered whitelist of 20 ActionSpeed, OneAction, official content/equipment, feature probe, save and debug mutation cases. Every terminal publishes setup/commit/cleanup ownership and fails closed; no unknown DLL is loaded, moved, deleted or adopted.
- `Smoke/Cases/**`, `ContentSmoke.cs`, and `DebugConsoleSmoke.cs`: transient resources/equipment and internal QA owners replace player-world fallbacks. Equipment-slot evidence explicitly enters and closes native `EquipmentBarUiState`; Oil/Mine intermediate receipts are cached; AdvancedDebug preflights both native spawn hosts before its first mutation; every case-local owner/transient is removed at terminal while production orphan recovery remains intact.
- `tools/scripts/run-game-smoke.ps1` and `common.ps1`: the runner captures the save triple, Runtime config tree, smoke settings and official enablement before QA staging; waits for stable process exit; restores byte-for-byte; and emits exact verification receipts. Process-aware log waits now stop promptly after an observed smoke quit even when a later wait did not itself observe the process.
- QA/ordinary UnitTests gate protocol projection, exact routing, no real-world fallback, production receipt names, native bottle `UseItemContinues`, safe cross-room footprints, internal owner cleanup, equipment UI ownership, fail-before-write AdvancedDebug preflight, and the external recovery transaction.

G6 adds the fishing and lifecycle/owner/save-load unit:

- `src/DTMAPI.GameBridge.DolocTown.QA/**` and `QaHost/DolocTownGameBridge.G6Fixtures.cs`: protocol 6 owns exact mutually exclusive fishing, Mod/Zoom owner, save/load, pending-pressure and long-title cases plus root-isolation and native-continuation projection. Gameplay cases fail closed until the production SaveLoaded callback reaches the participant; ModOwner stability is measured from the QA-issued ReturnHome request through continuous HomePage observation.
- `Smoke/Cases/AutoFishingSmokeCase.cs`, `FishingSmokeShared.cs`, and `LegacyFishingAutomationCompatibilitySmokeCase.cs`: the primitive route exports its own diagnostics and lifecycle terminal, while the old-DLL route removes only its synthetic owner and proves a pre-existing retained compatibility owner neither disappears nor grows.
- `Smoke/SmokeHarness.cs`, GameBridge callbacks and the fixture facade: the embedded G6 scheduler is mutually excluded whenever QA owns the route, the pre-exit gate waits for `Smoke.QaG6Lifecycle`, and production SaveLoaded/SaveSaved/Workshop ordering remains unchanged. The rollback lane remains available until G7 deletion.
- `tools/scripts/run-game-smoke.ps1`: protocol-6 staging projects all case settings, collects legacy and primitive terminals independently, waits for SaveLoaded across every extracted owner/long-title lane, and binds Zoom deactivation to the exact Published11 Workshop identity. Results accept the intentional restart-required state of an already loaded Mono DLL only after all Core/GameBridge roots and Camera leases are zero.
- QA/ordinary UnitTests gate callback order, exact route ownership, post-save ReturnHome timing, long-title continuous HomePage prerequisite, old-ABI owner preservation, current primitive cleanup/export, runner branch coverage, root/native projection and exact Published11 Zoom identity.

G7 completes the optional-host boundary and deletes the rollback scheduler:

- `src/DTMAPI.GameBridge.DolocTown/Smoke/**` is deleted and the old scheduler/settings path is gone. However, most scenario bodies were renamed/moved under production `QaHost/Fixtures/**`; case routing, state, mutations/assertions and evidence/process controls remain player payload debt pending corrective G8.
- `QaHostContracts.cs`, `DolocTownGameBridge.FixtureLifecycle.cs`, the optional factory/participant/settings, and the G4-G6 fixture facades implement the versioned participant dispositions `Continue`, `RequestInitialSaveLoad`, `RequestReturnHome`, and `RequestQuit`. The bridge accepts and continues a requested load only while the receipt-bound optional participant is active, delivers the title boundary once, and uses one deterministic participant-close/application-quit route.
- `BootstrapPlugin.cs`, Core Runtime paths, GameBridge update/lifecycle code, tests, and `run-game-smoke.ps1` remove `smoke-settings.json`, embedded-disable switches, ordinary-player diagnostics polling, and virtual-key pressure. Automation now fails closed unless the runner explicitly stages the optional QA host; the legacy settings filename remains only as rejected player-package residue.
- `tools/release/dtmapi-product-catalog.json`, Catalog/release gates and Unit/QA tests reject the optional QA artifact/reference in the player package, and the installed Runtime remains exactly five production DLLs. The Catalog now records `PartialOptionalHostWithPlayerFixtureDebt` because artifact exclusion did not prove semantic case-body removal.
- `disable-smoke-settings.ps1` is deleted. The runner owns QA staging, evidence and exact recovery; the Runtime no longer creates, reads, writes, backs up, restores, or polls that file.

## Validation

- Pre-Batch-4 Player Doctor closure is commit `f91bf06` and its exact tree passed `tools/scripts/test.ps1 -Configuration Release`.
- G0 first attempt `GAME-SMOKE/20260715-235010` retained valid failure evidence: all eleven public products passed Entry/Begin/Commit `1/1/1`, provenance, refresh, lifecycle, restoration and exit, but the exact owner set also contained the Catalog `NeverPublish` fixtures `DTMAPI.HelloDtmMod` and `DTMAPI.ConfigMenuExample` left in the game `Mods` folder.
- The normal Runtime-only installer transaction moved those two known development fixtures to `DTMAPI/backups/disabled-testmods-20260715-235316`, reinstalled Runtime `0.5.5`, and preserved the public package/enablement boundary. The installed status/Doctor path then passed with the exact five DLLs, exact three Doctor files/hashes, seven read-only artifacts, zero errors and zero warnings.
- G0 enabled `GAME-SMOKE/20260715-235342` passed exact eleven owners, Entry/Begin/Commit `1/1/1`, provenance, no-op refresh, owner/title lifecycle, save/profile restoration, no HookProbe, no fatal, and process exit. Missing-frame/stall/resubscribe/player-loop-refresh counts were `1/1/2/4` and recovered.
- G0 disabled `GAME-SMOKE/20260715-235512` passed the exact empty public-owner set with Entry/Begin/Commit `0/0/0`, title lifecycle, save/profile restoration, no HookProbe, no fatal, and process exit. Warning counts were again `1/1/2/4` and recovered.
- Runtime lock was released after the matrices, `DolocTown.exe` was absent, and ISSUE-010/011 remain open. These minute-scale runs are not GC evidence.
- Static G1/G2 audit confirmed the composition-participant boundary and found the pre-existing positive AutoFishing performance-target protocol break now owned by G2.
- The first G1 Release replay exposed and fixed a PowerShell StrictMode empty-array regression in installed status; the focused Runtime-only uninstall matrix then passed. A subsequent exact-tree Release run passed in `601.7s` before the live-review hardening.
- Cross-review then caught that pre-`Runtime.Start()` monitor output was discarded and same-HookId lifecycle states could coalesce. G1 now publishes a dedicated runId-scoped, non-coalesced lifecycle trail only after Runtime startup; failures remain explicit and participant `Close` exceptions finish as `failed-closed` instead of a false successful close.
- QA assembly activation now uses `Assembly.Load(validatedBytes)`, so the bytes loaded are the bytes covered by the receipt; unit coverage also proves the one-shot reserved identity and close-failure classification. Bootstrap and UnitTests build with zero warnings/errors, and `DTMAPI.UnitTests: OK` passed after this hardening.
- `GAME-SMOKE/20260716-004812` passed normal Steam/slot 3 without HookProbe: `validated`, `activated`, `attached`, `started`, `updated`, and `closed` were each present once in strict order for the staged runId; `failed=0`; close details proved listener removal, participant clearing and successful close. The activation receipt, QA DLL and settings matched their recorded lengths/SHA-256 and the exact tree was removed only after stable process exit.
- `GAME-SMOKE/20260716-004919` passed the matching normal Steam no-receipt control. It contained zero QA lifecycle lines, and after exit the activation file, QA root and `DolocTown.exe` were all absent.
- The installed-package status/Doctor path passed after both runs: exact five production DLLs, exact three Doctor files/hashes, seven read-only artifacts, zero Doctor errors/warnings, and no QA residue. The shared Runtime lock was released.
- The first post-review Release replay rebuilt all projects and passed UnitTests, Doctor, packaged Doctor, and retention behavior, then correctly stopped because the generated evidence-retention allowlist did not yet contain the new QA sources and two G1 smoke runs. `build-evidence-retention-allowlist.ps1` regenerated that tracked inventory; the subsequent clean full Release replay passed and G1 was frozen as commit `c7b8172`. G2 was later frozen as `0ee28df`; G3-G7 continued in this lifecycle.
- G2 focused Release validation passed with zero warnings/errors: ordinary UnitTests retained the strict pre-load activation boundary and passed in `70.7s`; the separate QA test process passed in `2.0s`; Windows PowerShell 5.1 parsed the runner; release-contract and product-Catalog gates retained exactly five player Runtime DLLs and eleven public products. Core, GameBridge, Bootstrap and ordinary UnitTests contain zero QA AssemblyRefs.
- G2 review found and closed three P1s before runtime acceptance: incomplete or failed result writes now force host `failed-closed`; the runner's performance/legacy/Mono/ordinary lifecycle pattern is one assigned branch; and EnabledNoRod selection restoration is receipt-bound across failure/title/shutdown. Profile values are also canonicalized case-insensitively. Measurement and terminal writes commit their state only after both file and status publication succeed, and are retryable after an injected write failure.
- `GAME-SMOKE/20260716-015136` was a valid prelaunch block: the AutoFishing scenario requires its explicit fifth-save fixture, so no game/Runtime mutation occurred for the requested slot 3. `GAME-SMOKE/20260716-015244` then exposed build-output pollution from `New-SmokeQaHostStage`: the native build output joined the returned stage object, so StrictMode cleanup could not read `Artifacts`. No game process launched; official profile restoration passed; after stable process absence every retained artifact and directory member matched `qa-host-stage.json`, and only those three exact artifacts plus empty owned directories were removed. The runner now captures native build output and returns only the typed stage receipt.
- `GAME-SMOKE/20260716-015625` passed the repaired no-SkipBuild Steam lane with save fixture 5, CoreOnly, no HookProbe and `InactiveNoConsumer` measured for one second. Protocol-2 lifecycle markers were exactly `validated/activated/attached/started/updated/closed = 1/1/1/1/1/1`, ordered, with `failed=0`; exact cleanup removed the hash-bound activation/DLL/settings tree only after stable exit.
- The exported QA result recorded `TargetFish=0`, `MeasuredFish=0`, zero casts/fish/accessor/native session roots, completed runtime memory trend, `TitleCleanupVerified=true`, and `owner=qa; fallback=false`. Mono exposed the allocation method but its calibration did not advance, so only that subprobe was explicitly `blocked-allocation-counter-nonfunctional`; the independent result remained verified. Known missing-frame/stall/resubscribe/player-loop-refresh counts were `1/1/2/4` and recovered; no new gameplay failure, Fatal GC, fresh crash or residual process occurred.
- The installed-package self-check passed after G2: exact five production DLLs, exact three Player Doctor files/hashes, seven read-only Doctor artifacts, zero errors/warnings and no QA activation/root residue. The shared Runtime lock was released after every attempt and recovery.
- The final exact-tree `tools/scripts/test.ps1 -Configuration Release` replay passed in `531.1s`, including every production/QA build, ordinary and QA UnitTests, Player Doctor packaged entrypoints, evidence retention (`390` source files, `719` smoke runs, `62` runtime identities), Author SDK portable/release gates, PowerShell 5.1, Runtime install/uninstall/upgrade transactions, developer install recovery, Catalog and the five-DLL/eleven-product release contract.
- G3 focused Release builds completed with zero warnings/errors; ordinary UnitTests and the separate QA UnitTests passed. Windows PowerShell parsed the runner, the executed G3 routing self-test selected the pure SaveLoaded branch, `git diff --check` passed, and Core/GameBridge/Bootstrap retained zero QA AssemblyRefs.
- Cross-review closed the G3 lifecycle/callback risks before runtime acceptance: the pure runner branch now records SaveLoaded success; prerequisite callback failure cannot notify QA; diagnostics reject empty expected-feature/mod evidence; CustomEntity retries and verifies real cleanup; FishRoe aggregation requires a verified terminal; and Workshop capture/Runtime/legacy receipts remain independent. The final review found no remaining P0/P1/P2.
- `GAME-SMOKE/20260716-025903` is retained failure evidence. All three G3 probes and QA SaveLoaded observation passed, exact stage cleanup and Published11 restoration passed, and the process exited, but `closed=0`: the only `ReturnedToTitle` preceded SaveLoaded and the later direct `Application.Quit` produced no observable Unity shutdown callback. The runner correctly failed only the QA lifecycle gate.
- The deterministic pre-quit close fix is unit-covered for ordering, close failure, exactly-one quit, and later shutdown idempotence. `GAME-SMOKE/20260716-031242` then passed normal Steam/slot 3/Published11 without HookProbe: protocol-3 `validated/activated/attached/started/updated/closed` markers were each exactly one, ordered, with `failed=0`; CustomEntity, FishRoe, diagnostics, and QA SaveLoaded passed. Exact QA artifacts were removed only after stable process absence, `mod_infos.json` restored byte-for-byte, and the installed tree contained the exact five production DLLs with zero QA references/residue.
- Both G3 runs recorded one missing-frame and one frame-driver stall warning, then recovered. The acceptance run had no gameplay failure, Fatal, fresh crash, forced process residue, or save restoration need. This minute-scale evidence does not change ISSUE-010/011 or count as either deferred GC ladder.
- G4 focused Release builds completed with zero warnings/errors; ordinary UnitTests and the separate QA UnitTests passed, Windows PowerShell parsed the runner, and production projects retained zero QA AssemblyRefs. The normal no-QA Steam control `GAME-SMOKE/20260716-061503` proved production title/settings repair with no activation receipt, QA root, HookProbe, or residual process.
- Final G4 UI/observation acceptances are `064857` (title settings plus official Mod UI), `064946` (Manager Status route), `065422` (pause layout), `084327` (real product-owned DebugConsole UI), `121921` (SaveSlots paging/continuation), `122027` (Manager MVP logs/export refresh), `123837` (19 read-only Animal rows), `125459` (real paper-box audio replacement), and `125805` (child/adult Hatch voice). Each exact stage/profile/process cleanup passed; the Animal route performed no progress seeding or mutation.
- `GAME-SMOKE/20260716-125923` is retained Camera failure evidence: room-constrained `SetPosition` produced native distance `0`; exact artifacts/tree and five valid PNGs were verified and only the recorded stage was recovered. After switching the optional fixture to native-owner `ForceSetPosition(position,false)` and adding a presentation-settle gate, `GAME-SMOKE/20260716-130638` passed 33 telemetry rows, 4x -> 2x -> vanilla arbitration, seven PNGs, independent agent/camera restoration, owner/root zero, exact cleanup, profile restoration and exit.
- `GAME-SMOKE/20260716-130820` passed read-only Oil-absent metadata observation. `GAME-SMOKE/20260716-131231` passed save-load -> ReturnHome -> continuously observed `HomePageUiState` for 1.5 seconds; the terminal timer no longer starts at the request.
- `GAME-SMOKE/20260716-132321` is retained external-input startup failure evidence: the initial marker guard compared the authorized repository evidence root with the unrelated game-side screenshot root, so Bootstrap failed before Runtime/input mutation. The runner restored all three slot-3 files and `mod_infos.json` byte-for-byte; exact staged bytes were recovered under a manual receipt. The fixed guard accepts only the fixed marker as a direct child of the explicitly configured runner root.
- `GAME-SMOKE/20260716-135228` is final G4 external-input acceptance: normal Steam, no HookProbe, exact retained Workshop YConsole, real OS `Y/Escape`, ten short taps and held-key input passed without synthetic input or owner takeover. QA observed the real UI and screenshot, atomically published/consumed the token marker, returned Home, continuously observed HomePage, and closed. Save triple, profile, QA stage and process all restored exactly.
- Every final G4 run had no gameplay failure, Fatal GC, fresh crash, crash dump or residual process. The accepted minute-scale runs continued to show one recoverable missing-frame warning and usually one recoverable frame-driver stall; this does not change ISSUE-010/011 and is not either deferred GC ladder.
- G5 focused QA tests and Release builds passed with zero warnings/errors. `GAME-SMOKE/20260716-151253` passed ActionSpeed tool/interaction plus all four OneAction cases using the published Workshop owners; `151952` passed ChestLocator, StrongPlantingGun and CropHarvesting with internal QA owners and no player-world fallback.
- Final-source Oil/Mine package validation was transactionally isolated from the user's existing packages. `GAME-SMOKE/20260716-152318` passed final Oil metadata, append-only LUT, natural `crude_oil` drop and cleanup. `153607` passed the full Oil/Mine/MoreEquipmentSlots combination, including the real native equipment UI, passive/hat/defense/shield application and recovery, Mine official JSON and production. `153710` independently passed Mine tech-tree UI and production. Original Oil (`9` files) and Mine (`12` files), `mod_infos.json`, install state and release manifest were restored by SHA-256 receipt after stable exit.
- `GAME-SMOKE/20260716-153942` passed ActionSpeed config application. `154035` passed InstantSave, inventory, weather, teleport CSV/transport, time and movement but retained valid AdvancedDebug failure evidence because CoreOnly intentionally lacked the YConsole creative generator. `154152` proved the published YConsole supplied that item, then exposed pre-CurrentRoom spawn-host timing. The fail-before-write preflight fixed that scheduler gap; `154416` is final AdvancedDebug acceptance with creative enable/disable, generator give, monster/resource spawn, exact post-exit restore and no residual process.
- Earlier G5 failures `145017` and `153011` remain diagnostic evidence for duplicate official-ID selection and missing QA equipment owner/UI setup. The repaired runner restored every save/config/settings/profile transaction after each fail-closed run. No final G5 run produced a gameplay failure, Fatal GC, fresh crash, crash dump or residual process. ISSUE-010/011 remain open and none of these minute-scale runs is AutoFishing or ActionSpeed GC evidence.
- G6 focused Release builds completed with zero warnings/errors; ordinary UnitTests and the separate QA UnitTests both passed after the final runner fixes. Protocol-6 source gates cover exact case routing, callback order, title timing, cleanup ownership, Workshop Zoom identity and independent legacy/current fishing result branches.
- `GAME-SMOKE/20260716-164917` passed the current AutoFishing primitive/full lifecycle and diagnostics export; `165054` passed movement cancellation. `170808` loaded the retained old Workshop AutoFishing DLL unchanged and passed its old ABI facade, real legacy-owner preservation, synthetic-owner cleanup, profile recovery and process exit. These short runs are compatibility/function evidence, not GC evidence.
- `GAME-SMOKE/20260716-173744` passed ModOwner save-preservation, QA-issued post-save ReturnHome, Runtime owner cleanup, continuous HomePage stability and exact exit/profile cleanup. `174203` passed Published11 Zoom deactivation with Core roots and Camera leases at zero, retained provider identity, zero cleanup failures and the intentional loaded-DLL `restart-required` terminal.
- `GAME-SMOKE/20260716-174311` passed two title/save cycles, the pre-load forced-GC probe and VersionPatcher native continuation; `174422` passed the bounded five-second pending-pressure route; `174707` passed the five-second long-title continuous-HomePage-before-load gate. `174845` passed one save cycle with `UiRuntime`, `YConsoleZoomNoInputEvents`, Lite snapshots and VersionPatcher continuation together.
- Diagnostic runs `173404`, `173840` and `174523` retained valid runtime terminals but exposed runner classification/identity gaps; the final runs above supersede those exact boundaries. All final G6 runs restored the selected profile, removed only the receipt-owned QA stage after stable process exit, left no process, and reported no new gameplay failure, Fatal GC or fresh crash. Recovered missing-frame/frame-driver warnings remain historical behavior and do not create a new Debug issue.
- ISSUE-010 and ISSUE-011 remain open. Neither the G6 pre-load probe nor any minute-scale G6 run replaces the deferred independent AutoFishing and ActionSpeed `1x -> enabled/no-speedup -> common -> high -> disabled/restore -> title-cycle` ladders.
- G7 focused Release builds completed with zero warnings/errors and ordinary/QA UnitTests passed after every final lifecycle repair. Catalog, release-contract and static source gates prove no old `Smoke/SmokeHarness.cs`, embedded-disable flag, settings reader, QA AssemblyRef, optional QA payload, or ordinary-player input poll remains; they did not detect scenario cases retained under production `QaHost/Fixtures`. Automation without `-StageQaHost` is rejected.
- `GAME-SMOKE/20260716-191455` retained the first deletion-tree defect: a transient Gameplay state made the initial slot-3 request return false, while the participant had already consumed its disposition. The outer runner was interrupted after 420 seconds; the exact save/profile/QA-stage receipts were then recovered manually. The participant now keeps requesting until the production load action accepts, and the bridge records acceptance only after `TryAutoLoadSave` succeeds.
- `GAME-SMOKE/20260716-192916` passed save-load, title and cleanup terminals but exposed repeated `OnReturnedToTitle` replay: the participant closed before it could emit its terminal quit disposition. The bridge now delivers that boundary exactly once and routes immediate close through the same fixture quit method; UnitTests prove close-before-exactly-one-quit and idempotent later shutdown.
- `GAME-SMOKE/20260716-193651` reached the official slot-3 selection but did not continue through the mod-change/fallback load prompt after the embedded scheduler was deleted. A receipt-gated `ContinueInitialSaveLoadForFixture` now runs only after an accepted optional-host request and only until SaveLoaded; ordinary player updates never execute it.
- `GAME-SMOKE/20260716-194746` passed all real external-input behavior—foreground process, exact Workshop artifact, ten short taps, hold, modal isolation, ReturnHome, cleanup, quit and recovery—but the runner still ordered queue-flushed legacy status text and expected the former quit wording. Direct token/cleanup log receipts and the new participant-quit contract replaced that stale classification rule; evidence replay verified the corrected order before the final rerun.
- Final G7 acceptance is `GAME-SMOKE/20260716-194541` (Published11 enabled), `194653` (all eleven public products disabled), and `195027` (normal Steam, no HookProbe, real player input). They passed exact owner sets, refresh/Runtime dispatch receipts, SaveLoaded, post-save ReturnHome, continuous HomePage, participant close/quit, save/profile/QA-stage recovery and stable process exit. Warning counts were respectively missing-frame/stall/resubscribe/player-loop-refresh `1/2/2/4`, `1/1/2/4`, and `1/1/2/4`; all recovered. No gameplay failure, Fatal GC, fresh crash, crash dump or residual process occurred.
- The final source still treats ISSUE-010 and ISSUE-011 as open. The G7 minute-scale matrices are extraction, combination and input evidence only; neither is the deferred independent AutoFishing or ActionSpeed GC ladder.
- The first final-tree Release replay ran `544.7s` and passed every build, ordinary/QA/Doctor/Author test, evidence gate, PowerShell 5.1 path, install/uninstall/upgrade/developer transaction, Catalog and release contract; it stopped only because this active Smoke router reached `19,518` bytes. Completed 2026-07-13 through 2026-07-15 rows moved unchanged to `smoke-matrix-history-20260713-through-20260715.md`; document governance then passed `5,091` checks with the active router at `10,365` bytes, and the regenerated retention inventory passed with `392` source files, `741` smoke runs and `62` Runtime identities.
- The frozen pre-commit candidate tree passed the complete Release suite. The 2026-07-16 review separately ran the same tracked suite on clean committed HEAD `7e62d050` for 582.1 seconds; it passed, closing commit-level provenance while leaving the architectural finding open.

## 2026-07-17 Corrective G8 Closure

G8 closes all four P1 findings from Review `20260716-0001` without changing the five-DLL player package or entering Batch 5:

- G8A adds `tools/release/batch4-production-qa-semantic-inventory.json`, a machine-readable classification of the permitted production host/native facade and forbidden QA policy. `tools/scripts/check-batch4-qa-semantic-boundary.ps1` runs from `test.ps1` and checks the production source set, physical-line budget, forbidden scenario/process/file/GC/status text, built GameBridge metadata/user strings, optional-QA ownership and the built Core public reflection surface.
- G8B/G8C move the G4/G5/G6 partials, all thirteen fixture-case bodies, CustomEntity session, camera/session helpers and fishing-performance contracts into `DTMAPI.GameBridge.DolocTown.QA`. `QaScenarioController` owns scenario routing/state; the production participant contract keeps only lifecycle dispositions and a neutral native driver. Production `QaHost/**` is now exactly four files and 608 physical lines, down from 25 files / 12,733 lines.
- `GameBridgeFixtureAccess` is now an owner-bound neutral bridge/runtime/evidence facade. QA-owned status policy lives in `GameBridgeFixtureStatusExtensions`; native fishing observation and config-edit operations are narrow internal production primitives. Core exposes only internal `ConfigureSaveLoadObjectSnapshotMode` and `ApplySmokeOwnerRootIsolationProfile`; the two public fixture methods no longer exist.
- `DTMAPI.Core` grants friend access only to the optional QA assembly. `DTMAPI.Abstractions` remains byte-identical to the frozen Author SDK (`1773527AB8D28A904C6C185FD8BC4A1502746CB7FA228E27AB80D15370357D44`), so no new public QA seam or ABI deletion was introduced.
- Catalog state is `CompletedOptionalHostSemanticBoundaryVerified`. Its closure text is enforced together with the semantic source/IL gate; package filename/project-reference exclusion can no longer authorize completion by itself.

Corrective validation on the final source:

- Focused ordinary/QA UnitTests passed. The G8 semantic gate reports `productionFiles=4; productionLines=608` and verifies the optional `DTMAPI.GameBridge.DolocTown.QA.dll` owns the required QA metadata/user strings while built GameBridge/Core do not expose the forbidden residues.
- `tools/scripts/test.ps1 -Configuration Release` passed the complete tracked suite in 543 seconds: zero build warnings/errors; ordinary/QA Unit, Doctor, packaged Doctor, Author SDK deterministic/portable, ABI, dual-host install/uninstall/upgrade/developer recovery, Catalog, five-DLL/eleven-product release contract, evidence retention and document governance all passed.
- The retained AutoFishing ABI gate passed with baseline public API count `5105`, candidate count `5251`, `RemovedPublicApiCount=0`, the unchanged old Workshop consumer loaded, all eleven public products resolved and Lamp references stayed zero.
- A fresh Runtime candidate built in a path containing spaces/non-ASCII passed the Workshop release-audit workflow under Windows PowerShell 5.1. Ten scripts parsed; missing/empty/valid/install/check/collect/uninstall/post-uninstall cases passed with `Blockers: 0`; the payload contains exactly the five Runtime DLLs and no QA assembly.
- `GAME-SMOKE/20260717-003431` is the final-source no-QA/no-receipt player lane. The runner installed current Release, launched through Steam with Published11 and no HookProbe, and the foreground player path selected the third archive, accepted the ordinary Mod-change prompt, entered the farm and changed the hotbar selection from slot 1 to slot 2. Runtime recorded `slot=2`, eleven committed owners, empty `QaHostRunId`, no QA root/load attempt, clean stable process exit and exact profile restoration. The five installed DLL hashes match the current Release outputs byte-for-byte.
- QA replay `GAME-SMOKE/20260717-004127` passed exact eleven products enabled plus G6 owner/title lifecycle; `004235` passed the exact disabled eleven-product lane; `004342` passed the migrated G5 ActionSpeed/OneAction world-mutation group; `004504` passed migrated G4 content observation plus CustomEntity/FishRoe/diagnostics. Every run used explicit receipt-bound `-StageQaHost`, cleaned the exact QA tree only after process exit, restored profile/save state and left no QA DLL or game process.
- Warning counts for `003431`, `004127`, `004235`, `004342`, and `004504` were respectively missing-frame/stall/resubscribe/player-loop-refresh `1/1/2/3`, `1/2/2/4`, `1/1/2/4`, `1/1/2/3`, and `1/1/2/3`; all recovered. There was no new gameplay failure, Fatal GC, fresh crash, dump or residual process. These minute-scale runs remain architecture/compatibility evidence, not the independent AutoFishing or ActionSpeed GC ladders; ISSUE-010 and ISSUE-011 stay open.

## 2026-07-17 P1/P2 Boundary and ABI Correction

The post-G8 approval review found that the closure evidence was still broader than the implemented boundary. This correction keeps the five-DLL player package and protocol 6, but removes the remaining concrete scenario owners before re-verifying Batch 4:

- `UiRuntime` profile interpretation and `Smoke.RootIsolation` status composition now live in `QaHostFactory`/`QaHostParticipant`. Production receives only an explicit neutral feature-suppression receipt (`DisableEquipmentSlotsRuntime` plus feature IDs); GameBridge no longer names the profile or publishes its QA status policy.
- VersionPatcher target selection, retry state and `Smoke.NativeLoadContinuationProbe` composition moved from production hook installation into optional `NativeLoadContinuationProbe`. Production retains only the inert callback/breadcrumb endpoints; without the optional QA-installed hooks they are never invoked. The later lifecycle closure below gives every run a distinct QA Harmony owner and an owner-only close boundary.
- owner-root profile mapping, ModOwner save/title assertions, synthetic owner setup and Zoom owner-deactivation/reconciliation moved from `DtmApiRuntime` into the optional G6 scenario. Core now exposes internal neutral lifecycle operations only: explicit owner entry/activation/deactivation, root count, owner-instance observation and root suppression over caller-supplied owners/root types.
- the Batch 4 semantic gate now recursively scans all five production source roots (currently 150 files), compares every QA/fixture semantic line against a machine-readable path/count/SHA-256 classification (currently 25 files), recursively locks the four-file/608-line neutral host, and checks GameBridge/Core/QA built metadata ownership. Schema 3 also enforces seven explicit lifecycle contracts and rejects two old-defect negative samples, so native continuation/probe/breadcrumb/unpatch cleanup and the runner's final cleanup receipts cannot disappear behind unchanged unrelated hashes.
- Default tracked ABI gate: every Release validates the exact built `DTMAPI.Abstractions.dll` against `tools/release/contracts/retained-abi-synthetic-contract.json`. It uses no private artifact and proves only the ten frozen synthetic AutoFishing/Lamp type surfaces, `StopOnManualMove` getter/setter/default/obsolete semantics and the four-type retired Lamp shell.
- Optional private consistency gate: only when all three explicit private-artifact environment variables are supplied does the suite compare the retained baseline and load the retained old AutoFishing/eleven-product binary set; partial configuration fails preflight. This layer was skipped on the current default tree, and a synthetic pass is not cited as a substitute for real retained-binary consistency.
- Catalog no longer instructs the release to restore an already restored property. It records the synthetic default gate and the optional explicit private consistency layer separately.
- the remaining repository-source Player Doctor P2 is closed without weakening player-package ownership: `check-dtmapi-status.ps1` still requires its exact sibling helper in a packaged tools tree, while an invocation from the canonical repository `tools/scripts` root may use the tracked developer build output or the current installed Runtime helper. The portable gate launches the source status entry in a child PowerShell process and rejects the former false `MISSING` result.

Validation for the first boundary correction, before the lifecycle-close follow-up below:

- `tools/scripts/build.ps1 -Configuration Release -SkipTests`: passed, zero warnings/errors.
- The former schema-2 semantic result was `productionSourceFiles=150`, `classifiedSemanticFiles=23`, `neutralQaHostFiles=4`, `neutralQaHostLines=608`. It is retained as pre-fix evidence and is not the final lifecycle gate.
- the immutable synthetic ABI replay passed candidate SHA-256 `1773527AB8D28A904C6C185FD8BC4A1502746CB7FA228E27AB80D15370357D44`, all ten frozen type-surface hashes, `StopOnManualMove` getter/setter/default/`Obsolete(false)` semantics and all four retired Lamp types. With no explicit private-artifact environment supplied, the separate private consistency layer reported a deliberate skip rather than reading untracked reverse data.
- the complete tracked `tools/scripts/test.ps1 -Configuration Release` suite passed on that earlier correction tree. It does not validate the later Hook-close and G6 failure-close changes.
- `GAME-SMOKE/20260717-084559` proved the changed QA-owned native continuation path but did not satisfy owner-root isolation because its selected profile did not load the two target products; that precondition miss is retained as diagnostic evidence, not acceptance.
- `GAME-SMOKE/20260717-084753` explicitly enabled Workshop YConsole and Zoom and passed protocol-6 lifecycle/cleanup, SaveLoadCycle, QA-owned `UiRuntime` policy, all four native continuation Hook pairs and exact owner-root suppression (`InputButton=5`, `EventHandler=7`, `suppressionSucceeded=True`). The profile was restored byte-for-byte, the game exited stably, and no Fatal, fresh crash or residual process remained. One missing-frame and one frame-driver stall warning recovered; ISSUE-010/011 remain open and this short run is not GC evidence.
- the Player Doctor portable/status gate passed both direct self-contained inspection and canonical repository-source `check-dtmapi-status.ps1` resolution; packaged entry-point tests continued to require and execute the package-local three-file helper.

## 2026-07-17 P1/P2 Lifecycle and Gate Closure

The final review found two QA cleanup holes and two record/gate holes after the first correction. This follow-up treats them as part of the same Batch 4 Update rather than opening Batch 5:

- `NativeLoadContinuationProbe` now creates owner `dtmapi.gamebridge.doloctown.qa.native-continuation.<runId>`, never the production Harmony owner. `HarmonyReflectionPatcher` accepts an explicit owner and exposes only exact `UnpatchAll(string ownerId)` cleanup; a zero-argument/global unpatch route is forbidden.
- Participant close executes native-probe cleanup before committing `closed=true`. A failed unpatch publishes `cleanup-failed`, keeps the probe/participant retryable and fails close; a successful close publishes the exact owner, four installed pair states and `unpatchSucceeded=True`.
- G6 synthetic owner setup now rolls back a partial registration. Scenario close independently disposes both Camera lease handles, deactivates both synthetic owners, verifies Core roots, instances and Camera lease counts are all zero, and publishes `Smoke.OwnerLifetimeCleanup=verified`; repeated close is idempotent.
- The semantic inventory now discovers `NativeContinuation`, probes, breadcrumbs and unpatch/Hook-cleanup vocabulary. Seven path-specific lifecycle contracts enforce owner selection, close ordering, exact owner-only unpatch, both G6 owner/lease cleanup paths, their behavioral test, and the runner's final native/G6 cleanup receipts; two frozen old-defect samples must remain rejected.
- The Catalog duplicate `releaseEligibility` keys were removed from both compatibility components. `check-product-catalog.ps1` now counts those keys in the raw compatibility JSON slice before `ConvertFrom-Json`, so PowerShell 5.1/7 cannot silently overwrite a duplicate.
- The tracked synthetic/default ABI result and optional private real-binary consistency result are separate claims in this Update and Catalog. The private layer remains conditional and skipped unless all three explicit paths are present.
- Hook lifecycle ownership is recorded in `docs/hook-map/focused/NativeLoadContinuationQa.md`; current reverse signatures remain unchanged.

Targeted validation completed before the single final Release replay:

- QA project and `DTMAPI.QaUnitTests` built with zero warnings/errors and passed. The new executed case creates two synthetic Core owner sets and one Camera lease per owner, failure-closes twice, and verifies the cleanup receipt plus both Camera counts at zero.
- The schema-3 semantic gate passed under PowerShell 7 and Windows PowerShell 5.1 with `productionSourceFiles=150`, `classifiedSemanticFiles=25`, `neutralQaHostFiles=4`, `neutralQaHostLines=608`, `lifecycleContracts=7`, and `lifecycleNegativeSamples=2`.
- Catalog validation passed under PowerShell 7 and Windows PowerShell 5.1 with exactly two compatibility components and one raw `releaseEligibility` key per component.
- `GAME-SMOKE/20260717-121830` passed normal Steam/slot 3/no HookProbe with the optional QA host, Workshop YConsole/Zoom, SaveLoadCycle and the final close gates. The probe's final status was `closed` under the exact run-scoped owner, all four Hook pairs were installed before close and owner-only unpatch returned `True`. `Smoke.OwnerLifetimeCleanup=verified` reported both synthetic owners with Core roots `0`, instances `False` and Camera leases `0`. Exact QA/profile recovery, no Fatal/fresh crash/residual process and stable exit passed; one missing-frame and one frame-driver stall warning recovered. This short run is not GC evidence.
- The first complete `tools/scripts/test.ps1 -Configuration Release` attempt reached the final document-governance gate after all preceding Unit/QA/ABI/Doctor/transaction/Catalog/semantic checks passed, then correctly failed because this Update still said `in-progress` while its monthly ledger already said `verified`; no product or test assertion failed.
- After synchronizing that lifecycle metadata, the necessary complete Release rerun passed on the final correction tree. The default tracked synthetic ABI gate passed; the distinct private real-binary consistency layer was explicitly skipped because its three environment paths were not supplied.

## Evidence

- G0 failure and acceptance runs are recorded in `docs/debug/regressions/smoke-matrix.md`.
- G1 staged/no-receipt acceptance runs are recorded in `docs/debug/regressions/smoke-matrix.md`; source/unit/package details remain here.
- G2 acceptance is `GAME-SMOKE/20260716-015625`. The prelaunch fixture block and recovered prelaunch staging failure remain under `015136` and `015244` for audit but are not gameplay matrix rows.
- G3 acceptance is `GAME-SMOKE/20260716-031242`. The valid pre-fix lifecycle failure remains under `025903` and is summarized above rather than promoted to a second active matrix row.
- G4 acceptance is the final matrix summarized by `GAME-SMOKE/20260716-061503`, `064857`, `064946`, `065422`, `084327`, `121921`, `122027`, `123837`, `125459`, `125805`, `130638`, `130820`, `131231`, and `135228`. Camera `125923` and marker-boundary `132321` remain failed audit evidence with exact manual recovery receipts.
- G5 acceptance is summarized by `GAME-SMOKE/20260716-151253`, `151952`, `152318`, `153607`, `153710`, `153942`, and `154416`. `154035` and `154152` retain the two final pre-fix AdvancedDebug failure boundaries; each passed the runner's exact external-state restoration gate.
- G6 acceptance is summarized by `GAME-SMOKE/20260716-164917`, `165054`, `170808`, `173744`, `174203`, `174311`, `174422`, `174707`, and `174845`. Diagnostic runner failures remain retained under `173404`, `173840`, and `174523`; they are superseded for their exact gates by the later passing runs.
- G7 deletion-tree acceptance is `GAME-SMOKE/20260716-194541`, `194653`, and `195027`. Pre-fix evidence remains retained under `191455`, `192916`, `193651`, and `194746`; each external-state transaction was recovered exactly, and the later passing run owns its acceptance boundary.
- G8 semantic closure is `GAME-SMOKE/20260717-003431`, `004127`, `004235`, `004342`, and `004504`, plus the tracked semantic inventory/gate and the zero-blocker final candidate package audit summarized above.
- The post-approval P1/P2 correction is accepted by `GAME-SMOKE/20260717-084753`; `084559` is retained as the explicit owner-profile precondition miss.
- The final Hook/G6 lifecycle-close correction is accepted by `GAME-SMOKE/20260717-121830`.

## Rollback

Each G0-G7 group is committed as one reviewable unit. G1-G6 retained a mutually exclusive prior runner route until the replacement group passed; G7 deletes that route and can be rolled back only to the complete passing G6 commit `68b941e`. Corrective G8 is one reviewable move/gate/runtime closure on top of baseline `7e62d050`; rollback it as a unit to reopen the exact review debt. If the game process has not exited, restoration is blocked and the recovery receipt is preserved.

## Follow-Up

Batch 4 is complete through corrective G8A-G8E and the post-approval P1/P2 lifecycle replay. The final semantic gate covers all production source roots plus explicit Hook/G6/runner cleanup contracts, scenario policy remains in the optional QA assembly, the tracked synthetic ABI gate is independent of private reverse data, and the Catalog duplicate-key/ABI wording defects are closed. Do not reinterpret minute-scale runs as GC evidence: the independent AutoFishing and ActionSpeed ladders remain separate later release gates, and ISSUE-010/ISSUE-011 stay open. Batch 5 was not started by this work.

## 2026-07-17 Post-Closure Source Boundary Reaudit

Later Review [20260717-0002](../../reviews/code/2026/20260717-0002-batch4-post-closure-source-boundary-review.md) found two ownership defects which the schema-3 semantic inventory had classified as allowed production behavior: ordinary Y-console/AnimalViewer/EquipmentSlots paths still compose QA evidence, and QA-only/proven-orphan mutation or synthetic-input controls remain in player Core/GameBridge state machines.

The G8 moves, lifecycle cleanup, successful Runtime runs, full Release results and five-DLL/no-QA package evidence above remain valid historical implementation evidence. Their inference that all Batch 4 ownership debt was closed is superseded; the final Follow-Up paragraph above no longer authorizes Batch 5.

Current acceptance work is owned by [Update 20260717-0002](20260717-0002-batch4-source-boundary-reopen.md). Batch 4 is reopened for G9 and Batch 5 remains unstarted.

## 2026-07-17 G9 Resolution

[Update 20260717-0002](20260717-0002-batch4-source-boundary-reopen.md) owns the subsequent G9 closure: it removed the residual production evidence policy and orphan fixture mutation, replaced the schema-3 allowlist model with schema-5 symbol/consumer/forbidden-behavior/lifecycle/negative-sample enforcement, and replayed staged-QA, ordinary no-QA and exact eleven-product runtime lanes. This Update remains the historical G0-G8 implementation owner; it does not duplicate the later validation narrative. Batch 5 was not started, ISSUE-010/ISSUE-011 remain open, and the independent AutoFishing/ActionSpeed GC ladders remain later release gates.
