Goal: add a trial Shell Crab `AnimalVoice` audio replacement JSON using the existing content-pack JSON plus WAV infrastructure. Use `speciesId=shell_crab`; child event `PLAY_ANIMAL_PET_SHEEP_CHILD` maps to `Content/Audio/shell_crab_pet_young.wav`; adult event `PLAY_ANIMAL_PET_SHEEP` maps to `Content/Audio/shell_crab_pet_adult.wav`; keep replacement scoped by animal context and do not change GameBridge code or public API. Sync the JSON and missing WAV files to the local runtime `DTMAPI_ShellCrab` package, then validate JSON parsing and file existence.
