# DTMAPI Public API Stability Matrix

Status date: 2026-08-30

This matrix is the public API stability contract. It is intentionally stricter than the smoke-evidence ledger: a smoke run can prove that a code path executed, but it does not by itself make an API stable for ordinary mods.

## Status Vocabulary

| Status | Meaning | Compatibility Promise |
| --- | --- | --- |
| Stable | Public contract is DTMAPI-owned or has proven native owner coverage. Breaking changes require migration notes, compatibility handling, and a new update record. | Ordinary mods may depend on it. |
| StableCandidate | Contract shape is likely correct, but it still lacks one or more promotion gates such as two real mods, manual QA, or regression matrix coverage. | Ordinary mods may experiment, but should expect final polish changes. |
| Experimental | API is usable for test mods or carefully scoped real mods, but native ownership, lifecycle, conflict behavior, or failure modes are not fully proven. | Ordinary mods should treat it as version-sensitive. |
| Diagnostic | Debug, smoke, report, console, or developer-support surface. It may call powerful native paths, but it is not a stable gameplay API. | Ordinary gameplay mods should not depend on it. |
| Proposed | Design direction or desired API surface with no current stable implementation guarantee. | Do not depend on it. |
| Failed | Existing implementation or evidence is known insufficient or contradicted by manual QA. | Do not use as completion evidence or stable API proof. |
| Disabled | Public shape is retained only to keep old binaries loadable; the provider fails closed and performs no gameplay work. | Do not adopt it. Existing consumers must migrate; physical removal still requires the documented breaking-version warning and scan gates. |
| Retired | Public surface or hook route existed in an experimental branch but was intentionally removed from active runtime/API code. | Do not depend on it; treat old evidence as archive/research only. |

`Blocked` is a qualifier, not a status. A row may be `Experimental` and explicitly blocked for native runtime creation until its GameBridge adapter is proven.

Source metadata keeps two independent dimensions. `DtmApiStatusAttribute` describes stability; the additive `DtmApiDispositionAttribute` describes whether a surface is open for adoption, frozen for compatibility, diagnostic-only, DTMAPI-internal, or disabled. Declarations without disposition metadata are `Open` by default. `Frozen` retains the existing ABI but rejects new capability work and must carry a non-error `[Obsolete(..., false)]` source warning; it does not turn an Experimental API into Stable or start a promised removal release by itself.

## Stable Promotion Criteria

An API may move to `Stable` only after all relevant gates are satisfied:

1. It is backed by a clear DTMAPI owner or a reviewed Doloc Town native responsibility owner.
2. It exposes stable DTMAPI contracts and does not expose raw Unity, Harmony, BepInEx, or copied decompiled game types.
3. At least two real mods, not only a smoke helper, use the API successfully.
4. Game evidence proves the API in the appropriate real gameplay path, usually the third local save unless a task states otherwise.
5. Visual, UI, input, camera, save/load, lifecycle, machine, vehicle, or other player-visible APIs have manual QA or screenshot/video evidence that covers the actual user-facing failure mode.
6. The debug regression matrix has a row for the API's main success path and high-risk failure path.
7. Clean exit evidence exists when the API participates in runtime/gameplay behavior: no leftover `DolocTown.exe` and no Steam waiting-for-exit regression.
8. Known failures, rejected hypotheses, and rollback notes are recorded in the relevant debug, review, hook-map, update, or API contract document.

Static screenshot smoke is allowed as supporting visual evidence, but it is not completion evidence for a visual/UI/camera API unless manual QA or an equivalent targeted visual review covers the same failure mode.

## Native Owner Domain Library Boundary

Future API domains that are not yet represented by a public matrix row should first be checked against `docs/reviews/api/native-owner-domains/INDEX.md`. That library records native owner candidates, official Workshop content/beauty boundaries, and blocked/gap status for broad gameplay/content requests. It is not a stability contract: a report may justify a proposed DTO or GameBridge adapter, but it does not promote an API to `Stable` or `StableCandidate` without the promotion criteria above.

## Internal Product UI Boundary

The title-page DTMAPI Manager UI is an internal product UI over existing Diagnostic and Experimental runtime data. `DtmManagerViewModel`, Manager row formatters, Manager report/export state, and Status Copy Summary are not `DTMAPI.Abstractions` public mod APIs. Their presence in the first-party Bootstrap assembly does not promote `IDtmDiagnosticsApi`, `IUiHelper`, CameraView, FishingAutomation, SaveSlots, or any GameBridge feature API to `Stable` or `StableCandidate`.

Manager UI smoke evidence may prove that support rows, summary text, and report export are visible in the title settings UI. It must not be cited by itself as native-owner proof for gameplay, save/load, camera, fishing, vehicle, equipment, machine, or content APIs.

## Stability Matrix

| Area | API / Contract | Status | Evidence Scope And Current Boundary |
| --- | --- | --- | --- |
| Framework | `DtmMod`, `IManifest`, `IManifestDependency`, `IDtmHelper` | Stable | DTMAPI-owned mod entry and helper container contracts. `IDtmHelper` remains Stable because the container shape is stable, but each child helper surface still follows its own `DtmApiStatus`; helper access does not promote Experimental/Diagnostic child APIs to Stable. Helper lifetime is explicit: Events, Config, ModRegistry and Input are owner-bound mutable services; UI is owner-bound/runtime-thread-only mutable state; Diagnostics permits stale/thread-safe snapshot reads but owner-binds report/evidence writes; Workshop and Content are shared immutable-publication reads; Translation is a per-owner immutable snapshot; Monitor is a per-owner thread-safe diagnostic sink deliberately valid for final cleanup reporting. No Doloc native owner is implied. Manifest/dependency hardening is tracked separately by Core runtime tests. The current release/API source authority is `0.6.1`; unit tests verify `MinimumDTMApiVersion=0.5.5` and legacy `0.4.2`/`0.3.1` requirements load while a future `0.6.2-alpha` requirement is blocked. The earlier `GAME-SMOKE/20260610-121420` and `GAME-SMOKE/20260610-121631` runs remain retained alpha dependency-normalization evidence. Current binary/file metadata is `0.6.1.0`, while the intentionally retained assembly compatibility identity is `0.5.3.0`. |
| Framework | `MinimumDTMApiVersion` compatibility policy | Stable | The compatibility policy remains numeric-only: prerelease/build suffixes are trimmed before `System.Version` comparison. The canonical Runtime authority now separates release/API `0.6.1`, binary/file `0.6.1.0`, and assembly compatibility `0.5.3.0`; retaining the assembly identity does not make `0.5.3-alpha` the current Runtime label. BepInEx plugin metadata remains numeric through `DtmApiRuntime.BinaryVersion = 0.6.1.0`; strict SemVer or an `AllowPrerelease` manifest property is deferred to a future compatibility design. The 2026-06-17 diagnostics wording follow-up does not change numeric comparison or the internal `api-too-new` status code; it only changes player-facing retained error text so users are told the installed DTMAPI runtime is too old and should run `1_install_dtmapi.bat`. |
| Framework | `IManifest.EntryType` | StableCandidate | Public read-only exposure of the already-parsed manifest `EntryType` value. Verified by unit test through the `IManifest` interface on 2026-06-09; runtime entry-type selection already uses the same field internally. This is a small public-surface addition and should stay StableCandidate until the next API review package confirms compatibility. |
| Framework | `IMonitor.Log`, `LogOnce`, `LogException` | Stable | DTMAPI-owned logging contract used across runtime and mods. |
| Framework | `IConfigHelper.ReadConfig<T>`, `WriteConfig<T>`, `GetConfigPath` | Stable | DTMAPI-owned config file contract. The helper is owner-bound: manifest `UniqueID` must equal the helper owner, stale helpers reject access after deactivation, and bad-JSON recovery plus atomic writes remain unchanged. |
| Framework | `IConfigHelper.RegisterMigration<T>` | StableCandidate | Owner-bound registration rejects manifest spoofing and duplicate owner/config-type migration entries; owner deactivation removes migrations. More real migration adoption is still required before promotion. |
| Framework | `IModRegistry.IsLoaded`, `Get`, `GetAll`, `GetApi<T>`, helper-bound `RegisterApi<T>` | StableCandidate | `RegisterApi<T>` binds to the helper owner; duplicate `(provider owner, contract)` registration throws without replacement. Owner-scoped providers return a stable consumer facade when the provider supplies that protocol. This now covers ConfigMenu plus every manifest-bearing GameBridge, Camera, fishing, Core CustomEntity, and DebugConsole route: supplied manifests must match the canonical helper owner, consumer/provider deactivation evicts the facade, and provider-side owner roots participate in the same zero-remaining cleanup proof. The four frozen CustomEntity interfaces have one provider identity, `DTMAPI`; the unsupported duplicate `DTMAPI.GameBridge.DolocTown` alias was removed after source/artifact scans found no consumer evidence. Process-lifetime provider services remain loaded across consumer disable and SaveLoaded/ReturnedToTitle; active-owner reconciliation resolves those providers from the authoritative runtime registry and does not require an ordinary discovered source. Source-managed providers require their refreshed discovered/enabled source, but dependency compatibility uses the canonical loaded registry manifest version. An in-place disk version change is update-pending/source-handoff, not proof that the resident DLL/API was upgraded. Only the consumer graph is removed. A successfully loaded assembly, including a code owner whose selected source identity or manifest version changes, cannot re-enter until restart. A no-assembly/non-code owner may republish only after every platform root is proven zero. This does not promote stability. |
| Framework | `ITranslationHelper.Language`, `Get` | StableCandidate | Per-Mod catalog data remains owner-scoped, while `Language` and `Get` dynamically project the current game locale through the case-insensitive `full region -> base language -> English` fallback chain. Runtime supplies the cached native `DolocAPI.CurrentL10nId` source; ordinary players follow the game language and only the explicit developer environment overrides (`DTMAPI_LANGUAGE` / `DTMAPI_UI_LANGUAGE`) take precedence. Steam/legacy aliases for English, German, French, Japanese, Korean, Brazilian Portuguese, Russian, Simplified Chinese and Traditional Chinese are normalized without changing the public signatures. Reads are thread-safe and stale-helper-safe because they retain no mutable platform registration. Broader locale/manual QA is still required before `Stable`. |
| Events | `IGameLoopEvents.GameLaunched` | StableCandidate | DTMAPI Core dispatch after mod entry. Promotion needs regression matrix coverage for load order and failure isolation. |
| Events | `IGameLoopEvents.UpdateTicked`, `OneSecondUpdateTicked` | Experimental | High-frequency events are fragile. Core has consecutive-failure circuit breakers. D4 keeps `UpdateTicked` aligned with every valid Runtime frame even while a DTMAPI or official modal is visible; modal ownership gates only DTMAPI input because the overlay does not pause native world time. Runtime-thread checks, Unity-frame deduplication and the TimerFallback prohibition remain unchanged. Timing and fallback behavior still require regression rows before stable promotion. |
| Events | `IGameLoopEvents.ReturnedToTitle` | Experimental | Lifecycle event exists, but title/save boundary semantics must stay tied to game evidence and clean-exit checks. |
| Events | `ISaveEvents.SaveLoaded`, `SaveSaving`, `SaveSaved` | Experimental | Reaches important native save/load paths, but save transaction semantics, sidecar writes, failed saves, and reload boundaries remain high-risk. |
| Events | Owner-bound subscription mutation and publication membership | Experimental | Batch 5 keeps the public event signatures unchanged and freezes two observable lifecycle rules. Both `+=` and `-=` mutate owner-bound subscriptions only on the DTMAPI Runtime thread while the owner is active; off-thread or inactive-owner mutation is rejected. A publication captures immutable membership when it begins: ordinary add/remove changes are visible to the next publication, while authoritative owner cleanup or quarantine may cancel an already-captured registration so deactivated or circuit-broken code is not invoked. Deterministic barrier tests cover ordinary removal, owner cleanup cancellation, quarantine and thread rejection. These rules clarify the current Experimental event contract; they do not promote native save/title timing to Stable. |
| Input | `DtmButton`, `DtmButtonState`, `DtmKeybind`, `DtmKeybindList`, `DtmInputScope`, `IInputRegistration`, `IInputHelper.GetState`, `IsDown`, `WasPressed`, `WasReleased`, `IInputEvents.KeybindPressed`, `KeybindReleased`; legacy `ButtonPressed`, `ButtonReleased`, `RegisterButton`, `Suppress` | Experimental | Typed owner-bound scoped registrations remain the ordinary hotkey direction; legacy string hotkeys retain their compatibility wrapper. D4 freezes one internal audience per sampled frame: Normal preserves scoped broadcast compatibility, an owner modal targets only that owner's context-valid non-Gameplay registrations, and a platform/official modal provides no new Mod input. Exact Pressed recipients receive the matching Release across eligibility changes, old configuration bindings live until settlement, and neutral rearm prevents held keys becoming new Pressed. String and typed queries are owner-aware; modal queries create no arbitrary local watch. Suppression hides only current-frame DTMAPI-visible state and cannot erase physical truth or an owed Release. The narrow old-DebugConsole ABI owner route remains frozen. Gameplay sampling drains from native `NormalGameState.OnUpdate`; PlayerLoop is title/non-normal fallback and Timer is health-only. One source consumes each latched native edge, rapid release+press remains valid, and stable Normal/owner-modal 10,000-frame paths allocate zero Core bytes in focused tests. Logical modifier aliases and `Plus`/`Equals` canonicalization remain unchanged. Direct Unity/Harmony input is outside the isolation promise. Keep Experimental pending broader multi-Mod player QA. |
| Config UI | `IDtmConfigMenuApi` registration/query APIs (`Register`, `Add*`, `SetDisplayName`, `GetKeybindConflicts`), optional `IDtmConfigMenuKeybindDefaultsApi`, `DtmColorPreset`; page/item state models used by DTMAPI runtime UI | StableCandidate / optional reset capability Experimental | The public signatures remain unchanged. Ordinary mods receive a stable owner-bound facade which validates every supplied manifest, always writes under the canonical helper owner, rejects duplicate pages/cross-owner mutation, retains the optional keybind-default cast, and rejects use after consumer/provider deactivation. Save/title boundaries preserve active pages; owner deactivation removes the page and callbacks, with same-process re-enable requiring restart. Preview diagnostics are bounded aggregates plus recent scalar failures. Stability is not promoted. |
| UI | `IUiHelper.OpenDtmApiStatusPage`, `OpenModListPage`, `OpenConfigPage`, `OpenErrorPage`, `OpenHookStatusPage`, `ExportLogs` | Experimental | Player-facing mutable DTMAPI UI helpers are exposed through an owner-bound facade. Every call is runtime-thread-only and stale-owner-invalid; off-thread or post-deactivation calls fail before shared UI/report state changes. Manual QA is still required for layout, input blocking, and stale-state/flicker cases before promotion. |
| Diagnostics | `IDiagnosticsHelper.GetErrors`, `GetWarnings`, `GetHookStatuses`, `GetLatestLogPath`, `IDiagnosticsEvents.LogExported`, `HookStatusChanged` | StableCandidate / Diagnostic | Snapshot/path getters are lock- or immutable-publication-backed process diagnostics. They are thread-safe read-only calls and deliberately remain valid through a stale helper; this does not authorize stale owner mutations or treat Hook text as native gameplay proof. `GetLatestLogPath()` remains the current-run `DTMAPI/logs/latest.log`; since 2026-06-16 runtime startup rotates a non-empty previous latest log to retained `latest-*.log` history. Owner-bound diagnostic event subscriptions retain their existing runtime-thread and cleanup rules. |
| Diagnostics | `IDiagnosticsHelper.ExportLogs`, `RecordEvidence` | Diagnostic | Report export and evidence capture write process files, so helper calls are runtime-thread-only and stale-owner-invalid even though read-only snapshots remain shared. `ExportLogs()` includes the 10 newest retained history files under `DTMAPI-history/`. `RecordEvidence` remains a QA/support seam rather than gameplay lifecycle state. |
| Diagnostics | `IDtmDiagnosticsApi.GetSnapshot`, `IDtmDiagnosticsSnapshot`, `IDtmLoadedModInfo`, `IDtmModStatusInfo`, `IDtmFeatureStatusInfo` | Diagnostic | Read-only runtime diagnostics snapshot registered under the `DTMAPI` runtime API owner. It exposes loaded mod rows, discovered/loaded/disabled/error mod status rows, structured `IDtmModStatusInfo.StatusCode`, retained errors, retained warnings, hook statuses, structured GameBridge feature statuses, latest log path, and latest report path without parsing logs or promoting gameplay APIs. Owner deactivation now reports `Status=inactive` / `StatusCode=restart-required`; this authoritative lifecycle state takes precedence over retained prior errors, while those errors remain present in the error list and `Reason`. `IDtmFeatureStatusInfo.FailureCount` remains a cumulative count; consecutive failure and recovery timestamp context is reported through the existing feature-status details string rather than new public snapshot members. Unit tests verify `loaded`, `disabled`, `restart-required`, `missing-dependency`, `dependency-cycle`, `entry-dll-error`, `code-load-error`, `api-too-new`, and `unknown-error` status codes while preserving `Status` and `Reason` text; optional dependency version mismatches now remain `loaded` with warnings instead of becoming error rows. The 2026-06-17 wording follow-up keeps internal `StatusCode=api-too-new` but changes retained error message/details to tell players the installed DTMAPI runtime is too old and to run `1_install_dtmapi.bat`; this is a presentation/diagnostic wording fix, not a new public status code. Latest diagnostics cap evidence `GAME-SMOKE/20260610-115014` verifies `Feature.Camera`, `Smoke.DiagnosticsSnapshot=verified`, `mods=14`, `modStatusCodes=loaded=14`, `errors=0`, `warnings=0`, `features=8`, and matching report `LatestReportPath`; `GAME-SMOKE/20260610-115225` verifies the same snapshot path for `Feature.ActionSpeed`. Unit coverage also verifies 1005 errors/warnings are capped to a 1000-entry retained window and exported reports include a `DiagnosticsTrimmed` summary. Latest feature failure recovery evidence `GAME-SMOKE/20260610-164830` and `GAME-SMOKE/20260610-165043` verifies Camera and ActionSpeed routes still pass after feature status details gained cumulative/consecutive/recovery semantics; earlier failure status throttle evidence `GAME-SMOKE/20260610-133933`, `GAME-SMOKE/20260610-134145`, `GAME-SMOKE/20260610-134259`, and `GAME-SMOKE/20260610-134406` verifies diagnostics feature snapshots stay current while `Feature.<Id>` hook-status publication is throttled. This remains Diagnostic, not Stable. |
| Diagnostics | Diagnostics report aggregate counters | Diagnostic | Internal/report-only repeated diagnostics aggregation. No new public API or snapshot member is exposed. Retained error/warning, Hook, feature, aggregate, and owner-failure fields use centralized per-field limits before key construction/storage; truncation appends a streaming SHA-256 hash and reports saturating net-omitted UTF-16 bytes through internal/report counters. Concrete DTO construction is defensive, snapshot inputs are copied into bounded rows with fixed row caps, overflow buckets cannot overwrite an ordinary caller key, runtime/evidence text caps at 262144 UTF-16 code units, and report summaries cap at 524288. Unit coverage records 1005 repeated errors and warnings, keeps `GetErrors()` / `GetWarnings()` capped to the latest 1000 entries, stress-tests foreign snapshot objects and long owner/key/message/details/path/install-state values, and verifies exported summaries include bounded `DiagnosticsAggregates`, `DiagnosticsScalarTrimmedBytes`, and `DiagnosticsSummaryTrimmedBytes`. Latest smoke evidence `GAME-SMOKE/20260610-142415` and `GAME-SMOKE/20260610-142626` verifies Camera and ActionSpeed diagnostics snapshot paths still report loaded mods, hooks, features, warnings/errors, and matching `LatestReportPath` values under the unchanged public surface. |
| Audio | `IAudioReplacementApi`, `AudioReplacementOptions`, `AudioReplacementState` | Experimental | Event-level Wwise replacement surface registered by GameBridge for carefully scoped mods. Current native-owner review targets wild paper boxes only: `DungeonResourceModelPaperBox.OnInteract` posts `SoundEvents.PLAY_RESOURCE_PAPER_BOX`, which flows through `WwiseSoundManager.InternalPostSoundEvent`. Runtime registration currently allowlists only the reviewed `PLAY_RESOURCE_PAPER_BOX` event, rejects duplicate suppressing replacements, and fails open if the hook is unavailable, a local WAV is missing/pending/failed, the event carries unsupported emitter/callback semantics, local playback cannot be verified, or playback does not start. The public API accepts owner manifests, reviewed native sound-event string IDs, local audio paths, volume, cooldown, and fail-open suppression policy without exposing raw Wwise, Unity, Harmony, or decompiled Doloc Town types. The first paper-box slice is verified by `GAME-SMOKE/20260617-065447`: after a real external `E` key, the smoke summary shows `paperBoxOnInteract=True;replacementEvent=True`; Unity `AudioClip` decode paths still produce zero-metadata clips for the test WAV, so GameBridge falls back to `System.Media.SoundPlayer` and logs `played=True suppressed=True`. It remains Experimental and blocked from promotion because replacement is global per reviewed Wwise event, verified playback is platform WAV playback outside Wwise bus/mixing/3D emitter semantics, owner unload cleanup and broader multi-mod arbitration are not yet proven, and a Wwise-native or Unity-native nonzero-metadata playback backend is still a follow-up. |
| Workshop / Content | `IWorkshopHelper.GetOfficialMods`, `GetDtmApiMods`, `IsOfficialEnablementManaged`, `GetEnablementHint`, `IWorkshopEvents.ModListChanged` | Experimental | Workshop helper queries read a shared immutable publication and are thread-safe/stale-helper-valid; only Core replaces the publication during authoritative source refresh. The Workshop event subscription itself remains owner-bound and runtime-thread-only. Reads respect official enablement and still need stronger Steam/official UI regression coverage before promotion. |
| Content Query | `IContentQueryHelper.FindAssets`, `GetKnownContentTypes`, `TryReadTextAsset`, `GetIndexedItems`, `GetAllIndexedItems`, `GetIndexedItem`, `GetAnyIndexedItem`, `IContentItemInfo` | Experimental | Shared read-only enabled official/Workshop source index over an immutable publication; helper reads are thread-safe and stale-helper-valid because they create no owner roots or mutation authority. DTMAPI owner assets and item rows publish only after the owner atomically activates and are removed on Entry failure, dependency block/cascade, disable, removal, shutdown or restart-required re-enable. Disabled/unknown official sources are not parsed into item-level rows; package state remains available through `IDtmModStatusInfo`. Third-party official JSON accepts the current native line-comment syntax and isolates a malformed enabled file without weakening strict DTMAPI manifests, receipts, configuration or ContentPack last-good publication. This remains separate from ownership of official content loading, is frozen against capability growth pending the thin native final-table/winning-source redesign, and is not promoted. |
| Content Pipeline | Content pack edits, ContentPatcher-style actions, update checks, compatibility DB, reflection helper, console command author API | Proposed | Direction exists in planning docs, but no stable public contract is promised here. |
| Migrated Gameplay | `IActionCompletionApi` | Experimental / Deprecated / Frozen | The interface and DTOs remain source/binary compatible for already-built Strict consumers, are closed to new consumers, and retain existing binary compatibility until a separately approved breaking change. Their executor is explicitly housed under `GameBridge/Compatibility/ActionCompletion`, is demand-inactive until an old consumer configures it, and refuses the resource route when the managed OneActionComplete owner is active. The admitted `Yuuka.DTMAPI.OneActionComplete` Advanced product does not consume this API: it privately owns resource classification, paid native extra-hit construction, fuel/feed completion and its two Postfixes. `GAME-SMOKE/20260722-080842` proves the initial migrated behavior; `141220` adds partial energy, ConfigMenu save/readback/reload and real Loader owner deactivation to zero actual patches/targets/callback/instance/Core roots. No signature changed and no removal release is promised; deletion requires a later breaking-version consumer audit and migration plan. |
| Migrated Gameplay | `IActionSpeedApi` | Experimental / Deprecated / Frozen | The interface and DTOs remain source/binary compatible for already-built Strict consumers, are closed to new consumers, and retain existing binary compatibility until a separately approved breaking change. Their executor is housed under `GameBridge/Compatibility/ActionSpeed`, remains demand-inactive until an old consumer configures it, and reconciles Hook-bearing plus AutoFill-only compatibility in both product/compatibility orders. The admitted `Yuuka.DTMAPI.ActionSpeed` Advanced product does not consume this API: it privately owns action timing, Animator acceleration, continuous-use policy, automatic bottle fill, native markers/restoration and a nine-target atomic Hook inventory. Existing ActionSpeed smoke and the 24-child L0-L5 ladder remain pre-migration baselines. `GAME-SMOKE/20260722-125239` is retained zero-patch fail-closed evidence for the wrong namespace; corrected `141220` passes Tool/ConfigApply/Interaction, title/restoration and exact nine-target Loader owner deactivation. No signature changed and no removal release is promised; deletion requires a later breaking-version consumer audit and migration plan. |
| Migrated Gameplay | `IFishingAutomationApi` | Experimental / Deprecated / Frozen | The intended public contract is preserved only as a compatibility adapter under `GameBridge/Compatibility/FishingAutomation`. The interface and related public fishing options/state DTOs are marked `[Obsolete(..., false)]`; the isolated adapter warns once per owner, receives no new capabilities, and lazily creates `LegacyFishingAutomationService` only after compatibility `SetEnabled(true)`. The self-contained Advanced AutoFishing product does not consume this API or its compatibility engine. The host gate confirms that `FishingAutomationOptions.StopOnManualMove` is restored with a public getter/setter, default `true`, and `Obsolete(false)`, that the retained Workshop AutoFishing DLL's setter token resolves against candidate Abstractions `0.5.3.0`, and that the complete retained 0.5.2 public surface has zero deletions. `GAME-SMOKE/20260721-161256` loads the exact retained DLL bytes without recompilation under Unity Mono and proves Entry, warning, Configure, enable/disable and clean exit. No removal version is promised: deletion requires a separate breaking goal after version-system completion, zero known consumers, migration guidance, and at least one actually published preview cycle carrying the warning. |
| Migrated Gameplay | `ILampControlApi`, `LampManualToggleOptions`, `LampManualToggleRegisterResult`, `LampManualToggleState` | Disabled / Retired compatibility shell | DTMAPI 0.5.5 restored the exact four retained public types, all 92 signature records, and their historical defaults solely to preserve old binary/type resolution. All four types are `[Obsolete(..., false)]` and `DtmApiStatus.Disabled`. The original `DTMAPI.GameBridge.DolocTown` provider returns a non-null owner-bound facade, rejects manifest and string-query owner spoofing, and deterministically reports `retired-disabled`; registration never succeeds. The provider is stateless and is not an `IGameBridgeFeature`: it installs no Hook, mutates no light, persists no state, creates no session override, enters no frame/lifecycle dispatch, and polls no file. Author SDK/Doctor migration diagnostics are later work. Existing binary compatibility remains retained until a separately approved breaking change after a renewed consumer audit and migration plan; no removal release is promised. Any real Lamp feature rebuild is a separate native-owner/API project. |
| Internal Gameplay | `IFirstPartyFishingPrimitivesApi`, `IFirstPartyFishingSession` | Retired internal / absent | Batch 6 removes these one-consumer friend/primitive definitions and their GameBridge provider/session implementation. This is a physical ProductNative rehome, not a public ABI deletion or stability promotion. The managed Advanced AutoFishing product now owns its private native adapter, sequence-checked transactions, hooks, caches, animation/input state and cleanup; the frozen public compatibility path remains separately isolated in GameBridge. |
| Migrated Gameplay | Fishing native responsibility review | Experimental | Review `docs/reviews/api/2026/20260610-fishing-native-responsibility.md` still owns the Ready/Cast/Wait/MiniGame/Pull/Cooldown native-owner mapping and keeps `IFishingAutomationApi` Experimental. Its old file-layout and primitive-runtime topology is historical: the current managed Advanced AutoFishing product privately owns the single-consumer native runtime, while GameBridge retains only the frozen compatibility executor. Fifth-save behavior, lifecycle and unchanged-package L0-L5 evidence are owned by Update `20260720-0008`; they do not stabilize the public API or open a general Advanced authoring lane. |
| Shared Native | `IItemDisplayNameApi` | Experimental | Minimal owner-bound read-only adapter shared by the admitted FishBreedingAssistant and AnimalHusbandryProgress products. On the DTMAPI Runtime/main thread, `TryGetDisplayName(itemId, out displayName)` resolves only `DolocAPI.QueryItemProto(itemId).Title`; it never negatively caches missing/failed/exceptional queries. A successful lookup first publishes an independent operation-lifetime lifecycle-protection demand. The shared `EnvironmentResetHookBridge`, which is not disabled with Camera, owns the one `DolocAPI.SetEnvCamera` Postfix used by Camera and ItemDisplayName. Until that physical Hook reports ready, successful titles are returned but deliberately not cached; once ready, only successful non-empty results enter the positive cache. SaveLoaded and ReturnedToTitle clear through the normal feature route, while the production static callback enters an independent `RunEnvironmentResetStep`; each boundary clears the cache and synchronously releases its demand. Focused Unit proves Hook-not-ready fail-closed behavior, shared physical-route deduplication, Camera-disabled ownership and the static callback-to-cache-clear path. It exposes no item DTO/enumeration/mutation, Unity/Harmony/proto type, Hook, formatting or UI behavior. The two-consumer/native-owner comparison is recorded in code Review `20260722-0008`; source/unit/SDK/package checks pass. `GAME-SMOKE/20260722-180502` proves the product behavior and product-owner cleanup; current-DLL `GAME-SMOKE/20260723-074311` proves both independent consumer queries, shared Hook readiness, two real callback clear/releases, title recovery and re-query. Update `20260722-0004` is `verified/passed/closed`. The API remains Experimental for 0.5.5 because only these two admitted products and one native responsibility are promised. |
| Migrated Gameplay | `IItemTooltipApi` | Experimental / Deprecated / Frozen | The interface and DTOs remain source/binary compatible for already-built consumers, are closed to new consumers, and retain existing binary compatibility until a separately approved breaking change. Its old title/description/detail executor now lives under `GameBridge/Compatibility/FishRoeTooltip`, is demand-inactive, and coordinates both product/compatibility load orders before physical Hook installation. The admitted FishBreedingAssistant Advanced product does not consume this API: it owns one `Item.get_title` Postfix and its duplicate-decoration rule, uses `IItemDisplayNameApi` only for the shared native lookup, and does not execute the empty placeholder fish-breeding table. `GAME-SMOKE/20260722-180502` verifies the frozen corrected shared-adapter product behavior and exact owner deactivation. No removal release is promised. |
| Migrated Gameplay | `IAnimalViewerApi` | Experimental / Deprecated / Frozen | The interface and DTOs remain source/binary compatible for already-built consumers, are closed to new consumers, and retain existing binary compatibility until a separately approved breaking change. Its former four-Hook executor now lives under `GameBridge/Compatibility/AnimalViewer`, stays demand-inactive, and fails closed in both product/compatibility load orders. The admitted AnimalHusbandryProgress Advanced product does not consume it: the product owns `AnimalFullInfoData(Animal)` Postfix, `AnimalViewer.Show` Prefix/Postfix, `AnimalPanelUiState.Unregister()` Postfix, derived rows/clones/throttle and cleanup, while using `IItemDisplayNameApi` only for output-item titles. Historical G9 runs remain pre-migration baselines. Focused gates and `GAME-SMOKE/20260722-180502` verify the frozen corrected four-patch/three-target DLL, read-only row/clone, native close and exact owner deactivation. No removal release is promised. |
| Diagnostic APIs | `IDebugConsoleApi`, `IInventoryDebugApi`, `IWeatherDebugApi`, `ITeleportDebugApi`, `IInstantSaveDebugApi`, `ITimeDebugApi`, `IMovementDebugApi`, `IAdvancedDebugApi` | Diagnostic | The remaining members of these interfaces are frozen Diagnostic compatibility surfaces; they are not an ordinary gameplay API or authoring command lane. The public DebugConsole `1.1.1` product calls none of the eight providers: its UI, private allowlisted actions and native patches are self-contained ProductNative code. It now reuses 27 public Debug DTO/enum types, so those remaining data shapes are real current-product dependencies even though the old provider route has no user. Current UI truth supersedes the 2026-08-01 hiding snapshot: Monster and Animal are searchable/paged ProductNative catalogs, Animal reads the merged runtime `TbAnimal` table and shares the stable item-source column instead of replacing it, the Generator quick action is absent and `dtmapi_creative_generator` is supplied through the ordinary item browser, while Resource spawning remains hidden. Movement uses a guarded current-player `BodyController.get_MoveSpeed` result factor; ordinary close retains it and 1x/save/title/disable/owner cleanup clears only product state. On 2026-08-30 the user confirmed the exact old `0.3.1-dtmapi` Y-console has no users and released its compatibility-retention restriction. The first bounded retirement authorized by that decision is complete on 2026-08-31: unused `ITeleportDebugApi.ExportDestinationsCsv` and `TeleportCsvExportResult` were physically removed from Abstractions, proxy/facade, Compatibility, QA and the ProductNative implementation. `GetCurrentSnapshot` and `TeleportSnapshot` remain because teleport results and validation still record before/after native snapshots; only their redundant live UI row was removed. The remaining Compatibility Host/broker route is unchanged pending its own bounded cleanup. Mandatory GameBridge still contains only the thin reflected broker. There is no stability promotion, and older raw-`MoveScaler` evidence remains historical. |

| Native Utility | `IMailDeliveryApi` | Experimental | Uses native item mail delivery with source gating and duplicate guards. Current game ignores custom mail title/content/sender, so it remains template-based and experimental. |
| Vehicle | `IMotorVehicleApi` and MotorVehicle DTOs | Retired | Removed from active `DTMAPI.Abstractions` and `DTMAPI.GameBridge.DolocTown` in the 2026-06-15 second-level cleanup Round 2 after the only real consumer, `SecondMotorMod`, was archived. The old 2026-06-14/15 slot 8/9 smoke history remains research evidence for why Doloc Town's native motor path is singleton-oriented, but it is not current completion proof and no ordinary mod should depend on this API. Future vehicle work must start from a smaller native-owner review and introduce a fresh public contract instead of reviving the retired surface. |
| Machine | `IMachineProductionApi`, `MachineDefinition`, `MachineRecipeInput`, `MachineOutputRule`, `MachineRegisterResult`, `MachineProductionState` | Experimental / Deprecated / Frozen | Retained as a non-error source warning and binary member-shape shell only. No mandatory GameBridge or Compatibility Host provider/executor remains, and new mods must not adopt it. The first-party Mine is an independently packaged ProductNative Advanced CodeMod; it does not replace this surface with another public machine API. Official JSON owns its item/equipment/recipe and native appliance threshold, while the product privately owns its fixed electric-only rule, session-derived scheduler, native power/inventory transaction and visual lifecycle. Cold-disabled `GAME-SMOKE/20260726-223105` and restored-enabled `223236` prove the corrected two-Mine, low/full preflight, move/remove/index-reuse, hot-toggle and exact-owner behavior matrix. A later independent source audit reopened only failed recipe/tech restoration retention; its focused Unit correction requires no replay of those valid game runs. Historical `IMachineProductionApi` smoke remains archive compatibility evidence, not current provider evidence or SharedNative authority. |
| Equipment | `IEquipmentSlotsApi` | Experimental / Deprecated / Frozen | Retained for the exact already-published MoreEquipmentSlots `0.3.1-dtmapi` consumer; the surface is closed to new consumers and existing binary compatibility remains retained until a separately approved breaking change. The interface and its six DTOs keep their binary members, provider ID `DTMAPI.GameBridge.DolocTown` and legacy owner-configurable `0..24` semantics; the retained DLL/hash and MemberRefs remain the compatibility authority. The mandatory provider is a thin on-demand proxy and the old executor lives in the dormant-shipped Compatibility Host, which loads only after this consumer demands it. `GAME-SMOKE/20260730-225518` proves the exact Workshop source activates that Host, renders and operates the old three-slot UI with both retained items and a `50/80` shield, completes one ordinary save, clears title state and exits. Its scoped transition field passes; the aggregate run is `partial` only because final disposable-root deletion found the runner's own stderr file still open. A separate input-frame warning recovered before the target flow and did not fail the compatibility semantics. New MoreEquipmentSlots `1.0.0`, Product-v3 migration and the five transaction findings in Review `20260730-0016` are deferred and are not evidence for this public contract. No `IProtectedStorageApi` is declared, admitted or promised, and this frozen ABI must not be recommended or generalized as shared storage. Native equipment, backpack/mail storage, vanilla shield priority, native hat appearance and the base AccessoriesBar remain game-owned. No removal version is promised. |
| Saves | `ISaveSlotsApi` | Experimental / Deprecated / Frozen | Retained for already-built consumers; the surface is closed to new consumers and existing binary compatibility remains retained until a separately approved breaking change. The exact retained MoreSaves DLL/hash and MemberRefs are frozen, the provider ID remains `DTMAPI.GameBridge.DolocTown`, and the one dormant-shipped Compatibility Host contains the demand-inactive fixed-six/twelve executor. Enabled requests normalize to twelve; disabled requests normalize to native six, including a cold disabled registration. An unregistered owner remains `IsConfigured=false`, and mixed enabled/disabled owners retain owner-specific state while reporting the shared effective native count. Both Product-first and compatibility-first writer orders fail closed through one internal `GameManager.archiveFileCount` lease. A final Loader-deactivation restoration failure retains that lease and reports failure, but does not claim an automatic retry after Loader removes event delivery; recovery waits for restart or a later explicit cleanup pass. The MoreSaves ProductNative atomic switch is complete: mandatory GameBridge now keeps only a thin frozen-ABI proxy and no save UI Hook or paging body. The product owns fixed twelve/six policy, config and restoration, references only `Assembly-CSharp`, installs zero Harmony patches, and subscribes `UpdateTicked` only while active-owner retry work is pending. Independent review is closed by `GAME-SMOKE/20260723-150215`: current DLL and HookProbe prove both initial and post-title native twelve-slot panels, third-save index `2`, real Loader deactivation to native six/zero roots/zero patches, exact restoration of all 36 archive paths, byte-unchanged protected indexes `6..11`, profile/source restoration and clean exit. Audit `20260723-0003` later closed the frozen state/idle-allocation evidence gaps through focused tests without requiring another game run; Updates `20260723-0003`/`0004` are verified/closed. Official `LocalSave`, `DataPersistenceManager`, `GameDataUiState`, `GameDataPanel` continue to own archive files, discovery, rendering, load/save/delete/copy and restart recognition. Historical 18/24 paging smoke is retained evidence, not a current API or product promise. No signature changed and no removal version is promised. |
| Inventory | `IChestLocatorEnhancerApi` | Experimental / Deprecated / Frozen | The ABI remains loadable only for retained Strict consumers and is implemented by a thin mandatory proxy plus the existing dormant Compatibility Host. The admitted `DTMAPI.ChestLocatorEnhancerMod` Advanced product does not consume the API: ProductNative owns one exact `ArchiveDataHandle.GetAvailableInventories(Vector2Int, Vector2Int, bool)` Postfix, while the game continues to own inventory instances and native `CountItem`/`CostItem` transactions. Product-first and compatibility-first activation fail closed before dual ownership. Focused ABI/owner gates and current-DLL `GAME-SMOKE/20260723-203127` verify the product route, native `0 -> 3 -> 1` Count/Cost behavior, title recovery and real Loader exact-owner cleanup to zero. This compatibility disposition does not promote the inventory policy to Stable or open new capability work. |
| Farming | `IStrongPlantingGunApi` | Experimental / Deprecated / Frozen | Retained as a warning-bearing ABI shell for the tracked legacy/no-Type source; there is no retained Workshop binary and therefore no Compatibility Host executor. The separately admitted `DTMAPI.StrongPlantingGunMod` Advanced product does not consume this API. ProductNative owns the fixed-three seed/film/fertilizer policy, the five atomic `ItemFarmingGun` constructor/use and `FarmingGunUiState` transfer patches, exact native function-capacity restoration, one `SaveLoaded` backpack repair for already-deserialized guns, config/menu lifecycle and fail-closed inventory transactions. JSON inventories already wider than three are preserved without truncation while the shared `ItemFunctionFarmingGun.Capacity` remains product-fixed at three and is restored on disable/title/owner deactivation. The mandatory GameBridge provider, service, Hook bridge, demand and callbacks have been removed. Focused source/Unit/SDK/package checks, direct SaveLoaded failure rollback, current-DLL protected third-save evidence `GAME-SMOKE/20260724-101757` and independent closeout pass under verified/closed Update `20260724-0001`. The interface and three DTO member shapes remain exact and no public expansion or stability promotion is authorized. |
| Farming | `ICropHarvestingApi` | Experimental | `CropHarvestingFeature` registers the API through `CropHarvestingService`, with explicit API calls over reviewed native `PlantBasin.CouldHarvest` / `PlantBasin.Harvest(bool,bool)` responsibility. The hardening pass narrows the contract to crop containers: ordinary PlantBasin crops plus PlantBasin-family vine, mushroom-bag, and bush categories may execute only when basin-level `CouldHarvest` / `IsCropMature` is true and the target still uses the reviewed `PlantBasin.Harvest(bool,bool)` owner. `TreeBasinCrop` means the cocoa-style `PlantBasinTree` crop-container path, not generic tree harvest; it is scan-only/unsupported until a separate `TreeCrop` / `PlantBasinTree` owner review. `GrassForageBasin` and wild forage/grass/tree content are outside this API's execution scope. Target ids are opaque transient handles for immediate same-session follow-up only. No mature targets is a successful no-op rather than API failure. Cleanup smoke `GAME-SMOKE/20260612-101549` verifies the same ordinary `PlantBasinSimple` scan/harvest path with `owner=DTMAPI.Smoke.CropHarvesting`, `CropHarvestingApi=Passed`, `CropHarvestingApiEvidence=Passed`, clean exit, and no fatal popup. Prior hardening smoke `GAME-SMOKE/20260612-072544` verifies `Feature.CropHarvesting=ready`, no-target `noTargetScan={success=True ... mature=0 ... failed=0}`, ordinary `PlantBasinSimple` scan/harvest through `PlantBasin.Harvest(bool,bool)`, and clean exit. First-slice smoke evidence `GAME-SMOKE/20260612-062154` remains the baseline for the original transient target path. A developer-only `CropHarvestingQaMod` fixture and manual checklist `docs/reviews/manual-qa/2026/20260612-0003-crops-harvesting-real-field-manual-qa.md` prepare real-field checks without expanding execution scope; the fixture is now installed only with explicit QA-fixture install mode and defaults its hotkeys to `None` to avoid F-key conflicts. Partial user QA on 2026-06-12 confirms the historical `F9` one-target harvest and `F10` batch harvest bindings put output into the backpack after manual fixture setup. The smoke owner is `DTMAPI.Smoke.CropHarvesting` so smoke status no longer pollutes the author-facing `AutoHarvestMod` sample owner. The API remains Experimental because room traversal, crop-family coverage, multi-owner scheduling, tree-basin/native-owner expansion, and remaining real-field boundary cases are not stable enough for ordinary-mod guarantees. |
| Camera | `ICameraZoomApi` 0.4.2 and DTOs | Failed / Deprecated / Frozen / ObsoleteCompatibility | Manual QA superseded the old CameraZoom completion claim. The exact ABI remains only for already-built binaries and now executes behind the existing dormant Compatibility Host; it is not an authoring direction. Do not cite `GAME-SMOKE/20260607-072228` or the `ZOOM-042` static screenshots as current completion evidence. No member removal or stability promotion is authorized. |
| Camera | `ICameraViewApi`, `ICameraViewLease` and DTOs | Experimental / Deprecated / Frozen | The retained published Zoom `0.4.2-dtmapi` DLL (SHA-256 `DFA74BDD3561A9E9647FEC01AB9B48F095826D2A752AE920566BE2C5063971B3`) is the exact known consumer. The ABI harness locks its complete 35-member CameraView MemberRef set (API/lease calls plus request/result/state DTO access); the earlier three-member summary and later 36-member count were incorrect and authorize no deletion or invented member. The current Advanced `DTMAPI.ZoomMod` product consumes neither Camera API. Its corrected Z1 ProductNative boundary owns scale/config and one vanilla orthographic-size snapshot only: scale changes never invoke native refresh; one exact `SetEnvCamera(Vector2,Vector2,bool,bool,bool)` Postfix and a `CameraController.RefreshResolution()` Prefix/Finalizer preserve the multiplier around genuine native refresh while leaving follow, enabled, camSize, ranges and position game-owned. The frozen CameraView/CameraZoom executors are mutually exclusive with that product and live only behind the existing Compatibility Host; mandatory GameBridge retains thin provider/demand/owner coordination. The 2026-08-01 correction changes no public signature or compatibility executor; its frozen player candidate passed moving-player 4x follow/flicker and scale-transition acceptance and is authorized only through the exact existing-Workshop update tree in Update `20260801-0002`. Earlier smoke remains historical evidence for its exact candidates, not acceptance of Z1. Background, fog, panorama, camera follow policy, UI scale and click mapping remain outside the frozen API boundary. No signature changed and no removal release is promised. |
| Camera | `IPanoramaCameraApi` | Proposed | Interface draft only. Panorama/background/fog/range compensation is deliberately not mixed into ordinary playable zoom. |
| Custom Entities | `ICustomAnimalApi`, `ICustomMonsterApi`, `ICustomAttackApi`, `ICustomDroneApi` | Experimental / Frozen | The existing interface and DTO ABI remains registered only under the `DTMAPI` Core provider for already-built consumers. All four interfaces publish separate Experimental stability, Frozen-since-0.5.5 disposition and non-error source warnings. The unsupported duplicate GameBridge provider alias is removed; tests and the only tracked fixture use Core, while source/artifact consumer scans found no GameBridge-alias evidence and no real product consumer. Registry validation, duplicate checks, cleanup, snapshots/status and blocked request results remain unchanged; native creation stays `runtime-creation-blocked`, no native host is admitted, and the C# surface must not expand. Historical `GAME-SMOKE/20260610-045414` proves registry/blocked behavior only. |
| Custom Entities | Native runtime creation verbs: animal/monster `RequestSpawn`, attack `SpawnProjectile`/`ExecuteAttack`, drone `RequestSummon`/`Equip`/`SetMode`, native handles, active runtime snapshots, save restoration | Experimental | Blocked. Current behavior intentionally returns `runtime-creation-blocked` / `RuntimeCreationBlocked`; no native animal, monster, projectile, attack, or drone is created. Ordinary mods must not depend on these runtime verbs until family-specific native adapters are verified in game. |
| Custom Entities | Native animal/monster/attack/drone adapters | Proposed | Future work must start from native owners such as animal proto/room/food/produce/save, monster AI/spawn/drop, bullet factory/collision/damage, and drone controller/weapon/equipment/persistence. |

## 2026-08-30 API Notes

- The user confirms the exact old DebugConsole `0.3.1-dtmapi` product has no
  remaining users and releases the prior consumer-retention prerequisite. A
  future bounded breaking cleanup may retire its old provider/UI/action route
  without reopening that user-count gate.
- This decision does not itself change the current compiled API. Public
  `1.1.1` still reuses 28 Debug DTO/enum types, so an implementation must
  internalize or deliberately retain those shapes before deleting them. The
  eight provider interfaces and optional Compatibility bodies remain present
  until that separate cleanup is implemented and validated.
- Current Product UI has searchable Monster and Animal catalogs; Infinite Fuel
  is an ordinary item-browser entry and only Resource spawning remains hidden.
  These ProductNative facts do not promote any Diagnostic contract.
- The `1.1.2` source candidate changes only ProductNative spawn execution and
  presentation: monster/animal batches now retain and visibly report partial
  success without a spawn rollback or cross-request circuit. The public
  `SpawnDebugResult` ABI is unchanged; `SpawnedCount` remains the count of
  roots whose postconditions passed, while actual manager additions travel in
  a product-internal result used by the UI and logs. This neither alters the
  immutable public `1.1.1` artifact nor stabilizes a Diagnostic API.

## 2026-08-01 API Notes

- The official Mod-page transaction and its deferred Workshop source refresh
  are internal Platform authority changes. No Workshop helper, event or public
  manifest contract changed.
- DebugConsole movement keeps the exact frozen `IMovementDebugApi` and DTO ABI.
  ProductNative and Compatibility now implement the existing multiplier as a
  mutually exclusive current-player final-speed Postfix rather than writing
  the game/Buff-owned aggregate `MoveScaler`. This corrects implementation
  ownership without promoting the Diagnostic API or opening new adoption.

## 2026-07-30 API Notes

- MoreEquipmentSlots `1.0.0`, its migration and publication are deferred.
  Workshop `3744059735` remains at the exact retained `0.3.1-dtmapi`
  artifact. `IEquipmentSlotsApi` and its DTOs remain frozen only so Runtime
  0.5.5 can load that already-built consumer through the demand-loaded
  Compatibility Host; an old-package smoke does not promote the API or accept
  the deferred Product.
- `IProtectedStorageApi` is a discussion name, not a declared interface,
  admitted capability or 0.5.5 promise. General protected-storage API work is
  paused. Any later design must first establish one data authority and one
  production adapter rather than stabilizing the current product-specific
  transaction machinery.

## 2026-07-27 API Notes

- DebugConsole admission changes no `DTMAPI.Abstractions` signature or status.
  `DTMAPI.DebugConsoleMod` 1.0 uses private ProductNative action interfaces and
  the existing typed input/config/UI helpers; it neither consumes nor replaces
  the frozen Diagnostic API contracts.
- Core's owner-bound custom-menu methods used by the product and QA are internal
  Platform coordination, not a public UI API. QA observes the exact
  `DTMAPI.DebugConsoleMod` owner token and does not consult the frozen
  `IDebugConsoleApi`.
- `GAME-SMOKE/20260727-083919` proves the current product executes its bounded
  actions through `ProductNative` and releases the complete title/Loader owner
  graph. Exact old 0.3.1/current-Host `085929` exercises all seven frozen
  action groups through `Compatibility`; isolated `NativeSaveExpected`
  `090702` proves two-click Save here, injected native failure propagation and
  one confirmed native save. These results support product/compatibility
  ownership without promoting typed input, UI helpers or any Diagnostic API.

## 2026-07-23 API Notes

- Public metadata now separates stability (`DtmApiStatusAttribute`) from adoption disposition (`DtmApiDispositionAttribute`). Frozen compatibility interfaces and their dedicated DTOs retain their prior stability while publishing `Frozen` since 0.5.5 plus non-error source warnings. This is ABI-additive and starts author-facing warning visibility; it neither promises a deletion release nor changes the retained provider/consumer gates.

## 2026-07-22 API Notes

- ActionSpeed no longer consumes `IActionSpeedApi`. The unchanged API/DTO ABI is Deprecated/Frozen under `GameBridge/Compatibility/ActionSpeed`; both load orders, including AutoFill-only compatibility demand, reconcile before duplicate work. `GAME-SMOKE/20260722-141220` supersedes the earlier type-name and owner-deactivation blockers and verifies the corrected nine-target product plus OneActionComplete continuation gaps.
- FishBreedingAssistant and AnimalHusbandryProgress are separately admitted ProductNative consumers of the exact Experimental `IItemDisplayNameApi` contract recorded above. `GAME-SMOKE/20260722-180502` verifies the frozen corrected shared lookup and both independent requested-owner cleanups. This adds no general item API, keeps their formatting/UI/Hooks/state product-owned, does not promote the API for 0.5.5, and does not authorize another product, general Advanced authoring or publication.
- The [Phase 2 retained-consumer Review](../../reviews/code/2026/20260722-0010-frozen-abi-consumer-and-compatibility-host-review.md) confirms the first five exact published consumers of `IFishingAutomationApi`, `IActionCompletionApi`, `IActionSpeedApi`, `IItemTooltipApi` and `IAnimalViewerApi`; the MoreSaves, ChestLocatorEnhancer, MoreEquipmentSlots and Zoom product Updates add the exact retained `ISaveSlotsApi`, `IChestLocatorEnhancerApi`, `IEquipmentSlotsApi` and `ICameraViewApi` consumers. The later DebugConsole Update adds the exact retained 0.3.1 `IDebugConsoleApi` consumer/UI/action route. The ABI harness now resolves eleven exact retained product DLLs across the ten compatibility families. All ten heavy executors live in one Catalog-owned dormant-shipped Compatibility Host outside the default-loaded GameBridge; first-call loading keeps the same provider IDs, ABI, owner and fail-closed behavior. All ten families carry warning-bearing frozen/compatibility disposition without changing binary signatures or promising an effective deletion window. Post-review `GAME-SMOKE/20260723-092718` proves dormant-to-resident first use plus zero transient and resident-owner Fishing state; Mono unload is not promised.
- The [Phase 4 audit](../../reviews/api/2026/20260722-0001-public-api-consumer-owner-thread-cleanup-audit.md) is closed by the 2026-07-23 bounded API cleanup: Custom Entities is ABI-preserving Experimental/Frozen under the single Core provider, AutoHarvest waits for the ordinary `SaveLoaded` lifecycle instead of consulting a Diagnostic API, and helper owner/thread/stale rules are explicit and tested. No C# CustomEntity expansion or first-party AutoHarvest product was introduced. The later Chest product is a separate admitted ProductNative migration and does not alter that API cleanup result.

## 2026-07-21 API Notes

- Batch 6 changes no public signature or stability level. The managed Advanced AutoFishing product is self-contained ProductNative code with minimum Runtime `0.5.5` and no GameBridge dependency; the internal first-party primitive/friend seam is absent. The frozen `IFishingAutomationApi` compatibility surface remains in GameBridge, is closed to new consumers, retains existing binary compatibility until a separately approved breaking change, and is not consumed by the new product.
- The uniquely admitted Advanced OneActionComplete product is likewise self-contained and does not consume `IActionCompletionApi`. That API and its DTOs are explicitly Deprecated/Frozen under `GameBridge/Compatibility/ActionCompletion`; the executor remains demand-inactive compatibility for old Strict binaries and receives no new capability. Its retained post-migration run is partial and leaves partial-energy, config save/reload and actual owner deactivation open.
- The retained ABI host gate reports zero public removals, loads all eight retained consumer types and resolves the exact old AutoFishing `StopOnManualMove` setter reference `1/1`. Exact-byte Unity Mono canary `GAME-SMOKE/20260721-161256` proves the legacy binary still loads, warns, configures, enables/disables and exits cleanly. This compatibility proof does not authorize 0.5.5 release or API removal.
- The 2026-07-11 primitive topology and `0.5.3-alpha`/DeveloperOnly product statements below are historical. Current version authority is public/API `0.6.1`, file `0.6.1.0`, assembly compatibility `0.5.3.0`; AutoFishing and OneActionComplete were the exact two admitted real products at this dated checkpoint. The later 2026-07-22 ActionSpeed admission supersedes only that count and still does not open a fourth product or general Advanced authoring.

## 2026-07-18 API Notes

- Batch 5 changes no public input signature or stability level. The first-party Zoom consumer now uses two bounded owner-scoped `Gameplay` registrations plus `KeybindPressed`, releasing both registrations and its event subscription when disabled, instead of retaining a product `UpdateTicked` callback for local `DtmKeybindList` polling. At that historical Batch 5 boundary AutoFishing owned one continuously armed toggle registration while its automation `UpdateTicked` subscription existed only for an active automation session; Batch 6 later rehomed that product lifecycle into its Advanced ProductNative assembly. Local `DtmKeybindList` snapshot queries remain supported for already-active or transient consumers; they are no longer a reason to add an otherwise-unneeded frame callback. The owning design rule is `docs/design/smapi-like-input-lifetime-contract.md`; Batch 5 runtime completion evidence remains owned by Update `20260718-0003`.
- `20260801-0001` changes no public input signature or stability level. It replaces the obsolete modal-wide update pause with a frame-frozen internal input audience: Normal retains scoped broadcasts, an owner modal targets only its owner's context-valid non-Gameplay input, and a platform/official modal provides no new Mod input. Exact Pressed recipients retain a settlement Release across modal, scope, save/title and configuration changes; configuration retains the old chord until settlement; eligibility loss requires neutral before another Pressed. String and typed queries use the same owner boundary, modal queries do not create arbitrary local watches, and `Suppress` never deletes physical state or an owed Release. Stable Normal and owner-modal 10,000-frame source tests allocate zero Core bytes. Direct Unity/Harmony input remains outside this promise.

## 2026-07-19 API Notes

- Batch 5 still changes no public CameraView or AnimalViewer signature and promotes neither API. `GAME-SMOKE/20260719-031358` adds current-tree ground-safe 4x/movement/2x/reset, real Zoom owner release, zero Camera leases after cleanup and stable-exit evidence. The earlier `20260719-024242` crash is classified as an optional-QA reentrant state-machine defect, not an ordinary `ICameraViewApi` P0; the map-boundary mask at wide zoom remains a known visual boundary and does not establish background/fog/panorama synchronization.
- Final ordinary Candidate11/Local11 no-QA `GAME-SMOKE/20260719-214048` adds exactly two causal real-player AnimalViewer renders, native close before session clear, overlay/order cleanup, real input and auto-drive provenance, exact five Runtime DLLs/no QA, exact eleven candidate-bound product sources, state restoration and stable exit. It supersedes the earlier passing `20260719-034102` for the Batch 5 final tree and the failed `20260718-190150` / `20260718-192244` attempts; historical Batch 4 `20260718-005546` remains valid only for its own exact tree. `IAnimalViewerApi` remains Experimental because native/viewer ownership and visual acceptance are still fragile.
- The owner-bound event mutation/membership row now records an implemented Batch 5 Experimental rule, not a stability promotion. Runtime tests cover same-thread `+=`/`-=`, immutable publication membership, late subscriber exclusion, ordinary removal visibility on the next publication, and authoritative owner-cleanup/quarantine cancellation of stale delegates.
- Published11 enabled `GAME-SMOKE/20260719-212635` and disabled/CoreOnly `20260719-212758` passed the bounded combination/lifecycle/cleanup/restore/exit gates. The independent ActionSpeed 24-child third-save ladder and AutoFishing L0-L5 fifth-save ladder also have terminal structural/lifecycle/trend evidence. These facts supersede the preflight-only `20260719-010116` gap, but they do not promote any API, establish a quantified memory/GC budget, reproduce the final source commit, or authorize a 0.5.5 release.

## 2026-07-15 API Notes

- `20260715-0012` adds no new fishing or Lamp gameplay capability. It restores and host-validates the frozen `FishingAutomationOptions.StopOnManualMove` ABI member plus the exact four-type/92-signature Lamp compatibility shape. The retained AutoFishing SHA-256 matches `E573F8CA1989663B672AF481921E4C6131C061294402F654A4062844DC5CA7FA`, its setter MemberRef resolves against candidate Abstractions `0.5.3.0`, all eight consumer types resolve, and getter/setter/default/obsolete checks pass. `GAME-SMOKE/20260715-131518` further proves exact-byte no-recompile Unity Mono binding, Entry, setter use, Configure and F6 enablement; movement-cancel and GC remain separate gates.
- The exact-artifact gate now reports `RemovedPublicApiCount=0`. All four Lamp types retain historical defaults, `Obsolete(..., false)`, and `DtmApiStatus.Disabled`; GameBridge exposes only the owner-bound deterministic `retired-disabled` shell described in the matrix row. The machine-readable result is `tools/release/baselines/retained-runtime-052-public-api-audit-20260715.json`. The Lamp result remains .NET 8 host and source/unit evidence, not Lamp gameplay evidence.

## 2026-07-12 API Notes

- `20260712-0009` changes no source signature yet, but corrects the current compatibility status: the intended Frozen `IFishingAutomationApi` DTO surface has one confirmed deletion, `FishingAutomationOptions.StopOnManualMove`, used by the existing Workshop AutoFishing DLL. DTMAPI 0.5.5 is blocked from release until the obsolete member is restored, the retained 0.5.2-to-current public diff has no unapproved deletion, and representative 0.5.1/0.5.2 consumer DLLs load without recompilation under Unity Mono. Public `0.5.5`, file `0.5.5.0`, and assembly compatibility identity `0.5.3.0` are separate projections for this gate.
- `20260712-0006` changes no public signature or stability level. It restores the intended distinction between `SaveLoaded` keybinds, which may close their own in-save modal UI, and `Gameplay` keybinds, which remain blocked by that modal boundary; the Y console now has one ordinary owner-bound toggle consumer.
- `20260712-0003` changes no public signature or stability level. Ordinary dependency version authority is the canonical manifest loaded with the resident owner transaction; an in-place disk version change triggers update-pending/source-handoff and cannot authorize a consumer against older resident code. Diagnostic implementation now bounds both retained object counts and every retained caller-controlled scalar, with stable hash suffixes and trimmed-byte accounting.
- `20260712-0001` changes no public signature or stability level. It corrects the internal active-owner dependency authority: process-lifetime Core/ConfigMenu/GameBridge/Bootstrap providers are published with one canonical manifest plus atomic contract registration and are resolved from the runtime registry across unchanged source refreshes, while ordinary providers remain governed by refreshed source identity, enablement, version, and reverse-dependency cleanup. Process-first source collisions and ordinary-first late platform claims are both rejected without replacement. Successful Mono assembly load remains the restart boundary; a no-assembly/non-code owner may republish only after every Core/participant root is zero. `SaveLoaded` now clears Core transient Input state and demand-local watches just like `ReturnedToTitle`, while persistent owner registrations and all other committed process-lifetime services remain active.

## 2026-07-13 API Governance Notes

- The selected U1 policy applies to gameplay capabilities with only one known first-party product consumer: new implementations default to first-party internal capabilities. It does not internalize naturally general Framework, manifest, config, content, event or diagnostics contracts by label alone.
- DTMAPI 0.5.5 removes none of the existing public ABI. Existing product-mirror APIs keep their current matrix status and enter the I1 freeze/warning/external-consumer-scan process; a real external consumer pauses retirement and triggers a narrow native-owner/multi-owner/lifecycle redesign.
- This governance decision does not automatically promote, deprecate or retire any concrete row. Such a status change still requires its focused Review, compatibility evidence and Update.
- The 2026-07-11 statement that AutoFishing was DeveloperOnly/unpublished is preserved as a historical source-label observation but is superseded for distribution classification by `docs/reviews/code/2026/20260713-0004-first-party-product-catalog-fact-review.md`: local Workshop history establishes AutoFishing as a public product identity whose source/release labels are stale.
- At the 2026-07-13 checkpoint, the source baseline remained `0.5.3-alpha`/`0.5.3.0` pending the 0.5.5 implementation. That conditional statement was superseded by the 2026-07-15 Batch 2 version split, the 2026-08-04 0.6 authority, and this 0.6.1 installer correction: current source projects release/API `0.6.1` and binary/file `0.6.1.0` while deliberately retaining assembly compatibility `0.5.3.0`. The selected first-party 1.0.0 product versions remain independent product identities.

## 2026-07-11 API Notes

- `20260711-0013` adds no public API and does not promote Camera stability. ZoomMod is now sourced as an optional first-party product, but it remains an ordinary Abstractions-only consumer of the unchanged Experimental `ICameraViewApi`; its real-owner disable/restart-required evidence validates existing lifetime semantics only.
- Historical Batch 5 note: `20260711-0005` added no public API. Its `AutoFishingPrimitiveSmokeCase`, `IFirstPartyFishingPrimitivesApi` and first-party primitive topology were later removed by the Batch 6 ProductNative rehome; `IFishingAutomationApi` remains Deprecated/Frozen. Ordinary mods use local `DtmKeybindList.JustPressed(helper.Input)`; the managed AutoFishing product's current input lifetime is owned by Update `20260720-0008`.
- `20260711-0004` adds no public API. Runtime observation windows, least-squares slopes, and Gen2 MonoUsed low-water epochs are internal Core diagnostic schema used by smoke evidence. Fishing Primitives and AutoFishing behavior are unchanged.
- `20260711-0003` adds no public API. `RuntimeMemoryTrendProbe`, allocation subprobe, Unity/Windows metric providers, `Wait.NextState` acceptance callback, and accepted-reel telemetry are internal diagnostics/GameBridge implementation. Fishing Primitives remain first-party internal and `IFishingAutomationApi` remains Deprecated/Frozen.
- `20260711-0002` adds no public API. Allocation-counter calibration, nullable performance JSON fields, process-lifetime visible-reel telemetry, and smoke Blocked classification remain internal GameBridge/test infrastructure. The runtime stays `0.5.3-alpha` and Fishing Primitives stay first-party internal.
- Historical checkpoint: Runtime/API preview metadata was aligned to `0.5.3-alpha` and binary/file metadata to `0.5.3.0`. Current source authority is public/API `0.6.1`, file `0.6.1.0`, with assembly compatibility deliberately retained at `0.5.3.0`; this still is not a formal 0.6.1 publication.
- No public Fishing surface was added. Hook readiness, transactional acquire, visible-reel retry, typed animation/physics access, and performance telemetry remain internal GameBridge/first-party implementation details.
- `IFishingAutomationApi` and related public fishing DTOs are now Obsolete, Deprecated/Frozen. Compatibility behavior remains available, but new products must not adopt it and it receives no new capability.
- Historical checkpoint: AutoFishing was recorded as `1.4.3-dtmapi`, DeveloperOnly and unpublished with `0.5.3-alpha` dependencies. Batch 6 supersedes that physical/package state: the sole real-product pilot remains version `1.4.3-dtmapi`, requires Runtime `0.5.5`, keeps the MCM/platform dependency and has no GameBridge dependency; its implementation status does not authorize publication.
- The legacy API has no fixed deletion release. A future breaking removal requires version-system completion, zero known consumers, migration documentation, and at least one actually published warning-bearing preview cycle.

## 2026-07-10 API Notes

- `IFishingAutomationApi` remains source-compatible and Experimental, but its implementation is now explicitly a compatibility controller over first-party Fishing Primitives. `Configure` no longer activates hooks or a runtime session; only `SetEnabled(true)` does so.
- Fishing Primitives are an internal product boundary, not an API promotion. Scalar snapshots, sequence-checked transactions, non-preemptive session ownership, input leases, and animation leases are compile-time restricted to first-party assemblies.
- The 20260710-0006 hot-path pass makes the primitive snapshot Hook-fed/pure and moves first-party `RollFish`/`NextState`/`Overwrite` into cached native transactions. `IFishingAutomationApi` remains the legacy Experimental facade; no ordinary mod can acquire the primitive service.
- The 20260710-0008 Hook-runtime seam adds no public API. `IFishingHookRuntime` and `FishingPrimitiveHookRuntime` are internal; first-party AutoFishing no longer constructs the legacy executor. Only compatibility `IFishingAutomationApi.SetEnabled(true)` may create `LegacyFishingAutomationService`, and compatibility/primitive modes cannot coexist even for the same owner ID.
- The first-party product owns hotkey, movement cancel, recast timing, InstantBite, CastChargeRatio, SkipMiniGame, FastAnimations, and minigame input decisions. The compatibility DTO separately retains `CastChargeRatio` for existing clients.
- Local input snapshot watches reuse stable owner/button state. Persistent actions that must accept the first short edge should use a scoped registration; the registration key is cached and the warmed Core route is allocation-free.

## 2026-07-08 API Notes

- `IFishingAutomationApi` remains Experimental and is now explicitly a lazy GameBridge capability. The runtime registers a facade at startup, activates service state and conservative process-long hooks only after a consumer calls `Configure` or `SetEnabled(true)`, and reports `inactive/no-consumer`, `configured`, `active`, or `disabled/consumer-present`. `StopOnManualMove` is retired from `FishingAutomationOptions`; movement cancel and default `F6` are AutoFishingMod product behavior, not DTMAPI Core or GameBridge semantics. At the 2026-07-08 phase the first-party product forced no-charge casting; the 2026-07-10 product restores an independent per-cast `0..1` charge request while the compatibility DTO stays unchanged.
- The input helper surface remains Experimental and adds local snapshot convenience methods: `IInputHelper.GetState(DtmButton)`, typed `WasReleased`, and `DtmKeybindList.IsDown(input)` / `JustPressed(input)` / `JustReleased(input)`. This supports ordinary mods that can read input locally without registering every transient key as a platform-managed root.

## 2026-07-01 API Notes

- Hatch `AnimalVoice` audio replacement is an internal ContentPack/GameBridge capability, not a public API promotion. Content packs may provide `Content/DTMAPI/audio-replacements.json` plus WAV files for reviewed short-SFX categories. The first category is `AnimalVoice`, scoped by native `Animal.PlayAnimalSound()` context (`speciesId + stage + nativeSoundEvent`) before the existing Wwise `InternalPostSoundEvent` prefix may suppress native audio. `GAME-SMOKE/20260701-073005` verifies Hatch child/adult WAV replacement on slot 7 with `played=True suppressed=True`; user manual QA on 2026-07-01 additionally verifies adult/child pet and hit paths and confirms vanilla chicken is not polluted. `IAudioReplacementApi` remains Experimental and still rejects animal voice events through its C# registration path; the code-mod reviewed slice remains `PLAY_RESOURCE_PAPER_BOX` only. BGM/music/looped audio remains out of scope because native music paths include callbacks, STOP events, state, and lifecycle ownership not proven by this short-SFX bridge.

## 2026-06-17 API Notes

- `IAudioReplacementApi` remains Experimental. Docs-only follow-up review `docs/reviews/api/2026/20260617-0002-audio-replacement-event-map-backend-research.md` records the long-term direction: future audio work should key replacements by Wwise `eventName` / `SoundEvents` plus a policy table, not by one native gameplay function per sound. Recommended backend order is Wwise custom-bank redirect first, Unity `AudioSource` plus DTMAPI-owned decode second, and `System.Media.SoundPlayer` as debug/probe fallback only. This does not expand the current paper-box allowlist and does not prove BGM, loop, STOP, callback, RTPC/state, bus/mixer, or arbitrary event replacement.

## 2026-06-16 API Notes

- `20260616-0004` does not promote any API. It improves diagnostic/runtime behavior only: GameBridge feature failures now store unwrapped root exceptions first, bootstrap `Update` failure publication is throttled after the first three full errors, and the existing Diagnostics log-export/history behavior from `20260616-0001` remains unchanged. Release build/test plus third-save smokes `GAME-SMOKE/20260616-124555` and `GAME-SMOKE/20260616-120817` verified the follow-up.
- `IDebugConsoleApi` and the Y-console debug APIs remain Diagnostic. At this dated checkpoint the focus-aware `Y` behavior and right-click give route through the actual item-cell Unity `PointerDown` owner were implementation details of the reflected Bootstrap UI host, not a new ordinary-mod input contract. The 2026-07-27 admission later moved the retained host into optional Compatibility and gave the new product private ProductNative actions without changing this API status.
- `IMovementDebugApi` remains Diagnostic. The new lease is a debug quality-of-life guard over `MotionAbility.SetMoveScaler`; it deliberately clears at save/title boundaries and is not a stable movement-buff API.
- `IAnimalViewerApi` remains Experimental. Clone-session cleanup and lifecycle status `Animals.ViewerRenderingLifecycle` are GameBridge internals for the long-session disappearance report, not new public AnimalViewer DTOs.
- `IActionSpeedApi` remains Experimental. The ActionSpeed bottom-layer branch is build/unit verified for native-owner classification including ready resin collectors and animal owner marking for room-connector overlap, plus marker lifetime across generic state-exit restore, but water-well/planting/animal/resin completion still requires fresh third-save manual smoke before any stability claim.

## 2026-06-15 API Notes

- `IMotorVehicleApi` and its MotorVehicle DTOs are retired/removed from active API code. The 2026-06-14/15 SecondMotor slot 8/9 smoke history is research history only: user manual QA on 2026-06-15 found abnormal light textures, abnormal farm-room textures, and severe cross-map texture pollution, so the active `SecondMotorMod` project and local packages were archived, then the remaining public API, GameBridge hooks, smoke body, and unit coverage were removed in cleanup Round 2. There is no replacement vehicle API in this release. Old experimental mods that reference `IMotorVehicleApi`, `CustomMotorDefinition`, `SecondMotorOptions`, or MotorVehicle result/state/event DTOs must remove those references; a compile or type-load failure for those retired types is expected, not a random loader bug. Do not cite archived SecondMotor smoke evidence as current completion proof or stable multi-vehicle evidence. Future vehicle work must start from a smaller native-owner slice and fresh manual QA.

## 2026-06-13/14 API Notes

- `20260614-0005` does not change `IEquipmentSlotsApi`. It adds a Diagnostic smoke dump of runtime `TbHat`/`TbItem` hat metadata for API review, verifying `hats=33`, `itemHatRows=33`, no orphan mappings, `box_hat` as `ItemFunctionHatShield` / `AgentEquipmentFuncProtoShield`, and `mushroom_hat` as `Defense=1` without `Skill` in `GAME-SMOKE/20260614-091839`. This is evidence for future design decisions only and does not promote EquipmentSlots beyond Experimental.
- `20260614-0006` does not promote `IEquipmentSlotsApi`. Shield hats are handled as internal GameBridge protected-storage behavior over native `ItemFunctionHatShield` / `AgentEquipmentFuncProtoShield` metadata; no raw hat, shield, `BodyController`, or `AgentEquipmentManager` type is exposed publicly. Multi-shield manual QA has passed, but the API remains Experimental until disabled/unsubscribed recovery, hot-disable UI cleanup, and real-hit tail automation coverage are complete.
- `IFishingAutomationApi` remains Experimental. The `20260613-0001` to `20260613-0003` follow-ups changed internal InstantBite timing/visibility and native post-bite routing. The `20260613-0005` rewrite deliberately changes the experimental DTO shape to native-stage strategy enums and rewrites AutoFishing defaults around the fifth-save real pond fixture. This is a breaking experimental API cleanup, not a stability promotion; hook/status IDs remain compatible where practical. Fifth-save smoke `GAME-SMOKE/20260613-094928` proved the new fixture gate and failure reporting while the local fifth save selected `DolocTown.ItemTool`; after the fifth save was updated to hold `carbon_fishrod`, the latest real loop and all current AutoFishing scenarios passed in `GAME-SMOKE/20260613-112900`, `20260613-113127`, `20260613-113240`, `20260613-113654`, `20260613-113824`, and `20260613-113937`.
- `20260613-0014` keeps `IFishingAutomationApi` Experimental while tightening semantics: `AutoCompleteVisibleMiniGame` is no longer a delayed direct `currentGameStatus=Success` write, and `FastCastPull` now has a player-facing `AnimationMultiplier` in `1..4` with migration/default `3`. Fifth-save evidence `GAME-SMOKE/20260613-153632`, manual red/green/yellow QA, hook-physics re-smoke `GAME-SMOKE/20260613-160354`, and Ready/Cast/Pull re-smoke `GAME-SMOKE/20260613-163812` supersede older `visibleSeconds=0.76` direct-success proof and animator-only animation evidence for current completion/animation semantics.
- `20260613-0016` keeps `IFishingAutomationApi` Experimental and adds `FishingAutomationOptions.CastChargeRatio` as a native Ready-stage policy. It does not expose raw Ready/Cast/FishRod types and does not stabilize multi-owner semantics. Fifth-save evidence `GAME-SMOKE/20260613-171405` supersedes the false-positive `GAME-SMOKE/20260613-170514` for configured charge because the final smoke required `target=0.5` in `Smoke.AutoFishingCastCharge`.
- `20260613-0017` does not change `IFishingAutomationApi`. AutoFishing toggle replacement is implemented by the ordinary mod through existing `IDtmConfigMenuApi.AddKeybindOption` and `IInputHelper.RegisterButton` / `UnregisterButton`; `F6` remains the default, explicit `None` disables the mod hotkey, and custom keys are preserved. Fifth-save smoke `GAME-SMOKE/20260613-175055` verified the F7 path from config/logged policy to input dispatch, `reason=hotkey F7`, and the default native loop. This does not promote config-menu keybinds or FishingAutomation beyond their existing status.
- `20260613-0021` does not change `IFishingAutomationApi`. It narrows the internal InstantBite implementation so `AgentStateFishingWait.OnEnter` only prepares the bite and `OnPlay` owns the single mirrored native reel/energy-cost transition. Unit coverage verifies exactly-once `CostEnergy` behavior; fifth-save energy-delta evidence is still pending, so the API remains Experimental.
- `IDtmConfigMenuApi` / title Manager UI stability does not change. `UI.NativeLayoutDiagnostics` is internal GameBridge logging plus narrow native-owner setter normalization for the remaining title/pause two-column layout regression; it is not a public API.

## Failed / Historical Evidence Rules

- CameraZoom 0.3.0 evidence proves only orthographic-size writes and restore. It is historical, not completion proof.
- CameraZoom 0.4.2 evidence is now marked `Failed / ObsoleteCompatibility` after manual QA. Its static screenshots and smoke logs may remain useful for comparison, but they must not be cited as `ICameraViewApi` completion evidence.
- CustomEntity 0.4.0 smoke proves registry and blocked runtime results only. It does not prove native entity creation.
- HookProbe/TestMod evidence can prove event delivery or internal status paths, but it cannot by itself promote a public gameplay API to `Stable`.

## Current Summary

- Stable: DTMAPI-owned framework contracts with low native-state risk.
- StableCandidate: strong DTMAPI-owned or contract-level APIs that still need adoption/manual/regression gates.
- Experimental: most GameBridge-facing gameplay/content/UI/native utility APIs.
- Diagnostic: Y-console, debug, evidence, and hook-status helper APIs.
- Retired: vehicle/MotorVehicle API and hook surfaces removed after the SecondMotor route failed manual QA.
- Proposed: content pipeline, Panorama camera API draft, and custom entity native adapters.
- Failed / ObsoleteCompatibility: CameraZoom 0.4.2 as a completed visual/camera API claim.
