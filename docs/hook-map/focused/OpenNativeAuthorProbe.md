# Open Advanced author probe

Status: PN-010 actual Mono observations accepted in [Update 20260909-0013](../../updates/2026/20260909-0013-platform-open-advanced.md), with [final evidence](../../debug/evidence/GAME-SMOKE/20260909-platform-pn010-final-runtime/README.md). Exact owner cleanup and independent author survival were observed both at normal shutdown and after deliberate partial Entry failure; agent/language reads succeeded after SaveLoaded. No cross-build guarantee or physical input claim.

The local metadata probe inspected public build 25163613, Assembly-CSharp SHA-256 `60489873c645886c5a523fd0d17c4d451a7d68df133f552c6667501245110ac6`. `DolocTown.BodyController DolocAPI::get_agent()` is a public static getter reading its backing field. `System.String DolocAPI::get_CurrentL10nId()` is a public static getter calling `DolocTown.Config.DolocConfig::get_Tables()` and `DolocTown.Config.Tables::get_CurrentL10nId()`. Neither getter directly writes fields. This is a focused metadata/body-reference observation, not a claim about all transitive behavior.

External authors install a void observation Postfix on exactly `get_CurrentL10nId()` and read agent/language after SaveLoaded. No prefix suppresses the getter, and no argument/return value or native save is changed. Owners are exact per-author namespaces, `Cedar.NativeControl.Native`, `Pine.NativeObserver.Native` and a retained-baseline author owner. Each instance removes only its own patches. The deliberate partial Entry failure occurs after the observer patch; existing Advanced supervision must roll back that owner while the independent control remains active.

This hook belongs only to the external author test Mods. No new production GameBridge Hook, shared native API, frame driver, or save operation is added. Ordinary lifecycle and player-instance facts remain in [RuntimeLifecycle](RuntimeLifecycle.md) and [DebugConsoleInput](DebugConsoleInput.md). The SDK distributes only generated author code and provenance, never the original native method bodies or local compiler surfaces.

PN-023 adds a direct UnityEngine.Object inequality reference in Rowan's agent check, which exposed the memory-loaded UnityEngine.CoreModule origin boundary. [Focused preloader review](../../reviews/code/2026/20260909-0007-platform-native-preloader-origin.md) owns the original-file versus transformed-resident distinction. This adds no native method patch to the platform; the author observation getter remains unchanged.
