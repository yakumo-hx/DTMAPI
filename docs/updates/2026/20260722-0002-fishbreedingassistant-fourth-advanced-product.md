# FishBreedingAssistant Fourth Advanced Product

## Metadata

- Update ID: `20260722-0002`
- Date: `2026-07-22`
- Lifecycle Status: `verified`
- Validation Level: `docs,source,unit,runtime`
- Runtime Validation: `passed`
- Related Issue State: `closed`
- Area: fish-roe/advanced/product-native/sdk/package/compatibility/hooks/lifecycle

## Source Request

After completing the corrected ActionSpeed third-save short acceptance, separately admit and split FishBreedingAssistant. Decide the placeholder data table, native fish-name fallback, and title Hook ownership before implementation; do not copy the existing GameBridge implementation. Keep validation focused and do not run Release, L0-L5, long tests, or publish 0.5.5.

## Owning Review

[FishBreedingAssistant Fourth Product Admission Review](../../reviews/code/2026/20260722-0006-fishbreedingassistant-fourth-product-admission-review.md)

## Scope

This Update owns the singular migration of `Yuuka.DTMAPI.FishBreedingAssistant` into a managed Advanced ProductNative assembly. It preserves Workshop/product/config identity and the title-only visible behavior, owns one title Hook and formatting rule, retires the empty public lookup from production, and leaves the old three-Hook `IItemTooltipApi` executor only as demand-inactive Compatibility. Its initially product-local native name fallback moved, after Animal became the second independent consumer, to the narrow owner-bound `IItemDisplayNameApi` SharedNative adapter approved by Review `20260722-0008`.

It does not admit AnimalHusbandryProgress, implement a fish-breeding dataset or G7, promote a public item-query API, publish 0.5.5, or run the complete Release suite, L0-L5, or a long test.

## Planned Slices

1. Record ownership and bind the fourth product to the tracked build, exact Hook target, Harmony owner, and SDK policy.
2. Move the player runtime to `products/first-party/FishBreedingAssistant`, remove the placeholder execution path, and implement the ProductNative native-title fallback and one atomic Hook.
3. Freeze the old API executor under Compatibility with bidirectional product collision handling.
4. Extend Catalog-driven package, install/Doctor/Manager, zero-leftover, and focused QA checks without a product-specific toolchain fork.
5. Pass focused source/unit/package checks and one bounded third-save product/title/owner-deactivation/restoration smoke.

## Changed Files

- `products/first-party/FishBreedingAssistant`: managed Advanced author intent/project/manifest, preserved Workshop assets/i18n/config and ProductNative title callback/runtime/atomic Hook installer.
- `testmods/FishBreedingAssistantMod`, `DTMAPI.sln`, `tools/scripts/build.ps1`: removed the old Strict shell from the active build/source lane.
- `src/DTMAPI.GameBridge.DolocTown/Compatibility/FishRoeTooltip`: retained the unchanged old ABI executor as explicit demand-inactive Compatibility with bidirectional fail-closed owner reconciliation.
- `src/DTMAPI.Abstractions/ExperimentalGameBridge.cs`, GameBridge `Features/ItemDisplayName` and owner-bound facades: added the minimal two-consumer read-only title adapter after the Fish/Animal comparison; Fish no longer queries native item protos directly.
- Author SDK policy/registry, Core, Doctor, Catalog, release/install/publish inventory and thin focused wrapper/checker: admitted the exact product through the shared Catalog-driven Advanced path and added live mandatory-Runtime zero-leftover coverage.
- optional QA: observes the product callback and actual one-patch Harmony owner externally without adding QA state to the player DLL.
- admission/shared-boundary Reviews, this Update, Hook/API/architecture/roadmap and smoke records.

## Validation

- Fish focused source checker: PASS; the product contains no `IItemTooltipApi`, compatibility implementation namespace, placeholder lookup execution or direct `QueryItemProto` call.
- GameBridge Unit and optional QA Unit tests: PASS, including product-first and compatibility-first fail-closed arbitration, exact one-target owner observation and final owner deactivation.
- Author SDK and InstallDoctor focused test executables: PASS with the exact Fish policy/owner/identity registry projection.
- Catalog checker and generic Advanced validate/build/package: PASS; live zero-leftover rule finds no Fish ProductNative execution in mandatory Runtime.
- Author SDK ZIP SHA-256: `fe9d4f3cf230bf004e98f15709ade960f08622feaff0c8c25b86750e25febf15`.
- Frozen corrected package SHA-256: `1EE02771C6C50D8ED345BD3E648DAD19136CE785F11A11F318E6A30ABBEE3430`; entry SHA-256 `2035ca1509ec588153bfdcd64e0599abdbdb7247f582c06b62272dbc5cf04e7a`; reference receipt SHA-256 `9f8c00f6a1320720cd15dc850fad7b2f672455edcf0df761008bd555ccbd0a3e`.
- Complete Release, L0–L5, long and GC tests were intentionally not run.

## Evidence

- Admission prerequisite: `GAME-SMOKE/20260722-141220` passed the corrected ActionSpeed and OneActionComplete acceptance boundary.
- Historical fish title baseline: `GAME-SMOKE/20260613-201326` observed native `fish -> 鱼` and `鱼卵 (鱼)` through the former GameBridge executor.
- `GAME-SMOKE/20260722-145154` is the accepted direct-fallback product run: LocalDevelopment Advanced DLL, exactly one `Item.get_title` owner patch, `鱼卵 (鱼)`, callback/instance/actual owner/Core-root zero after Loader deactivation, title/restoration/clean exit. `144108` and `144534` never launched; `145001` proved Fish but failed the combined source-selection precondition.
- The subsequent two-consumer comparison moved only the native lookup into `IItemDisplayNameApi` and rebuilt the package. Neither `GAME-SMOKE/20260722-153320` nor `153845` observed a process/log in its 180-second runner window, and exact retained QA stages were recovered by receipt under the runtime lock. A later delayed title-only process loaded the current Fish DLL with one owner patch and reported `ItemDisplayName` ready, but it started after QA/profile restoration, requested no save and exercised no fish-title or owner-deactivation QA. It remains non-acceptance infrastructure evidence; at that point the current-package behavior retest was still open.
- After the user resolved the Steam cloud-save conflict, `GAME-SMOKE/20260722-161022` passed against that unchanged current shared-adapter package. It loaded save slot 3, exercised the native display-name lookup, rendered `鱼卵 (鱼)`, observed exactly one product owner patch/target, and reduced instance/actual patch/callback/Core roots to zero through real Loader deactivation. Save/config/profile/QA restoration, no-fatal and clean process exit passed.
- The five-product correction rebuilt the package with the narrowed positive-only `IItemDisplayNameApi` contract and exception-safe closeout. After two retained QA-transition failures exposed and fixed a same-frame native-UI/ReturnHome race outside the product, `GAME-SMOKE/20260722-180502` passed the exact Fish/Animal profile and again proved `鱼卵 (鱼)`, one actual owner patch/target, exact Loader cleanup to zero, title recovery, byte-identical save restoration and clean exit. Update `20260722-0004` owns the correction/root-cause details.

## Rollback

Remove the Fish Advanced Catalog row/policy/product wiring and restore the former Strict source plus GameBridge executor as one unit. Do not leave a hand-authored Advanced manifest, receipt, or partially moved native fallback.

## Follow-up

The corrected shared-adapter ProductNative package is frozen by `180502`. The Fish/Animal comparison promotes only item display-name resolution; no fish dataset, title Hook or product lifecycle moves into GameBridge. A sixth product remains separately blocked.
