# Zoom Tenth Advanced Product

## Metadata

- Update ID: `20260724-0002`
- Date: `2026-07-24`
- Lifecycle Status: `verified`
- Validation Level: `docs,source,unit,runtime,player`
- Runtime Validation: `passed`
- Related Issue State: `closed`

## Source Request And Authority

After independently reaccepting MoreEquipmentSlots and freezing the exact
nine-product aggregate, compare the route without inventing SharedNative,
admit one tenth product, implement it, run focused validation and one bounded
third-save smoke, then independently review the result.

Admission authority is
[Review `20260724-0005`](../../reviews/code/2026/20260724-0005-tenth-product-zoom-admission-review.md):

- only `DTMAPI.ZoomMod` is admitted;
- DebugConsole remains on the optional diagnostic-host track;
- Mine remains `split-decided` / `PrototypeBlocked`;
- no new Host, public API, SharedNative component, receipt or checker family is
  authorized.

The user explicitly prohibits repeatedly running complete Release. This Update
will run no complete Release, L0-L5, GC gradient or long test.

## Intended Atomic Switch

- Replace the retained Strict product source with an SDK-generated
  `netstandard2.0` Advanced product at `1.0.0`, minimum DTMAPI `0.5.5`.
- ProductNative directly owns scale/config/keybind state, vanilla
  orthographic-size snapshot/restoration and one exact
  `DolocAPI.SetEnvCamera(Vector2,Vector2,bool,bool,bool)` postfix.
- Move the old `ICameraViewApi` and obsolete `ICameraZoomApi` executors into
  the existing dormant-shipped Compatibility Host for the retained published
  consumer.
- Keep mandatory GameBridge limited to thin proxies, demand/owner coordination
  and the independent ItemDisplayName environment-reset route.
- Preserve game ownership of camera objects, follow/clamp, environment,
  background, fog and panorama. Add no product save sidecar.

## Implemented Boundary

- `products/first-party/Zoom` is the SDK/Catalog-generated
  `DTMAPI.ZoomMod` `1.0.0` Advanced product. Its nine production files contain
  1,511 physical / 1,412 non-empty lines at the final reaccepted candidate.
- ProductNative owns two keybinds, config, current scale, the captured vanilla
  orthographic size, native CameraController derived-state refresh, exact
  restoration and one atomic
  `dtmapi.mod.dtmapi.zoommod` Postfix on the five-parameter
  `DolocAPI.SetEnvCamera`.
- The frozen CameraView/CameraZoom executors moved under the existing
  `DTMAPI.GameBridge.DolocTown.Compatibility` component. Product-first and
  compatibility-first activation both fail closed before native writes.
- Mandatory GameBridge retains only provider/demand/owner coordination and the
  separately justified ItemDisplayName environment-reset route. The old
  Strict Zoom product source and its product-specific owner-lifetime QA route
  were removed.
- `ICameraViewApi`, `ICameraViewLease`, CameraView DTOs and the obsolete
  CameraZoom family remain warning-bearing Frozen ABI. The exact retained Zoom
  DLL hash is
  `DFA74BDD3561A9E9647FEC01AB9B48F095826D2A752AE920566BE2C5063971B3`;
  the final ABI gate locks its complete 35-member CameraView MemberRef set,
  not the earlier incorrect three-member summary.

## Validation And Evidence

Focused validation passed:

- Unit Release build, `zoom-product`, `compatibility-host`, `api-metadata`,
  `zoom-acceptance-routing`, QaUnit, save-mode runner, Batch 4 semantic
  inventory and Batch 6 Phase 0 contract;
- the exact retained ABI gate over baseline Abstractions, all eleven public
  Workshop products, mandatory GameBridge and Compatibility Host:
  zero public deletions, nine known frozen consumers and zero mandatory heavy
  executor markers;
- Catalog, Author SDK/package, Install Doctor and Player Doctor product-policy
  checks;
- Author SDK SHA-256
  `9BBAB1A6EBA71219D5BE5100533C3402223296C22D54FCC3944D8B2A814B3935`;
  product package
  `355958FAAC1228EA75251AC4CF52336BC14643A716D19D4494E2DA21B94A4093`;
  entry DLL
  `2475736D88341337468D9448545ED5B3D2A50AD7A5EDB266770FBDDC4DFBA41B`;
  Advanced reference receipt
  `A2241DF754F887AD9BA535A34A217CF4D29AA29E9B2D1439920D5603FF9914E2`;
  Player Doctor
  `B8AA4AFF0A704680C9E3344D0A61345623AC5FD8F32CEC0B763F9F5BE1F48D40`.

`GAME-SMOKE/20260724-180233` was initially treated as accepted third-save
`NoNativeSave` evidence. It loaded the current local Advanced product and HookProbe, observed
Product Hook `1` / Compatibility Hook `0`, exercised native camera sizes
`33.75` at 2x and `67.5` at 4x, invoked the real five-parameter
`SetEnvCamera`, observed the Postfix reapply to `270`, restored `67.5` on
config disable, re-enabled once, returned to title from pending 4x and proved
`title4to1+native1`. Real Loader deactivation then reported product instance,
callback, exact Hook and Core roots all zero. Player Doctor was green, the
three selected archive files and both committed-sidecar paths were unchanged
by length/hash/mtime before any cleanup, no routine byte backup or player
archive writeback occurred, source/profile/QA state restored, and no game
process remained. The independent correction below supersedes the behavior-
acceptance interpretation.

`GAME-SMOKE/20260724-175944` is retained non-acceptance evidence: product
loading and no-save preservation passed, but the QA case treated the short
post-`SaveLoaded` `CurrentRoom` publication gap as failure. The correction
keeps the case pending before any camera write and retries on a later frame.
Two earlier installer preflight stops did not launch `DolocTown.exe` or create
Smoke evidence.

No complete Release, L0-L5, GC gradient or long test was run. Complete Release
count for this slice is zero.

## Independent Closeout Audit Correction

[Review `20260724-0006`](../../reviews/code/2026/20260724-0006-moreequipment-zoom-closeout-audit.md)
finds that the structural migration remains implemented, but Zoom behavior is
not accepted:

- the real game `SetEnvCamera` method does not change orthographic size;
- the Postfix recaptured the already-scaled `67.5` value as vanilla and applied
  `4x` again, producing `270`;
- config disable restored the polluted `67.5`, not the original `16.875`;
- the same baseline logic leaves `2x -> 1x` and `MaxViewScale -> 1` unable to
  restore the true native size;
- the Unit/QA oracles modeled or accepted the polluted baseline.

Accordingly `180233` is retained non-acceptance evidence. Its archive,
sidecar, Doctor, ownership and Loader-zero observations remain valid, while its
camera restoration claim does not. The retained exact ABI set contains 35
MemberRefs, not 36; the exact-set gate itself is complete.

The evidence package/source hashes bind the tested content, but its manifests
name parent commit `02265fd75fcf` rather than final implementation commit
`cbabf184`. This does not independently require a rerun. After the focused
baseline tests pass, run one corrected bounded third-save `NoNativeSave` Zoom
smoke; do not run a complete Release, L0-L5, GC gradient or long test.

## Corrected Final Reacceptance

The closeout corrections preserve a true native camera baseline instead of
recapturing the product's last scaled value. Direct `2x -> 1x`, decrement to
1x, `MaxViewScale -> 1`, config disable, title recovery and Loader cleanup now
restore the actual baseline. A real `SetEnvCamera` callback whose current
orthographic size still equals the product-applied value reuses the existing
baseline; a genuinely changed native size can still be registered explicitly.
Focused `zoom-product`, `zoom-acceptance-routing`, Compatibility Host,
API-metadata and QA tests cover these paths.

`GAME-SMOKE/20260724-202032` is the accepted exact-final-candidate third-save
`NoNativeSave` run. Its release manifest records
`BuildCommit=25403595da3e`. It reports:

```text
scale2=33.75
direct1=16.875
scale4=67.5
resetVanilla=16.875
afterRealSetEnvCameraTwice=67.5
maxTo1Restore=16.875
configDisableRestore=16.875
```

The same run proves one product Hook and zero Compatibility Hooks, correct
pending-title restoration, real Loader deactivation from
`title4to1+native1+actual1+callback1` to
`instance0+actual0+callback0+roots0`, Player Doctor green, unchanged player
archive/committed-sidecar metadata before cleanup, clean source/profile
restoration and no remaining game process. Historical `180233` remains
non-acceptance evidence and is not reinterpreted.

The final deterministic artifacts are:

- package SHA-256
  `E080423A76A0A068717D6939B52E894BE7C4F49B066E541393F225F04D26C29C`;
- entry DLL SHA-256
  `C1BEAE853EA8508E7C2ABDB52774A358B82B7FAF771F810AE873E6732C1D431D`;
- Advanced reference receipt SHA-256
  `52ECD4AE70421B61B8CA7B10F7D48AAE2F310538559C77F6B3A138F175CCD331`.

Review `20260724-0006` closes after this evidence. No complete Release, L0-L5,
GC gradient or long test was run; complete Release count remains zero.

## Second Independent Audit Correction

[Review `20260724-0007`](../../reviews/code/2026/20260724-0007-equipment-zoom-final-closeout-audit.md)
supersedes the lifecycle conclusion above. The covered orthographic-size
correction remains valid, but resolution/fullscreen refresh can derive
`CameraController.camSize` and room ranges from the scaled camera and leave
that state after a 1x restore. The same Review also finds that a failed
`MaxViewScale -> 1` apply is overwritten by a success status. Zoom remains
physically migrated but `implemented/open` until native derived-state restore,
failure propagation, focused tests and one smallest corrected game run pass.

## Final Derived-State Reacceptance

Commit `136a7278ed79614d7dffe259d21789b02329d869` closes the second
independent audit findings without changing the one-Hook ownership boundary:

- every transition to native 1x restores the retained orthographic baseline
  and invokes the reviewed native `CameraController.RefreshResolution()` path,
  so `camSize` and the room x/y ranges are rebuilt from vanilla state;
- scale application is transactional. A failed direct/decrement/configuration
  transition to 1x retains failure and cannot publish a successful state or
  replace the vanilla baseline;
- a resolution/fullscreen refresh at 2x or 4x may temporarily derive native
  controller state from the scaled camera, but the next 1x, configuration
  disable, title recovery or Loader deactivation restores both the camera and
  those derived fields;
- focused `zoom-product`, `zoom-acceptance-routing`, `compatibility-host`,
  QaUnit and save-mode runner checks pass. The final Catalog-generated package
  SHA-256 is
  `0066C3611A53121BC35B4791D2559BBCD524F5AAB337FFBA873B96FF78BA7C67`,
  the 30,720-byte entry DLL SHA-256 is
  `11D23B0D3E7A884BDB7829B4CFC065D441528AA030FE397C354A043BA14A37F0`,
  and the Advanced reference receipt SHA-256 is
  `59F4AD702A9FFE04DA22274895EF0DDBD8F20F160B5163D396BC15396ED86176`.

`GAME-SMOKE/20260724-221902` is the final accepted third-save
`NoNativeSave` evidence and binds
`BuildCommit=136a7278ed79614d7dffe259d21789b02329d869`. It reproduces native
resolution/fullscreen-derived state at 2x and 4x, then verifies the original
`16.875` orthographic baseline and exact initial `camSize`/x-range/y-range
after direct 1x, maximum-to-1, configuration disable, title recovery and real
Loader deactivation. The product remains one Hook while active and finishes at
`instance0+actual0+callback0+roots0`; selected player archive and committed
sidecar metadata are unchanged before cleanup, and no game process remains.

Review `20260724-0007` is therefore dispositioned by this Update and Zoom
returns to `verified/closed`. Historical `180233`, `175944`, and the earlier
preflight stop remain non-acceptance evidence. No complete Release, L0-L5, GC
gradient or long test was run.

## Runtime Weight And Cross-Product Result

The five mandatory Runtime projects now contain 65,534 physical / 58,710
non-empty compiled source lines and their five DLLs total 2,111,488 bytes.
Mandatory GameBridge is 26,658 / 23,602 lines and 798,208 bytes. Relative to
the corrected nine-product baseline this is `-807 / -727 / -24,064`; the
default-loaded reduction is real.

The final ten ProductNative source trees total 91 files / 23,347 physical /
21,526 non-empty lines and their DLLs total 531,456 bytes. Mandatory plus
products is therefore 88,881 / 80,236 lines and 2,642,944 bytes; the
Compatibility Host is 437,760
bytes. This proves only a smaller default-loaded Runtime. It does not prove a
smaller repository, download, installation, total shipped package or
all-products-enabled process.

The ten-product comparison found no new common native owner. Catalog,
Advanced SDK/package, Doctor/Manager, ConfigMenu registration, lifecycle and
zero-leftover machinery remain Platform. Camera and the other nine product
domains do not justify new SharedNative; the existing two-consumer
ItemDisplayName adapter remains the only promoted gameplay/native adapter in
this route.

## Rollback

Rollback reverts the Catalog/source/identity switch together: restore the
Strict product and mandatory Camera feature, remove the new Advanced product
and Compatibility Host Camera component, and restore the previous provider
routes. Do not roll back only one owner or use destructive Git operations
across unrelated work.

## Follow-Up

Freeze Zoom and the exact ten-product behavior/owner baseline as
`verified/closed`. DebugConsole, Mine, every eleventh-or-later product, G7 and
0.5.5 publication remain blocked pending their own bounded authority.
