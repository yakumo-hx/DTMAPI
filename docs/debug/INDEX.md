# DTMAPI Debug Index

DTMAPI debug records must be durable and evidence-based. Do not rely on chat memory for repeated bugs.

## Required Debug Assets

- `issues/`: one recurring symptom per issue file.
- `protocols/`: repeatable evidence collection procedures.
- `regressions/smoke-matrix.md`: build/game/hook/exit regression matrix.
- `evidence/`: raw logs and collected reports, grouped by issue and date.
- `lessons.md`: failed directions and engineering lessons that future Codex sessions must not forget.

## First Known High-Risk Issues

- Direct `DolocTown.exe` smoke launch can exit before title and leave `Fatal error: Another instance is already running`; use Steam launch for smoke unless explicitly testing direct EXE behavior.
- Steam can get stuck before process creation after a previous launch action, with `steam://rungameid/2285550` logged but no new `DolocTown.exe` or DTMAPI startup log; track this as a Steam launch-blocking issue, not as DTMAPI startup slowness.
- Startup evidence should compare `startup-timeline.json` launch wall-clock fields against DTMAPI startup segment logs; a slow `LaunchToStartupPatternMs` with normal `Bootstrap.Awake totalMs` is pre-runtime launch delay, not DTMAPI runtime startup cost. Use `tools/scripts/run-startup-samples.ps1` for repeated normal/slow/blocked launch sampling; use `-NoTitleSettingsMenu -AutoExitAfterSeconds 20` for faster pure-startup sampling, `-StopOnSlowSample` for capture-and-stop runs, and read `startup-sample-summary.md` for threshold counts and min/max/average ranges. Use `tools/scripts/run-startup-monitor.ps1` when the goal is a longer unattended capture loop that stops on slow, blocked, needs-review, failed, or leftover-process evidence and preserves the triggering `STARTUP-SAMPLES` batch. Use `tools/scripts/run-startup-observer.ps1` when the launch is triggered outside the smoke harness, such as manual Steam UI launches. Use `tools/scripts/compare-startup-evidence.ps1` before claiming the normal-vs-abnormal requirement is satisfied; it must report `RuntimeSlowComparisonReady` for a true DTMAPI runtime slow comparison. Failed child smokes or batches should still leave primary evidence and aggregate summaries; treat `sample-failure-count-*` separately from true DTMAPI runtime slowness.
- Steam shows Doloc Town as still exiting after the game window closes.
- Stutter/performance regressions after save load or inventory/menu interactions.
- Config menu input boundaries and title-screen-only UI entry.
- HookProbe is test-only; leaving it installed during normal manual play can open DTMAPI UI after save load and block gameplay hotkeys.
- 0.2.4 manual QA regression ledger is open in `issues/ISSUE-005-20260603-manual-qa-024-regressions.md`. Treat it as newer than all 0.2.3 smoke evidence. Fresh 0.2.4 evidence now exists for Animal hidden-produce/custom color (`GAME-SMOKE/20260603-152851`, `GAME-SMOKE/20260603-160739`), ActionSpeed/AutoFishing runtime and title UI (`GAME-SMOKE/20260603-154207`, `GAME-SMOKE/20260603-154721`, `GAME-SMOKE/20260603-154816`, `GAME-SMOKE/20260603-160739`, `GAME-SMOKE/20260603-173435`), Y-console instant save/teleport CSV (`GAME-SMOKE/20260603-152451`), SecondMotor dual-visible/appearance/ride/original restore plus edge transition (`GAME-SMOKE/20260603-155505`, `GAME-SMOKE/20260603-202948`), Oil metadata/Y-console source/category plus coal-drop mining (`GAME-SMOKE/20260603-190919`, superseding `GAME-SMOKE/20260603-184455` for fuel/icon/sale/localization checks), Mine official JSON/workbench/2x-cover/player placement/placed-production (`GAME-SMOKE/20260603-210216`, superseding earlier Mine telemetry-only runs), and More Equipment Slots read-only player equipment strip plus equip/recover (`GAME-SMOKE/20260603-210216`, superseding earlier storage-only runs). Manual visual SecondMotor crossing and an equipment-strip screenshot remain useful user-acceptance polish, but the automated third-save requirements are smoke-proven.
- 0.2.5 critical manual QA ledger is smoke-verified in `issues/ISSUE-006-20260604-critical-manual-qa-025.md`. Treat it as newer than all 0.2.4 smoke evidence. Current 0.2.5 evidence: full enabled third-save smoke `GAME-SMOKE/20260604-111533` proves SecondMotor registration/key/dual visibility/appearance isolation/edge transition/original restore, Mine official Industrial tech route with `dtmapi_oilx10|steel_ingotx10`, `EquipmentFuncCase` storage `16/4`, 2x renderer scale, and production into Mine-owned storage; disabled SecondMotor smoke `GAME-SMOKE/20260604-111901` proves `Skip=2 Registration=0 Mail=0`; Y-console lifecycle smoke `GAME-SMOKE/20260604-112250` proves first open after `SaveLoaded` has `searchText=<empty>` while same-save reopens can preserve `石油`; all checks exited with no `DolocTown.exe`.
- 0.2.4 new content/API evidence: Oil official JSON/Y-console metadata and coal-drop mining are smoke-proven in `GAME-SMOKE/20260603-190919`: `dtmapi_oil` loads as native `ItemFunction`, `fuelEnergy=1500` is above base highest `pumpkin:1200`, source/category are visible via `IInventoryDebugApi`, sale/localization/icon metadata are present, and a rendered `coal_mine` hit through native `ToolCollider.HandleTools` placed `dtmapi_oil x1` through native backpack placement. Mine official JSON/player placement/API telemetry is smoke-proven in `GAME-SMOKE/20260603-210216`: `NewContentMineOfficialJson=true`, `generatedItemType=DolocTown.ItemEquipment`, `cover=8x6`, `baseWellCover=4x3`, `sceneAsset=sprite_equipment_well`, `recipeInputs=stonex80|iron_ingotx8|electric_wirex4|dtmapi_oilx5`, `recipeGroup=equipment_workbench includes=True`, placement evidence `playerItem=dtmapi_mine ItemEquipment`, screenshot `DTMAPI-evidence/NEWCONTENT-024/20260603-210256/mine-placed-dtmapi-mine.png`, and runtime production state `fuel=7180/7200`, `cycleTUs=24`, `electricPower=10`, and native backpack placement for `copper_ore x1`. EquipmentSlots now has player equipment strip evidence in `GAME-SMOKE/20260603-210216`: AccessoriesBar hooks render three read-only clones, `grandmas_button` occupies `dtmapi.more_equipment.1`, attributes are applied without visual-slot takeover, and recovery returns the item to backpack `0->1`. The equipment-strip screenshot fallback returned unavailable in-save; keep the log/summary evidence and optional manual screenshot as follow-up polish.
- Direct EXE smoke must launch `DolocTown.exe` with `SteamAppId=2285550` and `SteamGameId=2285550` in the child environment. Failed attempts `GAME-SMOKE/20260603-163034` and `GAME-SMOKE/20260603-163622` reached DTMAPI startup but never loaded slot 3 because a fatal `Another instance is already running` popup appeared. Passing DirectExe evidence `GAME-SMOKE/20260603-164202` records `DirectExeSteamEnv=SteamAppId=2285550;SteamGameId=2285550`.
- F-key input can reach migrated mods through Unity Input System, but the DTMAPI overlay still has an open rendering bug tracked in `issues/ISSUE-003-hotkey-openconfig-no-overlay.md`.
- External smoke key injection can miss the Doloc Town window or reuse a stale key-down state. For debug-console smoke, require foreground verification, short retries, and exact log-count checks. Retained attempts `GAME-SMOKE/20260601-054331`, `GAME-SMOKE/20260601-054917`, and `GAME-SMOKE/20260601-055310` show the difference between API success, focus/key-state issues, and a too-strict `reason=hotkey T` matcher; 0.2.0 evidence `GAME-SMOKE/20260601-060427` verified T/Escape/T/T. The 0.2.1 console moved to Y because T conflicts with game input; final Y evidence is `GAME-SMOKE/20260601-140915`, with `Y/Escape/Y/Y`, inventory/weather/teleport/time/movement, third-save load, and clean exit.
- Y console input reliability now depends on DTMAPI's cached key-down edge, not only frame-local `GetKeyDown`. The stronger 10-short-tap/hold evidence is `GAME-SMOKE/20260602-022417`, which also captures native icons and hover-name evidence under `DTMAPI-evidence/DEBUG-CONSOLE-UI/20260602-022500`; later aggregate smokes such as `GAME-SMOKE/20260602-020710` keep `DebugConsoleTenYShortTaps=true` and `DebugConsoleHoldYNoFlicker=true`.
- 0.2.2 Y console hotkey and Workshop-mod UI evidence is `GAME-SMOKE/20260602-122848`: Steam launch, third-save load, Y/Escape/Y/Y, ten short Y taps, long-Y no-flicker, screenshot capture, no fatal popup, and no leftover `DolocTown.exe`. The copied screenshot `DTMAPI-evidence/DEBUG-CONSOLE-UI/20260602-122951/debug-console.png` shows `modItemsOnly=True`, search text `黄油`, native mod icons, and hover/source text for `mod_butter` from `Workshop.3722791728`.
- 0.2.3 partial code pass `20260603-0003` changes Y-console item cells so hover no longer dirties/rebuilds the whole panel before click dispatch, left click uses a Unity `Button` give-1 path, and right click has a `Mouse1` fallback against the recently hovered/clicked item when reflected Unity `EventTrigger` does not deliver a right-button click. `20260603-0004` extends the UI with source/category columns, nearby hover detail tooltip, and detailed teleport names, with third-save smoke evidence below.
- Failed partial smoke `GAME-SMOKE/20260603-030142` reached third-save `SaveLoaded`, verified `AutoFishingPhase`, `DebugInventory`, and clean exit/no fatal popup, but external Y injection failed to open the console (`DebugConsoleOpenY1=false` through attempt 4). Treat it as AutoFishing/Inventory partial evidence only, not Y-console UI or real-click proof.
- Passing 0.2.3 Y-console productization smoke `GAME-SMOKE/20260603-032406` reached the third save and verified `DebugConsoleOpenY1=true`, `DebugConsoleCloseEscape=true`, `DebugConsoleOpenY2=true`, `DebugConsoleCloseY=true`, `DebugConsoleTenYShortTaps=true`, `DebugConsoleHoldYNoFlicker=true`, `DebugInventory=true`, `DebugTeleport=true`, `ProcessExited=true`, and no fatal popup. Screenshot `DTMAPI-evidence/DEBUG-CONSOLE-UI/20260603-032451/debug-console.png` shows the larger source/category layout, nearby item detail tooltip, and non-generic teleport labels such as `上游丘陵1-下端`; logs show `wood` and `dtmapi_second_motor_key` native backpack gives with before/after counts and teleport from farm container room to `city_郊区-上游丘陵1`.
- Passing 0.2.3 real mouse item-give smoke `GAME-SMOKE/20260603-035653` reached the third save and verified `DebugConsoleMouseGive=true`, with OS-level left-click on the first item cell logging `owner=DTMAPI.DebugConsoleMod item=dtmapi_second_motor_key requested=1 placed=1 before=0 after=1 success=True`, and OS-level right-click logging `Debug console right-click give source=mouse1-fallback item=dtmapi_second_motor_key` followed by `requested=10 placed=10 before=1 after=11 success=True`; process check says no `DolocTown.exe` and no fatal popup.
- Passing 0.2.3 AnimalHusbandryProgress visual/config smoke is split across `GAME-SMOKE/20260603-040354` and title smoke `GAME-SMOKE/20260603-052444`: the third-save AnimalPanel run verified `AnimalViewerUi=true`, logged native-like UI overlay `rows=1, 羊毛脂 0/100`, captured a delayed screenshot under `DTMAPI-evidence/ANIMAL-001/20260603-040437/animal-viewer-ui-delayed.png`, and exited cleanly; the title settings run captured `DTMAPI-evidence/UI-004/20260603-052523/title-settings-config-animal-husbandry-progress.png`, which was visually checked for direct swatches, no right-side `Orange` value text, no non-Custom `FF942E` input, and a far-right custom `+` swatch.
- Passing 0.2.3 ActionSpeed strong auto-fill smoke `GAME-SMOKE/20260603-041950` reached the third save and verified `ActionSpeedInteraction=true`; no-key in-water auto-fill invoked native `ItemBottle.UseAsItem` with `strong=True`, `cooldownSeconds=0.08`, `normalCooldownSeconds=0.1`, `strongCooldownSeconds=0.08`, `inventoryChanged=True`, and clean exit. Title settings smoke `GAME-SMOKE/20260603-052444` captured `DTMAPI-evidence/UI-004/20260603-052523/title-settings-config-action-speed.png`, visually checked for same-row `自动装水` plus `强化自动装水`.
- Passing 0.2.3 AutoFishing movement/skip smoke `GAME-SMOKE/20260603-042437` reached the third save and verified `AutoFishingInputLog=true`, `AutoFishingMovementCancel=true`, `AutoFishingPhase=true`, and `AutoFishingMiniGameSkip=true`; logs show external F6 enabled automation, W disabled it with `manual-move W`, F6 re-enabled it, native `BodyController.UseFishRod` auto-cast ran, Pull animation speed applied at multiplier 3, and `InstantBite` skipped to `AgentStateFishingPull` with `autoCompleteMiniGame=True` and `skipMiniGame=True`. Title settings smoke `GAME-SMOKE/20260603-052444` captured `title-settings-config-auto-fishing.png`, visually checked for same-row `自动完成小游戏` plus `跳过小游戏`.
- Passing 0.2.4 AutoFishing skip=false minigame smoke `GAME-SMOKE/20260603-173435` reached the third save and verified `AutoFishingMiniGameComplete=true`; the retained failed attempt `GAME-SMOKE/20260603-172124` proved that a rolled garbage item (`waste_plastic_bottle`, `FishProto.IsFish=false`) does not create the native minigame. The passing run logs `FishingGameScrollBar`, `autoHook=AgentStateFishingBattle`, `fish=loach`, `isFish=True`, `forceFishForSmoke=True`, `currentGameStatus=Success`, `visibleSeconds=0.76`, clean exit, and no fatal popup.
- 0.2.2 Workshop item give evidence is `GAME-SMOKE/20260602-122848`. Subscribed `Workshop.3722791728` (`多洛可战前料理 | 奶油 & 黄油系列`) was temporarily enabled from `SAVE/mod_infos.json`, backed up/restored under `WORKSHOP-MODINFO-BACKUP/20260602-122846`, and the final restored entry is `enabled=False priority=-1`. The smoke log gave `mod_butter` (`黄油`) from `Workshop.3722791728` with `runtimeLoaded=True`, `before=0`, `after=1`, `given=1`, and `workshopRuntimeItem=verified`. Earlier mineral-seed evidence remains under `GAME-SMOKE/20260602-115735` / `WORKSHOP-MODINFO-BACKUP/20260602-115707`.
- 0.2.2 second-motor evidence is `GAME-SMOKE/20260602-122848`: `DTMAPI.SecondMotorMod` registered `dtmapi.second_motor`, the official-local key item was given, generated native `ItemMotorKey.OnUse` summoned the DTMAPI clone on `farm_type1-平地`, ride-on/ride-off events fired, original motor was restored/summoned, speed multiplier `2` produced base/effective max speed `25/50`, and endurance progress stayed `1` across summon/ride/dismount. A retained failed run `GAME-SMOKE/20260602-112141` is the disabled-location negative proof: the key hook intercepted `dtmapi_second_motor_key` in an in-house/disabled room and failed with `reason=in-house`.
- 0.2.3 SecondMotor current evidence is `GAME-SMOKE/20260603-051204` plus title UI smoke `GAME-SMOKE/20260603-052444`: the official-local package is `DTMAPI 异色飞行摩托`, the item title is `飞行摩托钥匙（异色）`, `IMailDeliveryApi` called native `DolocAPI.SendItemAsEmail` with `sent=True`, `pendingMail=1`, the smoke first generated an original `motor_key` and verified the native `ItemMotorKey.OnUse` path, then generated `dtmapi_second_motor_key` for the DTMAPI route. The dual-visible probe now logs `originalVisible=True originalRoom=farm_type1-平地 secondVisible=True secondRoom=farm_type1-平地 dualVisible=True`, followed by second-motor ride-on/ride-off, original motor snapshot restore, original summon OK, 2x speed, unchanged endurance, clean exit, and no fatal popup. Title screenshot `DTMAPI-evidence/UI-004/20260603-052523/title-settings-config-second-motor.png` shows the DTMAPI status page. Historical run `GAME-SMOKE/20260603-050208` retained the earlier `dualVisible=False` blocker and should be cited only as the pre-fix failure.
- 0.2.3 SecondMotor appearance-fix evidence is `GAME-SMOKE/20260603-082749`: installer output says global official vehicle example sprite assets were skipped and stale `Content/DTMAPI/official-vehicle-example` assets removed; the installed `DTMAPI_SecondMotor` package has no `sprite_vehicle_motor*` files under `Content/DTMAPI`; GameBridge logs `Vehicle.SecondMotorAppearance = experimental`, `appearanceIsolated=True`, `originalScopedTint=0/10`, `secondScopedTint=5/10`, and `appearance=instance-scoped-tint hex=#8CE6FF renderers=5/10 skippedDriver=5`, followed by dual-visible, ride-on/off, original summon OK, `VehicleSecondMotor=true`, clean exit, and no leftover `DolocTown.exe`.
- 0.2.4 SecondMotor edge-transition evidence is `GAME-SMOKE/20260603-202948`: GameBridge patches `DolocAPI.EnterRoom`, logs `Vehicle.SecondMotorMapTransition` target capture/apply for `city_郊区-上游丘陵1`, and the final edge probe records `changedRoom=True`, `secondStillRiding=True`, `secondInCurrentRoom=True`, `distanceToDestination=0.839`, `nearDestination=True`, `originalRoomAfterTransition=farm_type1-平地`, `originalVisibleAfterTransition=False`, `originalAtNewEntry=False`, and `noStuck=True`. Result has `VehicleSecondMotor=true`, `ProcessExited=true`, no fatal popup, and no leftover `DolocTown.exe`. Retained failed attempts before the fix prove the rejected paths: recovery-settle timing, original singleton at the new entry, and stale rider position through native `DolocAPI.AgentPosition`.
- DirectExe vehicle smokes `GAME-SMOKE/20260602-105301` and `GAME-SMOKE/20260602-111609` hit `Fatal error: Another instance is already running` after the game/Steam spawned a second process. Use Steam launch for normal game smoke; keep DirectExe only for explicit harness regression checks.
- The in-save debug console is intentionally a reflected Unity Canvas host plus ordinary DTMAPI mod hotkey, not the old F8/F10 diagnostics overlay. It should stay closed on title screens and block gameplay hotkeys while open.
- The time debug button must advance weather-period boundaries, not 08:00. Current evidence is `GAME-SMOKE/20260602-021349`: `06:10 -> 18:00`, `18:00 -> next-day 00:00`, and `00:00 -> 06:00`, all through `ArchiveDataHandle.PassTimeNoControl`.
- Direct EXE smoke can reach a different failure from Steam launch: `GAME-SMOKE/20260601-131105` loaded the 0.2.1 Y console package but exited before save load with `InvalidOperationException: Steamworks is not initialized` inside the official `ModManager.ReloadMods` path. Treat this as a DirectExe/Steamworks harness blocker; the passing 0.2.1 smokes use Steam launch.
- Do not combine `-AutoOpenAnimalPanel` with ActionSpeed/OneAction gameplay exercise flags in one run unless the smoke harness explicitly closes the animal panel first. Retained interrupted evidence `GAME-SMOKE/20260601-133839` shows `Animal viewer UI evidence OK` followed by repeated `Waiting for NormalGameState before action-speed interaction smoke`; the clean split runs are `GAME-SMOKE/20260601-135144` for animal UI and `GAME-SMOKE/20260601-135239` for ActionSpeed/OneAction.
- Animal hidden-produce rendering must enumerate configured husbandry definitions and fill missing progress as `0`; the current 0.2.3 visual proof is `GAME-SMOKE/20260603-040354/DTMAPI-evidence/ANIMAL-001/20260603-040437/animal-viewer-ui-delayed.png`, showing the native-style `羊毛脂 0/100` row without the old `隐藏产物` label. Earlier `20260602-001819` and `20260602-002157` animal attempts showed stale duplicated text and should not be cited as final visual proof.
- Decal equipment such as `resin_collector` must be created or selected through the game's decal host/slot path; ordinary `CreateEquipment(..., null, -1)` is expected to fail with `建造贴纸设备需要传入decalHost和decalSlot参数`.
- ActionSpeed harvest classification cannot rely only on static/current equipment scanner state. Wild vegetation harvest is reached through `InteractableManagerEx` and a `VegetationRenderer` wrapper, and stale scanner selections must be cleared before collecting water/vegetation evidence.
- ActionSpeed ordinary food/drink is verified as animation speed only; right-click continuous use is intentionally restricted to bottled water. Current gameplay evidence is `GAME-SMOKE/20260603-041950`, which verifies `BottledWaterRightClick`, no-key `AutoFillBottle` through native item paths, and the 0.2.3 strong auto-fill cooldown difference (`0.08` vs ordinary `0.1` in the Debug smoke profile).
- AutoFishing 0.2.3 player toast policy limits visible feedback to F6 on/off and movement cancel; no-water/no-rod/auto-cast states are log/HookStatus evidence. Current third-save smoke `GAME-SMOKE/20260603-042437` verifies external F6, movement cancel through W, native auto-cast, Pull animation speed, wait-phase InstantBite, and skip-minigame routing to `AgentStateFishingPull`. Current 0.2.4 smoke `GAME-SMOKE/20260603-173435` verifies the separate skip=false minigame auto-complete route through `FishingGameScrollBar.UpdateGame`. Earlier `GAME-SMOKE/20260603-030142` remains a partial proof for suppressed no-water/no-rod/cast toast policy.
- OneAction vegetation/dandelion must not be treated as a `DungeonResourceRenderer` resource. It uses `VegetationRenderer.OnFell -> Vegetation.CheckToolConstraints`, so DTMAPI records it as a native exception path and verifies wrong/correct tools without applying one-action damage.
- Hook regressions after Doloc Town build updates.

## Rule

Before changing runtime lifecycle, shutdown, BepInEx, Harmony patches, event dispatch, input, config menu, Workshop loading, or mod loading, check this folder and update it after the attempt.
