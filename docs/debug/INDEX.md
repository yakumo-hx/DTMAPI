# DTMAPI Debug Index

DTMAPI debug records must be durable and evidence-based. Do not rely on chat memory for repeated bugs.

## Required Debug Assets

- `issues/`: one recurring symptom per issue file.
- `protocols/`: repeatable evidence collection procedures.
- `regressions/smoke-matrix.md`: build/game/hook/exit regression matrix.
- `evidence/`: raw logs and collected reports, grouped by issue and date.
- `lessons.md`: failed directions and engineering lessons that future Codex sessions must not forget.
- `../reviews/api/native-owner-domains/INDEX.md`: fixed native-owner domain library for future API/GameBridge rebuilds; read it before turning broad gameplay/content requests into stable API assumptions.

## First Known High-Risk Issues

- 2026-06-15 SecondMotor scoped-asset/clone-cleanup follow-up: user retest found the original motor appearance could still be replaced and second-key summon failed with `NullReferenceException`. Root cause evidence points at two boundaries: the installer copied official global `sprite_vehicle_motor*` names under an enabled official-local package, and clone failures occurred before GameBridge tracked the clone, so cleanup reported `cleaned=0`. Current fix renames copied assets to `Content/DTMAPI/assets/second-motor/dtmapi_second_motor.*`, removes stale `Content/DTMAPI/vehicle-appearance`, rebinds cloned `MotorController` fields to clone-owned components before falling back to native `Init()`, forces native init when the clone lacks its own `enduranceProgressCircle`, and sweeps orphan `DTMAPI.SecondMotor.*` GameObjects. Retained failed smoke `GAME-SMOKE/20260615-191720` proved the intermediate failure was `ensure-stage=reset`; final save-slot 8 smoke `GAME-SMOKE/20260615-192916` and slot 9 smoke `GAME-SMOKE/20260615-193040` passed with scoped sprite isolation, native 1x speed, ride/dismount, clean process exit, and no fatal window. Update: `docs/updates/2026/20260615-0001-second-motor-scoped-asset-clone-cleanup.md`; regression row: `VEHICLE-001`.
- 2026-06-14 multi custom motor API rebuild: native motor ownership is singleton-oriented, so `IMotorVehicleApi.RegisterCustomMotor` remains Experimental and GameBridge-owned even after the first rebuilt SecondMotor route passed save-slot 8 (`GAME-SMOKE/20260614-063456`) and save-slot 9 (`GAME-SMOKE/20260614-063639`) smokes. Slot 8 verifies native-unlocked custom key/scoped-sprite/summon/ride/dismount at 1x; slot 9 verifies custom scoped-sprite summon/ride while the official vehicle remains locked. A retained pre-fix failure `GAME-SMOKE/20260614-043810` showed scoped sprite reflection can throw during clone initialization; current policy is to log reflected sprite failures and fall back to instance-scoped tint rather than aborting summon. Intermediate smokes `GAME-SMOKE/20260614-045125` and `GAME-SMOKE/20260614-045247` proved behavior but are superseded because they still used tint fallback before the texture rect/pivot fix. Review: `docs/reviews/api/2026/20260614-0001-multi-custom-motor-native-owner-review.md`; update: `docs/updates/2026/20260614-0004-multi-custom-motor-api.md`; regression row: `VEHICLE-001`.
- Direct `DolocTown.exe` smoke launch can exit before title and leave `Fatal error: Another instance is already running`; use Steam launch for smoke unless explicitly testing direct EXE behavior.
- Steam can get stuck before process creation after a previous launch action, with `steam://rungameid/2285550` logged but no new `DolocTown.exe` or DTMAPI startup log; track this as a Steam launch-blocking issue, not as DTMAPI startup slowness.
- Startup evidence should compare `startup-timeline.json` launch wall-clock fields against DTMAPI startup segment logs; a slow `LaunchToStartupPatternMs` with normal `Bootstrap.Awake totalMs` is pre-runtime launch delay, not DTMAPI runtime startup cost. Use `tools/scripts/run-startup-samples.ps1` for repeated normal/slow/blocked launch sampling; use `-NoTitleSettingsMenu -AutoExitAfterSeconds 20` for faster pure-startup sampling, `-StopOnSlowSample` for capture-and-stop runs, and read `startup-sample-summary.md` for threshold counts and min/max/average ranges. Use `tools/scripts/run-startup-monitor.ps1` when the goal is a longer unattended capture loop that stops on slow, blocked, needs-review, failed, or leftover-process evidence and preserves the triggering `STARTUP-SAMPLES` batch. Use `tools/scripts/run-startup-observer.ps1` when the launch is triggered outside the smoke harness, such as manual Steam UI launches. Use `tools/scripts/compare-startup-evidence.ps1` before claiming the normal-vs-abnormal requirement is satisfied; it must report `RuntimeSlowComparisonReady` for a true DTMAPI runtime slow comparison. Failed child smokes or batches should still leave primary evidence and aggregate summaries; treat `sample-failure-count-*` separately from true DTMAPI runtime slowness.
- Steam shows Doloc Town as still exiting after the game window closes.
- Stutter/performance regressions after save load or inventory/menu interactions.
- Config menu input boundaries and title-screen-only UI entry.
- 2026-06-08 ConfigMenu API boundary: ordinary mods must use `IDtmConfigMenuApi` only for `Register`, `Add*`, `SetDisplayName`, and `GetKeybindConflicts`. `GetPages`, `GetPage`, `BeginEditing`, `Save`, `Reset`, `Cancel`, `SetPageLock`, and pending preview are DTMAPI internal runtime/UI responsibilities through `IConfigMenuRuntime`; public `IConfigMenuPage` no longer declares edit verbs. Treat older HookProbe page-edit smoke behavior as legacy internal evidence, not a public mod API constraint. Release build and unit tests passed; no game smoke was run for this boundary-only refactor.
- 2026-06-08 Core loader hardening 2: manifest `EntryType` selects a code-mod entry type, `EntryDll` must be a relative `.dll`, multi-entry DLLs without `EntryType` are rejected, dependency-cycle participants are blocked from loading, `MinimumGameVersion` logs a warning when the host cannot detect the game version, and event handler removal is owner-bound. Release build/test passed with `DTMAPI.UnitTests: OK`; no game smoke was run because no GameBridge, CameraView, Harmony hook, or smoke harness behavior changed.
- 2026-06-09 `IInputHelper.Suppress` scope: suppression is one-frame DTMAPI helper state cleared with pressed state in `Input.ClearFrame`. It is not consumed by native input hooks and must not be cited as Doloc Town tool/item/menu/UI isolation. Release build/test passed with `DTMAPI.UnitTests: OK`; no game smoke was run because no GameBridge, native hook, smoke harness, or installed-game behavior changed.
- HookProbe is test-only; leaving it installed during normal manual play can open DTMAPI UI after save load and block gameplay hotkeys.
- 2026-06-14 MoreEquipmentSlots protected storage: the old global `equipment-slots-<owner>.json` sidecar is treated as a cross-save/退订 protection risk. Current branch writes DTMAPI-managed extra equipment slots under `config/protected-items/equipment-slots/slot-<archiveIndex>` with archive/player/save-clock guards, waits for a loaded archive before reading sidecars, migrates legacy global files, and recovers orphaned entries from highest slot index to lowest. Release build/test and `git diff --check` passed. Initial smoke `GAME-SMOKE/20260614-004621` blocked before launch because `DolocTown.exe` was already running. After local install, `GAME-SMOKE/20260614-011222` passed third-save NewContent APIs and MoreEquipmentSlots equip/recover basics (`grandmas_button`, attribute-only `straw_hat`, native hat preserved, clean exit/no fatal/no leftover process). User manual QA then confirmed extra-slot effects apply, character sprite visuals stay vanilla-owned, equip-save-exit-reenter does not duplicate, other saves are not polluted, and disabling MoreEquipmentSlots returns protected items to the backpack. Remaining issue: disabling the mod and re-entering the save in the same game process can leave three inert extra-slot visuals until full restart; item placement is blocked with red warning text. Treat that as current-process hot-disable UI-registration residue, not protected-storage failure. Automated full-flow smoke, Workshop unsubscribe/mail overflow evidence, and hot-disable visual cleanup remain pending.
- 2026-06-12 branch-wide code audit on `Refactor`: current known facts are that Manager pages intentionally render first-N summaries, title config pages render only `page.Items.Take(13)` with no scroll layer, SaveSlots only raises official `DolocAPI.gameManager.archiveFileCount`, CameraView remains orthographic-size-only without background/fog/panorama synchronization, and Mine still exposes/uses hybrid fuel plus electric power settings. Rejected hypotheses: 18+ SaveSlots overflow is not evidence of save-file corruption, CameraView background sync has not been implemented by another hidden path, and Mine has not been converted to pure electric consumption. Validation passed: `git diff --check` with line-ending warnings only, Release build, and Release tests; no game smoke is claimed because the only code edit removed unused private AnimalViewer dead code. Record: `docs/reviews/code/2026/20260612-0001-refactor-branch-wide-code-audit.md`; regression row: `BRANCH-WIDE-CODE-AUDIT-20260612`.
- 2026-06-12 bottom-layer refactor branch `codex/bottom-layer-refactor-audit-20260612`: known facts after implementation are that title Settings uses a text `模组设置` entry plus internal paging for config and Manager lists; SaveSlots keeps official archive/render/load ownership but adds `GameDataUiState.Show`/`GameDataPanel.Select` paging for expanded panels; Mine is normalized to electric-only/no-fuel with stale fuel config fields forced to zero; Mine preview/visual scale reapply is tied to builder and environment-reset lifecycle; AnimalViewer prefix clears stale clones and postfix owns inactive-prefill-activate with repeated smoke refresh/select evidence; Camera background/fog/panorama remains deferred after native-owner review. Rejected hypotheses: SaveSlots 18+/24 containment requires save-system replacement, Mine needs hidden DTMAPI fuel to run electric mode, and Camera background sync can be safely patched from playable movement success alone. Validation: `git diff --check` passed with line-ending warnings only; Release build/test passed with 0 warnings/0 errors and `DTMAPI.UnitTests: OK`; game evidence is `GAME-SMOKE/20260612-165456` title/config, `GAME-SMOKE/20260612-170119` Manager MVP, `GAME-SMOKE/20260612-170240`/`170331`/`170421`/`170512` SaveSlots 6/12/18/24, `GAME-SMOKE/20260612-170708` Mine electric-only and visual containment, `GAME-SMOKE/20260612-170819` AnimalViewer repeated-switch first-frame guard, and Steam HookProbe `GAME-SMOKE/20260612-170928` with no leftover `DolocTown.exe`. Record: `docs/updates/2026/20260612-0012-bottom-layer-refactor-audit-implementation.md`; regression row: `BOTTOM-LAYER-REFACTOR-20260612`.
- 2026-06-12 Title homepage layout guard removal: user feedback after `20260612-0016` found that the reflected official `HomePageTextMenu.ResetLayoutSize(1)` / `slotLayoutGroup.constraintCount=1` guard affected horizontal pause-menu icons. The guard has been removed from `ReflectedTitleMenuSettingsUi`; compact title icon and DTMAPI config/Manager paging remain. Validation: Release build passed; game/manual QA is intentionally pending for the user to retest pause-menu icon layout and title layout. Record: `docs/updates/2026/20260612-0017-title-homepage-layout-guard-removal.md`; regression row: `TITLE-HOMEPAGE-GUARD-REMOVAL-20260612`.
- 2026-06-12 Title/MoreSaves/AutoFishing follow-up: manual feedback found that the title DTMAPI icon had regressed into a large text button, the official homepage menu became two columns, MoreSaves did not honor the intended fixed extra-save behavior, and AutoFishing single-feature toggles still needed independence. The follow-up restores the compact title icon button with `assets/branding/dtmapi-icon.png`, adds a `HomePageTextMenu.ResetLayoutSize(1)` single-column guard only while `HomePageUiState` is active, fixes MoreSaves/SaveSlots enabled requests to exactly 12 total official slots with no player-facing count control, and decouples AutoFishing so `InstantBite` only changes bite timing while `SkipMiniGame` independently routes bite-ready states to Pull and wins over `AutoCompleteMiniGame`. FastAnimations now applies through reachable auto-cast/cast/pull animator paths and reports pending smoke evidence when no writable path is reachable. Validation: `git diff --check`, Release build/test, title smoke `GAME-SMOKE/20260612-204350`, MoreSaves third-save smoke `GAME-SMOKE/20260612-203558`, AutoFishing scenario smokes `GAME-SMOKE/20260612-204934`/`205040`/`205147`/`205248`/`210544`/`210646`/`210744`, and final Steam HookProbe `GAME-SMOKE/20260612-210900` passed; tracked by `TITLE-SAVESLOTS-AUTOFISHING-FIX-20260612` and update `docs/updates/2026/20260612-0016-title-saveslots-autofishing-fix.md`.
- 2026-06-13 installer/AutoFishing/UI diagnostics follow-up: user retest after uninstalling old SMAPI and installing new DTMAPI found the packaged title icon still missing, title/pause menus still switching to two-column layout, and AutoFishing `InstantBite` still not visibly biting after waiting. The package installer now resolves `assets/branding/dtmapi-icon.png` from Release output, the packaged payload, or source assets; `check-dtmapi-status.ps1` treats the installed title icon as required. AutoFishing wait automation now runs on `AgentStateFishingWait.OnEnter` with `OnPlay` fallback and calls native `InvokeFishOnHookTip` after `RollFish`, so visible bite feedback is native-owned. The homepage/pause menu issue is not claimed fixed: a diagnostics-only `UI.NativeLayoutDiagnostics` feature now logs official `HomePageUiState.RenderTextMenu`, `MenuUI.SetCapacity`, `MainMenuPanel.OnStartShow`, and relevant `DolocGridUI` layout calls with counts/stacks and no layout mutation. Rejected hypotheses: another single-column guard should be added before proving the owner; status-only AutoFishing proof is enough when the player sees no bite. Validation: `git diff --check`, Release build/test, package staging/install, status check for `[OK] DTMAPI title icon asset`, installed icon length `8018`, and no `DolocTown.exe` residual process; third-save visual/menu confirmation remains pending. Record: `docs/updates/2026/20260613-0001-installer-autofishing-ui-diagnostics.md`; regression row: `INSTALL-AUTOFISHING-UI-DIAGNOSTICS-20260613`.
- 2026-06-13 title/pause/AutoFishing follow-up: user retest of the installed asset-fixed build still reproduced title two-column, delayed pause icon two-column/vertical layout, and no observed `InstantBite`. Latest logs proved `HomePageTextMenu` could carry stale `layoutConstraintCount=2` even though `totalCapacity=5` and `lineCapacity=1`, because native `TextMenu.Render()` skipped layout reset when logical capacity values were unchanged. Native review proved `MainMenuPanel.OnStartShow()` owns pause-menu horizontal layout by resetting `MenuUI` to the visible icon count. The follow-up now repairs only those native-owner invariants: `HomePageTextMenu=1` after `HomePageUiState.RenderTextMenu`, and active `MainMenuUiState.panel.menu=visible icon count` every 500 ms. AutoFishing now writes `_fishOnHookDuration` from native `PullTiming` instead of DTMAPI's unconditional `100f`, while preserving `InstantBite` as timing-only and separate from auto-pull/minigame routing. Validation: `git diff --check` passed with line-ending warnings only; Release build passed with 0 warnings/0 errors; Release tests passed with `DTMAPI.UnitTests: OK`; package staging/install and status check passed with installed GameBridge DLL length `970752` and no `DolocTown.exe` residual process. Third-save visual confirmation remains pending. Record: `docs/updates/2026/20260613-0002-title-pause-autofishing-follow-up.md`; regression row: `TITLE-PAUSE-AUTOFISHING-FOLLOWUP-20260613`.
- 2026-06-13 title/pause/AutoFishing root fix follow-up: user clarified that title becomes two-column without pressing ESC, pause menu cycles between horizontal and two-column/vertical, and `InstantBite` should directly hook then enter the native minigame. Rejected hypothesis: active 500 ms repair is harmless; it visibly fought a later writer and caused the cycling. New facts: active `MainMenuUiState.Update` probing is diagnostic-only; concrete diagnostics now patch `HomePageTextMenu.ResetLayoutSize`, `MenuUI.ResetLayoutSize`, `MenuUI.SetCapacity`, `MainMenuPanel.OnStartShow`, and `GameDataPanel.SetCapacity` with call stacks, repairing only concrete owner events. AutoFishing now resolves a forced `InstantBite` bite-ready state to `ReelNativeBite`, which routes fish into `AgentStateFishingBattle` and non-fish/native skip paths to `AgentStateFishingPull`. Validation: `git diff --check` passed with line-ending warnings only; Release build passed with 0 warnings/0 errors; Release tests passed with `DTMAPI.UnitTests: OK`; package staging/install and status check passed with installed GameBridge DLL length `975360` and no `DolocTown.exe` residual process. Third-save visual confirmation remains pending. Record: `docs/updates/2026/20260613-0003-title-pause-autofishing-root-fix.md`; regression row: `TITLE-PAUSE-AUTOFISHING-ROOTFIX-20260613`.
- 2026-06-13 AutoFishing native-loop rewrite: user clarified the desired semantic contract is F6 default full automation (cast, native wait, auto reel, visible minigame auto-complete, collect, recast), with only three player options: `InstantBite` skips the wait, `SkipMiniGame` uses native result routing without forcing success, and `FastAnimations` touches only cast/pull. The old smoke fixture is rejected for current verification because it could create pools, write `FishingCache`, and overwrite `AgentStateFishingWait`; new AutoFishing phase smokes must use explicit fifth save / index 4 where the character starts at the pond with a selected rod. Early smoke `GAME-SMOKE/20260613-094928` correctly blocked when the fifth save selected `DolocTown.ItemTool`; after the fixture was updated to hold `carbon_fishrod`, Release tests and fifth-save smokes passed for `DefaultLoop` (`GAME-SMOKE/20260613-102412`), `InstantBite` (`20260613-103216`), `SkipMiniGame` (`20260613-103328`), `FastAnimations` (`20260613-103427`), `CombinedInstantSkip` (`20260613-103531`), and `CombinedInstantComplete` (`20260613-103621`). The final implementation also fixes the real post-bite no-reel defect by mirroring the native `AgentStateFishingWait.NextState` branch when no player input edge is present. Review: `docs/reviews/manual-qa/2026/20260613-0005-autofishing-native-loop-fifth-save-review.md`; update: `docs/updates/2026/20260613-0005-autofishing-native-loop-rewrite.md`; regression row: `AUTOFISHING-NATIVE-LOOP-FIFTH-SAVE-20260613`.
- 2026-06-13 AutoFishing smoke revalidation and title layout evidence: after the fifth save was confirmed to hold the fishing rod, the AutoFishing smoke gate was tightened to accept already-advanced native Cast/Wait/Battle/Pull phases and no longer fail when `FastAnimation` overwrites the latest application summary. Latest fifth-save smokes passed for `DefaultLoop` (`GAME-SMOKE/20260613-112900`), `InstantBite` (`20260613-113127`), `SkipMiniGame` (`20260613-113240`), `FastAnimations` (`20260613-113654`), `CombinedInstantSkip` (`20260613-113824`), and `CombinedInstantComplete` (`20260613-113937`) with clean process/fatal checks. Title homepage/settings smoke `GAME-SMOKE/20260613-114113` passed after owner-state layout repair. DirectExe HookProbe/exit smoke `GAME-SMOKE/20260613-114850` passed `HookProbe`, `SaveLoaded`, `ProcessExited`, `NoFatalInstanceWindow`, and `ForcedClose`. Pause-menu delayed cycling still requires user visual confirmation because the automated title smoke does not open the in-save pause menu. Update: `docs/updates/2026/20260613-0006-autofishing-smoke-revalidation-and-title-layout.md`; regression row remains `AUTOFISHING-NATIVE-LOOP-FIFTH-SAVE-20260613`.
- 2026-06-13 title/pause layout setter normalization: this is now a superseded intermediate workaround. The previous Update-based owner-state repair was not enough because logs showed `constraintCount=2` could be written after startup and then fought by a repair loop, matching the user's visible single/two-column cycling. The intermediate fix patched `UnityEngine.UI.GridLayoutGroup.set_constraintCount` and normalized only object-identity matches for the active `HomePageUiState.textMenu.slotLayoutGroup` and active `MainMenuUiState.panel.menu.slotLayoutGroup`; later root-cause work proved this was masking a SaveSlots hook scope bug. Historical evidence remains `GAME-SMOKE/20260613-122550`, `GAME-SMOKE/20260613-122313`, `GAME-SMOKE/20260613-122714`, `GAME-SMOKE/20260613-123521`, and `GAME-SMOKE/20260613-124240`. Superseded update: `docs/updates/2026/20260613-0008-title-pause-layout-setter-normalization.md`; current root fix: `docs/updates/2026/20260613-0013-saveslots-select-hook-layout-root-fix.md`.
- 2026-06-13 SaveSlots Select hook title/pause root fix: temporary setter-stack diagnostics proved the delayed homepage `constraintCount=2` write came from `SaveSlotsService.TryResetLayoutSize -> RestoreOfficialSavePanel -> ApplyOfficialSavePanelPaging -> EnsureOfficialSavePanelPageForSelection -> GameDataPanelSelectPrefix`, because the `GameDataPanel.Select` prefix resolved to inherited `DolocGridUI<T>.Select` and ran for `HomePageTextMenu` / pause `MenuUI`. The final fix stops installing that Select hook for fixed 12-slot MoreSaves, adds a `DolocTown.UI.GameDataPanel` guard before SaveSlots layout changes, removes global `GridLayoutGroup.constraintCount` setter normalization, and leaves UI layout diagnostics as diagnostic-only. Validation: Release build passed with `DTMAPI.UnitTests: OK`; `git diff --check` passed with line-ending warnings only; title smoke `GAME-SMOKE/20260613-143434` passed with `Save.MoreSlotsUiPaging=not-required` and no setter normalization; pause smoke `GAME-SMOKE/20260613-143621` observed 14 stable horizontal samples over 12 seconds; MoreSaves smoke `GAME-SMOKE/20260613-144038` revalidated `archiveFileCount=12`, `panelSlotCount=12`, `renderedSlots=12`, `visibleSlotsAfterPaging=12`, clean process exit, and no fatal popup. Update: `docs/updates/2026/20260613-0013-saveslots-select-hook-layout-root-fix.md`; regression row: `TITLE-PAUSE-AUTOFISHING-ROOTFIX-20260613`.
- 2026-06-13 AutoFishing minigame input and animation multiplier follow-up: user feedback proved the previous `AutoCompleteVisibleMiniGame` was still a delayed direct `FishingGameScrollBar.currentGameStatus=Success` path, not the requested green-hold/red-release/yellow-tap automation. Native review confirmed `FishingGameScrollBar.UpdateGame(float dt)` plus `DolocUserInput` getter reads own the minigame input/scoring path; user manual QA later confirmed red/green/yellow handling succeeds. The first animation fix still missed visible cast speed because `AgentStateFishingCast.OnEnter` animator speed is not the visible hook-flight owner: `FishRodRenderer.CastHook` pushes `FishRodHook.Velocity` and Rigidbody2D gravity. A later user follow-up identified the click/hold charge animation; native owner is `AgentStateFishingReady._castTimer` ticked from `AgentStateFishingReady.OnPlay`. The final fix keeps scoped native minigame input, adds `AnimationMultiplier` range `1..4` with old-config migration to default `3`, scales Ready charge timer via extra `_castTimer.Tick`, scales cast hook physics via `FishRodRenderer.CastHook`, scales `FishRodRenderer.Pull` duration, restores hook physics/animators on lifecycle exit, and tightens smoke so animator-only samples no longer pass visible animation speed. Validation: Release build/test passed; fifth-save `DefaultLoop` smoke `GAME-SMOKE/20260613-153632` passed with `AutoPlayVisibleMiniGame`, `stableHoldFrames=82`, `releaseFrames=49`; first `FastAnimations` attempt `GAME-SMOKE/20260613-153850` failed because missing old multiplier normalized to `1`; retained `GAME-SMOKE/20260613-154252` proved animator/Pull-duration paths but is incomplete for visible cast speed; hook-physics re-smoke `GAME-SMOKE/20260613-160354` passed with `FastCastHookPhysics`, `hook.Velocity:(18.55,29.68)->(55.65,89.04)`, `hook.gravityScale:12->108`, Pull duration `0.147->0.049`, report `dtmapi-report-20260613-160443.zip`, no leftover `DolocTown.exe`, and no fatal popup. Ready-charge unit coverage verifies `_castTimer.Tick(extraDt)` and progress-circle refresh; `GAME-SMOKE/20260613-163257` is retained as a smoke-harness false negative where Ready evidence existed but the old gate sampled a later Cast/Pull summary. Final fifth-save re-smoke `GAME-SMOKE/20260613-163812` passed `AutoFishingAnimationSpeed`, `AutoFishingPhase`, `AutoFishingMiniGameComplete`, clean process/fatal checks, report `dtmapi-report-20260613-163900.zip`, `FastReadyCharge` sample `castTimer.Progress:0->0.04;extraDt=0.04;powerBar.updated=2`, `FastCastHookPhysics` `hook.Velocity:(22.366,35.786)->(67.098,107.357)`, `hook.gravityScale:12->108`, Pull duration `0.117->0.039`, and final summary `readyCharge=True, fastAnimation=True`. Review: `docs/reviews/manual-qa/2026/20260613-0006-autofishing-minigame-animation-follow-up.md`; update: `docs/updates/2026/20260613-0014-autofishing-minigame-input-animation-multiplier.md`; regression row: `AUTOFISHING-MINIGAME-INPUT-ANIMATION-20260613`.
- 2026-06-13 AutoFishing cast charge setting follow-up: user requested a charge amount setting where the default is no charge, values can reach full charge, and `FastAnimations` accelerates the charge phase. Native review keeps ownership in `AgentStateFishingReady`: DTMAPI now holds/releases `DolocUserInput.NormalUseToolInProgress` until `_castTimer.Progress` reaches `FishingAutomationOptions.CastChargeRatio`, then lets `AgentStateFishingReady.NextState` enter Cast. `CastChargeRatio` is experimental, normalized to `0..1`, and exposed by AutoFishingMod with default `0`. Validation: `git diff --check`, Release tests, and fifth-save `FastAnimations` smoke `GAME-SMOKE/20260613-171405` passed with `AutoFishingCastCharge=Passed`, `target=0.5`, release at `progress=0.54`, Ready speed samples, Cast hook physics, Pull duration scaling, clean process/fatal checks, and report `dtmapi-report-20260613-171455.zip`. Retained evidence: `GAME-SMOKE/20260613-170514` is a false positive from PowerShell integer clamp truncating `0.5` to `0`; `GAME-SMOKE/20260613-171104` is the stricter-gate failure that exposed that clamp. Update: `docs/updates/2026/20260613-0016-autofishing-cast-charge-setting.md`; regression row: `AUTOFISHING-CAST-CHARGE-SETTING-20260613`.
- 2026-06-13 AutoFishing toggle-key rebind follow-up: user confirmed other AutoFishing behavior is manually OK and requested hotkey replacement. The defect was in AutoFishingMod ownership, not the native fishing state machine: `ToggleKey` existed but was not exposed as a config-menu keybind and the smoke/config path still assumed fixed F6. The fix adds a keybind option, preserves custom keys, migrates missing/blank keys to F6, keeps explicit `None` disabled, re-registers input after config save, and extends fifth-save smoke with `-AutoFishingToggleKey` plus key-specific input/log matching. Validation: PowerShell 5.1 parse for `run-game-smoke.ps1` passed; Release tests passed; local install passed; fifth-save `DefaultLoop` smoke `GAME-SMOKE/20260613-175055` passed with `Toggle=F7`, `Input F7 pressed`, `reason=hotkey F7`, `AutoFishingPhase=Passed`, `AutoFishingMiniGameComplete=Passed`, report `dtmapi-report-20260613-175205.zip`, no leftover `DolocTown.exe`, and no fatal popup. Update: `docs/updates/2026/20260613-0017-autofishing-toggle-key-rebind.md`; regression row: `AUTOFISHING-TOGGLE-KEY-REBIND-20260613`.
- 2026-06-13 AutoFishing InstantBite energy-cost follow-up: user reported fishing stamina/energy consumption was doubled from the native 10 to 20. Root cause: `AgentStateFishingWait.OnEnter` is called before native `AgentStateBase.Update` assigns the new Wait state as current, so AutoFishing's old InstantBite `OnEnter` overwrite could be clobbered by that native assignment and then replayed from `Wait.OnPlay`, consuming mirrored native fishing energy twice. The fix lets `Wait.OnEnter` prepare the bite only and defers reel/state advancement to `Wait.OnPlay`, where the native transition is complete. Validation: Release tests passed with new `FishingAutomationInstantBiteDefersReelUntilWaitPlay`; `git diff --check` passed with line-ending warnings only; local install and `check-dtmapi-status.ps1` passed required install checks. Game smoke/manual fifth-save energy delta remains pending. Update: `docs/updates/2026/20260613-0021-autofishing-instantbite-energy-cost.md`; regression row: `AUTOFISHING-INSTANTBITE-ENERGY-20260613`.
- 2026-06-13 FishRoe native title fallback: user reported fish roe tooltip text regressed from `鱼卵（鱼名称）` to only `鱼卵`. Root cause is that the shareable `FishBreedingAssistantMod/Generated/FishBreedingLookup.g.cs` is a public placeholder that returns no data, while old smokes masked that by registering a smoke-only fallback provider after the first miss. The fix keeps `IItemTooltipApi` Experimental, removes the smoke-only fallback provider, and makes GameBridge fallback from provider lookup misses to native `DolocAPI.QueryItemProto(fishId).Title` using `ItemFishRoe.fishName`. Validation: Release build/test, package staging/install, official local upload-source sync, and Steam third-save experimental hook smoke `GAME-SMOKE/20260613-201326` passed with `Fish roe native title lookup resolved fish -> 鱼`, `Smoke exercise FishRoeTooltip OK item=fish_roe title=鱼卵 (鱼) detail=`, clean process exit, and no fatal popup. Retained failed `GAME-SMOKE/20260613-200911` is a too-short `AutoExitAfterSeconds=20` harness timing failure, not a FishRoe failure. Update: `docs/updates/2026/20260613-0019-fishroe-native-title-fallback.md`; regression row: `FISHROE-NATIVE-TITLE-FALLBACK-20260613`.
- 0.2.4 manual QA regression ledger is open in `issues/ISSUE-005-20260603-manual-qa-024-regressions.md`. Treat it as newer than all 0.2.3 smoke evidence. Fresh 0.2.4 evidence now exists for Animal hidden-produce/custom color (`GAME-SMOKE/20260603-152851`, `GAME-SMOKE/20260603-160739`), ActionSpeed/AutoFishing runtime and title UI (`GAME-SMOKE/20260603-154207`, `GAME-SMOKE/20260603-154721`, `GAME-SMOKE/20260603-154816`, `GAME-SMOKE/20260603-160739`, `GAME-SMOKE/20260603-173435`), Y-console instant save/teleport CSV (`GAME-SMOKE/20260603-152451`), SecondMotor dual-visible/appearance/ride/original restore plus edge transition (`GAME-SMOKE/20260603-155505`, `GAME-SMOKE/20260603-202948`), Oil metadata/Y-console source/category plus coal-drop mining (`GAME-SMOKE/20260603-190919`, superseding `GAME-SMOKE/20260603-184455` for fuel/icon/sale/localization checks), Mine official JSON/workbench/2x-cover/player placement/placed-production (`GAME-SMOKE/20260603-210216`, superseding earlier Mine telemetry-only runs), and More Equipment Slots read-only player equipment strip plus equip/recover (`GAME-SMOKE/20260603-210216`, superseding earlier storage-only runs). Manual visual SecondMotor crossing and an equipment-strip screenshot remain useful user-acceptance polish, but the automated third-save requirements are smoke-proven.
- 0.2.5 critical manual QA ledger is smoke-verified in `issues/ISSUE-006-20260604-critical-manual-qa-025.md`. Treat it as newer than all 0.2.4 smoke evidence. Current 0.2.5 evidence: full enabled third-save smoke `GAME-SMOKE/20260604-111533` proves SecondMotor registration/key/dual visibility/appearance isolation/edge transition/original restore, Mine official Industrial tech route with `dtmapi_oilx10|steel_ingotx10`, `EquipmentFuncCase` storage `16/4`, 2x renderer scale, and production into Mine-owned storage; disabled SecondMotor smoke `GAME-SMOKE/20260604-111901` proves `Skip=2 Registration=0 Mail=0`; Y-console lifecycle smoke `GAME-SMOKE/20260604-112250` proves first open after `SaveLoaded` has `searchText=<empty>` while same-save reopens can preserve `石油`; all checks exited with no `DolocTown.exe`.
- 0.2.6 Mine/Y-console manual QA ledger is smoke-verified in `issues/ISSUE-007-20260605-mine-yconsole-026.md`. Treat it as newer than the 0.2.5 Mine/Y evidence. Focused third-save smoke `GAME-SMOKE/20260605-181224` records `AutoExerciseVehicle=False`, `AutoExerciseNewContentApis=False`, `AutoExerciseMineContentApis=True`, `NewContentMineApis=true`, `NewContentMineOfficialTechTreeUi=true`, `NewContentMineOfficialTechTreeUiScreenshotFile=true`, Mine tech `techPoint=1`, `unlockEntries=recipe-only`, `equipmentEntries=0`, `recipeEntries=1`, `costValues=SCIENCE:1`, official tech-tree UI screenshot evidence, Mine-only production `mode=mine-only`, `scaleContainment={containment=True, contamination=False, nonMineScaled=0}`, Y-console save-only instant save `reloadDisabled=True`, first open `searchText=<empty>`, hover tooltip evidence, compact one-row weather UI screenshot, `ProcessExited=true`, and no fatal popup.
- 0.2.7 manual QA root-cause ledger is smoke-verified in `issues/ISSUE-008-20260605-manual-qa-027-root-cause.md`. Treat it as newer than the 0.2.6 Mine/Y and 0.2.3 Animal/Y mouse evidence. Current evidence is split to avoid cross-smoke UI interference and the continuation did not execute motor automation: Y-console smoke `GAME-SMOKE/20260605-230442` records `AutoExerciseDebugConsoleMouseGive=True`, `AutoExerciseVehicle=False`, `UI.DebugConsoleInputIsolation=verified`, `AgentControllerState.EnterUICheck suppressed`, `Debug console right-click give source=mouse1-hit-test`, native give `requested=10 placed=10`, `DebugConsoleMouseGive=true`, and clean exit; Mine smoke `GAME-SMOKE/20260605-231924` records native pass-time `06:30 -> 18:00`, five catch-up `MachineProduction cycle OK` entries, `afterCycles=5`, `outputTarget=equipment-storage`, `storage=9/16`, `storageLineCapacity=4`, `AutoExerciseVehicle=False`, and clean exit; Animal smoke `GAME-SMOKE/20260605-232019` records `single-pass native moodBar`, `moodInfo=羊毛脂 0/100`, `clonedOverlay=False`, `AnimalViewerUi=true`, `AutoExerciseVehicle=False`, and clean exit. A normal-install directory check after smoke found no `DTMAPI.ConfigMenuExample`; `BepInEx/plugins` contains only the DTMAPI bootstrap directory.
- 0.2.8 readme implementation ledger is smoke-verified in `docs/updates/2026/20260606-0002-028-readme-implementation.md`. The prior `D:\steam\steamapps\common\Doloc Town\DTMAPI\smoke-settings.json` permission blocker was rechecked as writable and restored to disabled. All final 0.2.8 smokes used `DisableSecondMotorForSmoke=True`, did not execute `-AutoExerciseVehicle`, restored `Local.DTMAPI_SecondMotor.enabled=true`, and exited with no `DolocTown.exe`: new-content smoke `GAME-SMOKE/20260606-031316` records MoreEquipmentSlots native clone bind diagnostics with `interactive=3`, `hoverable=3`, `readOnly=false`, passive/hat equip and recover, Oil `crude_oil` metadata/drop, and Mine official JSON/electric production; Animal smoke `GAME-SMOKE/20260606-031522` records `independent cloned ProgressBar rows=1`, `moodOverride=False`, and `stateDescriptionOverride=False`; Y-console smoke `GAME-SMOKE/20260606-031919` records left/right mouse give, inventory/weather/teleport/time checks, and movement `levels=1x:12,2x:24,3x:36,4x:48`; Mine-only smoke `GAME-SMOKE/20260606-032127` records fallback `recipeInputs=metal_frameworkx15|engine_corex10|steel_ingotx20|coalx100`, `electricOnly=True`, `powerCost=10`, `outputRules=...|crude_oil:2:DTMAPI.OilMod`, and machine-owned storage production.
- 0.2.9 readme implementation ledger is smoke-verified in `docs/updates/2026/20260606-0004-029-readme-implementation.md`. The `D:\steam\steamapps\common\Doloc Town\DTMAPI\smoke-settings.json` access-denied blocker was rechecked after the permission change, read successfully, and restored to disabled. All final 0.2.9 smokes used `DisableSecondMotorForSmoke=True` and did not pass `-AutoExerciseVehicle`: Mine/MoreSaves smoke `GAME-SMOKE/20260606-051653` records official save UI expansion through `DolocAPI.gameManager.archiveFileCount -> GameDataUiState.Show -> GameDataPanel.Render` with `archiveFileCount=12`, `panelSlotCount=12`, `renderedSlots=12`, and Mine `electronicComponent=EComProtoAppliance/10` with native electric `Launch()` success plus low-power due retention; Animal smoke `GAME-SMOKE/20260606-051838` records `independent cloned ProgressBar prefilled rows=1`, `moodOverride=False`, and `stateDescriptionOverride=False`; Y-console smoke `GAME-SMOKE/20260606-051958` records the 0.2.9 UI screenshot, Y/Esc/Y open-close checks, left/right mouse give, inventory/weather/teleport/time/movement checks, and clean exit; new-content no-save smoke `GAME-SMOKE/20260606-052246` records equipment-slot sidecar `dirty` without `SaveSaved`/persist and the sidecar file stayed at its old timestamp; combined new-content + delayed instant-save smoke `GAME-SMOKE/20260606-053233` records sidecar `dirty`, native `SaveSaved`, `EquipmentSlots storage persisted`, and `Player.EquipmentSlotsSaveTransaction = verified`. All process checks say no `DolocTown.exe`.
- 0.3.0 Zoom slice is smoke-verified in `docs/updates/2026/20260606-0006-030-zoom-api-mod-slice.md`, but the broader 0.3.0 goal remains open. Release build/unit passed with 0 errors after the CameraZoom state-snapshot fix. Third-save DirectExe smoke `GAME-SMOKE/20260606-134042` records `AutoExerciseZoom=True`, `SaveLoaded=true`, `HookProbe SaveLoaded OK slot=2`, `Camera orthographic size 16.875->67.5 viewScale=4 reason=smoke max-view`, `Camera orthographic size 67.5->16.875 viewScale=1 reason=smoke restore-vanilla`, `Smoke exercise Zoom OK`, `Zoom=true`, `NoFatalInstanceWindow=true`, `ProcessExited=true`, and `ForcedClose=false`. Retained failed attempts: `GAME-SMOKE/20260606-132812` did not reach `SaveLoaded` because the smoke waited for `HomePageUiState`; `GAME-SMOKE/20260606-133448` proved third-save load plus 4x/restore camera writes but failed because `ICameraZoomApi.GetState` returned an internal mutable state reference before the snapshot fix.
- 0.3.1 regression/new-content round is smoke-verified in `docs/updates/2026/20260606-0007-031-regression-new-content-round.md`. Treat it as newer than the 0.2.3-0.3.0 manual-QA claims. Release build/unit passed with 0 errors; NU1900 warnings were restricted-network package vulnerability index warnings. Final evidence: title config smoke `GAME-SMOKE/20260606-150928` captures ActionSpeed, AutoFishing, Animal preset, and Animal Custom/`+` screenshots; Animal smoke `GAME-SMOKE/20260606-150721` records `independent cloned ProgressBar prefilled rows=1`, `moodOverride=False`, and `stateDescriptionOverride=False`; ActionSpeed smoke `GAME-SMOKE/20260606-150543` records config apply `2 -> 4`, tool animation, interaction, and auto-fill evidence; AutoFishing smoke `GAME-SMOKE/20260606-150631` records F6 through DTMAPI input, movement cancel, native auto-cast, fishing phase, and `AutoFishingMiniGameComplete=true`; Y-console smoke `GAME-SMOKE/20260606-150210` records instant save, teleport CSV rows=80, teleport, inventory/weather/time/movement, and in-game hotkey smoke `openCount=8 escapeCloseCount=1 yCloseCount=6 shortTaps=10 holdNoFlicker=True`; SecondMotor smoke `GAME-SMOKE/20260606-150357` records dual-visible original/second motors, instance-scoped appearance, key summon, ride/dismount, edge transition with `originalAtNewEntry=False` and `noStuck=True`, then restores `Local.DTMAPI_SecondMotor.enabled=false`; new-content smoke `GAME-SMOKE/20260606-150834` records Oil `crude_oil` metadata/drop, Mine hybrid official JSON (`fuelCapacity=7200`, `fuelOnlyCost=120`, `electricFuelCost=20`, `powerCost=10`) and production `mode=electric fuelCost=20 fuelRemaining=7180/7200`, plus MoreEquipmentSlots interactive/attribute-only equip and recovery. All final process checks say no `DolocTown.exe`.
- 0.3.0 advanced Y-console partial evidence is retained in `docs/updates/2026/20260606-0008-030-advanced-yconsole-partial.md` on the current 0.3.1 worktree. DirectExe third-save smoke `GAME-SMOKE/20260606-155802` verified +1 day, 4x/reset time scale, money `210726->210727`, tech `ANIMAL:11->12`, tech-tree unlock `affected=3`, crop maturity `169/171`, and resource spawn `alfalfa count=1`, while recording the then-open creative/generator/monster blockers.
- 0.3.0 Chest Locator Enhancer slice is smoke-verified in `docs/updates/2026/20260606-0009-030-chest-locator-enhancer.md` on the current 0.3.1 worktree. Release build/unit passed with 0 errors; DirectExe third-save smoke `GAME-SMOKE/20260606-163226` records `ChestLocatorEnhancer=true`, `SaveLoaded=true`, `ProcessExited=true`, `NoFatalInstanceWindow=true`, and no forced close. Logs show `ChestLocatorEnhancer API register success=True`, `Inventory.ChestLocatorEnhancer = verified`, `base=1 appended=5 sharedCases=5`, and `Smoke exercise ChestLocatorEnhancer OK item=dtmapi_mine, baseline=0, afterPlace=3, afterCost=1`. Process/fatal checks say no `DolocTown.exe` and no fatal popup.
- 0.3.0 Strong Planting Gun slice is smoke-verified in `docs/updates/2026/20260606-0010-030-strong-planting-gun.md` on the current 0.3.1 worktree. Release build/unit passed with 0 errors; DirectExe third-save smoke `GAME-SMOKE/20260606-170334` records `StrongPlantingGun=true`, `SaveLoaded=true`, `ProcessExited=true`, `NoFatalInstanceWindow=true`, and no forced close. Logs show `Farming.StrongPlantingGun = experimental`, three-slot official farming gun expansion, seed/film/fertilizer actions `1/1/1`, `Farming.StrongPlantingGun = verified`, and `Smoke exercise StrongPlantingGun OK ... capacities=inventory:3/total:3/line:3 ... basinState=planted:True,protected:True,fertilized:True`. Process/fatal checks say no `DolocTown.exe` and no fatal popup.
- 0.3.0 advanced Y-console closure is smoke-verified in `docs/updates/2026/20260606-0011-030-advanced-yconsole-closure.md` on the current 0.3.1 worktree. Release build/unit passed with 0 errors; DirectExe third-save smoke `GAME-SMOKE/20260606-172855` records `AdvancedDebug=true`, `SaveLoaded=true`, `ProcessExited=true`, `NoFatalInstanceWindow=true`, and no forced close. Logs show `Debug.CreativeMode = verified` with `ignoreMaterialCost=True`, `skipMoneyVerifyInShop=True`, `ignoreSpiritCost=True`, `canAffordMoneyIntMax=True`, `costEnergyNoChange=True`, `noTimeHookInstalled=True`, and `generatorAvailable=True`; `Debug.CreativeGeneratorGive = verified`; `Debug.SpawnMonster = verified` through official `Command_GenerateMonster`; `Debug.SpawnResource = verified`; and `Smoke.AdvancedDebug = verified`. Process/fatal checks say no `DolocTown.exe` and no fatal popup.
- 0.4.0 stable custom entity API work is smoke-verified in `CUSTOM-ENTITY-040-STABLE-API` and `docs/updates/2026/20260606-0014-040-stable-custom-entity-apis.md`. The fixed target version is 0.4.0, not 0.4.1. Controlled version sources changed from 0.3.1 to 0.4.0 in `Directory.Build.props`, `DtmApiRuntime.ApiVersion`, and install-package DTMAPI dependency normalization. Stable public contracts now cover custom animals, monsters, attacks/projectiles/barrages, and drones; Core registry tests passed with invalid-ID, duplicate-ID, owner cleanup, save-boundary cleanup, snapshots/status, and `runtime-creation-blocked` request results. GameBridge reports custom entity adapter statuses as configured-blocked until native Animal/Monster/Bullet/Drone adapters are verified. DirectExe third-save smoke `GAME-SMOKE/20260606-191219` records `HookProbe SaveLoaded OK slot=2`, `CustomEntities.CoreRegistry=verified`, all four family statuses `configured-blocked`, `Smoke.CustomEntityApis=verified`, `CustomEntityApis=true`, `ProcessExited=true`, `NoFatalInstanceWindow=true`, and no leftover `DolocTown.exe`.
- 0.4.2 CameraZoom API rebuild is smoke-verified in `docs/updates/2026/20260607-0014-camerazoom-api-rebuild.md` and `ZOOM-042-API-REBUILD`. Controlled version sources moved from 0.4.0 to 0.4.2 after the archived 0.4.1 handoff was confirmed non-blocking. Release build/unit passed with 0 warnings and 0 errors. DirectExe third-save smoke `GAME-SMOKE/20260607-072228` records `HookProbe SaveLoaded OK slot=2`, `Camera.ZoomEnvironmentLifecycle = experimental`, `Camera.ZoomApi = verified`, `Camera orthographic size 16.875->67.5 viewScale=4`, `cameraController=refreshed=True, positioned=True`, `background=compensated-background=4`, `fog=compensated-depth-fog=4; compensated-building-depth-fog=4`, `scanner=refreshed`, `uiScale=unchanged`, `room=farm_type1-平地`, `Smoke.Zoom = verified`, `Zoom=true`, `ProcessExited=true`, `NoFatalInstanceWindow=true`, and no leftover `DolocTown.exe`. Screenshot evidence under `DTMAPI-evidence/ZOOM-042/20260607-072308` includes before/4x/reset captures; visual inspection of `zoom-4x.png` confirms the farm background fills the large 4x view instead of becoming a small framed rectangle. Retained failed attempts: `GAME-SMOKE/20260607-070702` found the `CameraController.SetPosition` Vector3/Vector2 mismatch; `GAME-SMOKE/20260607-071602` found async screenshot evidence that only wrote the reset capture.
- 0.4.2 CameraView lease rebuild supersedes the old CameraZoom completion claim for ordinary playable zoom. Release build/test passed with 0 warnings and 0 errors. DirectExe third-save smoke `GAME-SMOKE/20260608-150914` records `HookProbe SaveLoaded OK slot=2 isNewGame=False`, `Camera.ViewApi = contract`, `Camera.ZoomApi = obsolete-compatibility`, high-priority `DTMAPI.ZoomMod` 4x lease, fallback to low-priority `DTMAPI.CameraViewCompetingSmoke` 2x after high-priority release, reset to 1x after lower-priority release, `nativeRefresh=not-called-playable`, `uiScale=unchanged`, `Smoke.CameraPlayable = verified`, `Smoke.Zoom = verified`, `Zoom=true`, `ProcessExited=true`, `NoFatalInstanceWindow=true`, and no leftover `DolocTown.exe`. Screenshot/summary evidence exists under `DTMAPI-evidence/CAMERA-PLAYABLE/20260608-150952`. The previous `ZOOM-042-API-REBUILD` evidence remains historical/supporting only because manual QA showed its `CameraController.RefreshResolution`/`SetPosition`/scanner/background/fog path was not acceptable for live playable zoom.
- 0.4.2 CameraPlayable dynamic follow-up supersedes `GAME-SMOKE/20260608-150914` for playable movement proof. Release build/unit and `run-game-smoke.ps1` syntax check passed. DirectExe third-save smoke `GAME-SMOKE/20260608-222542` records `RunStatus=Passed`, `HookProbe SaveLoaded OK slot=2 isNewGame=False`, `Smoke.CameraPlayable = verified`, `Smoke.Zoom = verified`, `ProcessExited=Passed`, `NoFatalInstanceWindow=Passed`, and `ForcedClose=Passed`. Evidence under `DTMAPI-evidence/CAMERA-PLAYABLE/20260608-222622` includes `camera-playable-dynamic-telemetry.csv`, `summary.txt`, and start/mid/end screenshots for both 4x and 2x phases. The 4x pass ran 30.016s with 29 samples, `playerDistance=102.528`, `cameraDistance=67.705`, `orthographicSize=67.5-67.5`, active owner `DTMAPI.ZoomMod`, and room `farm_type1-平地`; the 2x fallback pass ran 30.005s with 29 samples, `orthographicSize=33.75-33.75`, and active owner `DTMAPI.CameraViewCompetingSmoke`. The smoke-only movement driver is recorded as `movement=smoke-agentposition-camera-setposition`; CameraView implementation still records `nativeRefresh=not-called-playable` and remains orthographic-size-only. Manual QA checklist record: `issues/ISSUE-009-20260608-camera-playable-dynamic-qa.md`.
- Historical 0.2.4 new content/API evidence: pre-0.2.8 Oil official JSON/Y-console metadata and coal-drop mining were smoke-proven in `GAME-SMOKE/20260603-190919`: `dtmapi_oil` loaded as native `ItemFunction`, `fuelEnergy=1500` was above base highest `pumpkin:1200`, source/category were visible via `IInventoryDebugApi`, sale/localization/icon metadata were present, and a rendered `coal_mine` hit through native `ToolCollider.HandleTools` placed `dtmapi_oil x1` through native backpack placement. Mine official JSON/player placement/API telemetry was smoke-proven in `GAME-SMOKE/20260603-210216`: `NewContentMineOfficialJson=true`, `generatedItemType=DolocTown.ItemEquipment`, `cover=8x6`, `baseWellCover=4x3`, `sceneAsset=sprite_equipment_well`, `recipeInputs=stonex80|iron_ingotx8|electric_wirex4|dtmapi_oilx5`, `recipeGroup=equipment_workbench includes=True`, placement evidence `playerItem=dtmapi_mine ItemEquipment`, screenshot `DTMAPI-evidence/NEWCONTENT-024/20260603-210256/mine-placed-dtmapi-mine.png`, and runtime production state `fuel=7180/7200`, `cycleTUs=24`, `electricPower=10`, and native backpack placement for `copper_ore x1`. EquipmentSlots historical player equipment strip evidence in `GAME-SMOKE/20260603-210216` shows read-only clones before the 0.2.8 interactive upgrade.
- Direct EXE smoke must launch `DolocTown.exe` with `SteamAppId=2285550` and `SteamGameId=2285550` in the child environment. Failed attempts `GAME-SMOKE/20260603-163034` and `GAME-SMOKE/20260603-163622` reached DTMAPI startup but never loaded slot 3 because a fatal `Another instance is already running` popup appeared. Passing DirectExe evidence `GAME-SMOKE/20260603-164202` records `DirectExeSteamEnv=SteamAppId=2285550;SteamGameId=2285550`.
- F-key input can reach migrated mods through Unity Input System, but the DTMAPI overlay still has an open rendering bug tracked in `issues/ISSUE-003-hotkey-openconfig-no-overlay.md`.
- External smoke key injection can miss the Doloc Town window or reuse a stale key-down state. For debug-console smoke, require foreground verification, short retries, and exact log-count checks. Retained attempts `GAME-SMOKE/20260601-054331`, `GAME-SMOKE/20260601-054917`, and `GAME-SMOKE/20260601-055310` show the difference between API success, focus/key-state issues, and a too-strict `reason=hotkey T` matcher; 0.2.0 evidence `GAME-SMOKE/20260601-060427` verified T/Escape/T/T. The 0.2.1 console moved to Y because T conflicts with game input; final Y evidence is `GAME-SMOKE/20260601-140915`, with `Y/Escape/Y/Y`, inventory/weather/teleport/time/movement, third-save load, and clean exit.
- Y console input reliability now depends on DTMAPI's cached key-down edge, not only frame-local `GetKeyDown`. The stronger 10-short-tap/hold evidence is `GAME-SMOKE/20260602-022417`, which also captures native icons and hover-name evidence under `DTMAPI-evidence/DEBUG-CONSOLE-UI/20260602-022500`; later aggregate smokes such as `GAME-SMOKE/20260602-020710` keep `DebugConsoleTenYShortTaps=true` and `DebugConsoleHoldYNoFlicker=true`.
- 0.2.2 Y console hotkey and Workshop-mod UI evidence is `GAME-SMOKE/20260602-122848`: Steam launch, third-save load, Y/Escape/Y/Y, ten short Y taps, long-Y no-flicker, screenshot capture, no fatal popup, and no leftover `DolocTown.exe`. The copied screenshot `DTMAPI-evidence/DEBUG-CONSOLE-UI/20260602-122951/debug-console.png` shows `modItemsOnly=True`, search text `黄油`, native mod icons, and hover/source text for `mod_butter` from `Workshop.3722791728`.
- 0.2.3 partial code pass `20260603-0003` changes Y-console item cells so hover no longer dirties/rebuilds the whole panel before click dispatch, left click uses a Unity `Button` give-1 path, and right click has a `Mouse1` fallback against the recently hovered/clicked item when reflected Unity `EventTrigger` does not deliver a right-button click. `20260603-0004` extends the UI with source/category columns, nearby hover detail tooltip, and detailed teleport names, with third-save smoke evidence below.
- Failed partial smoke `GAME-SMOKE/20260603-030142` reached third-save `SaveLoaded`, verified `AutoFishingPhase`, `DebugInventory`, and clean exit/no fatal popup, but external Y injection failed to open the console (`DebugConsoleOpenY1=false` through attempt 4). Treat it as AutoFishing/Inventory partial evidence only, not Y-console UI or real-click proof.
- Passing 0.2.3 Y-console productization smoke `GAME-SMOKE/20260603-032406` reached the third save and verified `DebugConsoleOpenY1=true`, `DebugConsoleCloseEscape=true`, `DebugConsoleOpenY2=true`, `DebugConsoleCloseY=true`, `DebugConsoleTenYShortTaps=true`, `DebugConsoleHoldYNoFlicker=true`, `DebugInventory=true`, `DebugTeleport=true`, `ProcessExited=true`, and no fatal popup. Screenshot `DTMAPI-evidence/DEBUG-CONSOLE-UI/20260603-032451/debug-console.png` shows the larger source/category layout, nearby item detail tooltip, and non-generic teleport labels such as `上游丘陵1-下端`; logs show `wood` and `dtmapi_second_motor_key` native backpack gives with before/after counts and teleport from farm container room to `city_郊区-上游丘陵1`.
- Passing 0.2.3 real mouse item-give smoke `GAME-SMOKE/20260603-035653` reached the third save and verified `DebugConsoleMouseGive=true`, with OS-level left-click on the first item cell logging `owner=DTMAPI.DebugConsoleMod item=dtmapi_second_motor_key requested=1 placed=1 before=0 after=1 success=True`, and OS-level right-click logging `Debug console right-click give source=mouse1-fallback item=dtmapi_second_motor_key` followed by `requested=10 placed=10 before=1 after=11 success=True`; process check says no `DolocTown.exe` and no fatal popup.
- Passing 0.2.3 AnimalHusbandryProgress visual/config smoke is split across `GAME-SMOKE/20260603-040354` and title smoke `GAME-SMOKE/20260603-052444`: the third-save AnimalPanel run verified `AnimalViewerUi=true`, logged native-like UI overlay `rows=1, 羊毛脂 0/100`, captured a delayed screenshot under `DTMAPI-evidence/ANIMAL-001/20260603-040437/animal-viewer-ui-delayed.png`, and exited cleanly; the title settings run captured `DTMAPI-evidence/UI-004/20260603-052523/title-settings-config-animal-husbandry-progress.png`, which was visually checked for direct swatches, no right-side `Orange` value text, no non-Custom `FF942E` input, and a far-right custom `+` swatch.
- Passing 0.2.3 ActionSpeed strong auto-fill smoke `GAME-SMOKE/20260603-041950` reached the third save and verified `ActionSpeedInteraction=true`; no-key in-water auto-fill invoked native `ItemBottle.UseAsItem` with `strong=True`, `cooldownSeconds=0.08`, `normalCooldownSeconds=0.1`, `strongCooldownSeconds=0.08`, `inventoryChanged=True`, and clean exit. Title settings smoke `GAME-SMOKE/20260603-052444` captured `DTMAPI-evidence/UI-004/20260603-052523/title-settings-config-action-speed.png`, visually checked for same-row `自动装水` plus `强化自动装水`.
- Passing 0.2.3 AutoFishing movement/skip smoke `GAME-SMOKE/20260603-042437` reached the third save and verified `AutoFishingInputLog=true`, `AutoFishingMovementCancel=true`, `AutoFishingPhase=true`, and `AutoFishingMiniGameSkip=true`; logs show external F6 enabled automation, W disabled it with `manual-move W`, F6 re-enabled it, native `BodyController.UseFishRod` auto-cast ran, Pull animation speed applied at multiplier 3, and `InstantBite` skipped to `AgentStateFishingPull` with `autoCompleteMiniGame=True` and `skipMiniGame=True`. Title settings smoke `GAME-SMOKE/20260603-052444` captured `title-settings-config-auto-fishing.png`, visually checked for same-row `自动完成小游戏` plus `跳过小游戏`.
- Passing 0.2.4 AutoFishing skip=false minigame smoke `GAME-SMOKE/20260603-173435` reached the third save and verified `AutoFishingMiniGameComplete=true`; the retained failed attempt `GAME-SMOKE/20260603-172124` proved that a rolled garbage item (`waste_plastic_bottle`, `FishProto.IsFish=false`) does not create the native minigame. The passing run logs `FishingGameScrollBar`, `autoHook=AgentStateFishingBattle`, `fish=loach`, `isFish=True`, `forceFishForSmoke=True`, `currentGameStatus=Success`, `visibleSeconds=0.76`, clean exit, and no fatal popup.
- 0.2.2 Workshop item give evidence is `GAME-SMOKE/20260602-122848`. Subscribed `Workshop.3722791728` (`多洛可战前料理 | 奶油 & 黄油系列`) was temporarily enabled from `SAVE/mod_infos.json`, backed up/restored under `WORKSHOP-MODINFO-BACKUP/20260602-122846`, and the final restored entry is `enabled=False priority=-1`. The smoke log gave `mod_butter` (`黄油`) from `Workshop.3722791728` with `runtimeLoaded=True`, `before=0`, `after=1`, `given=1`, and `workshopRuntimeItem=verified`. Earlier mineral-seed evidence remains under `GAME-SMOKE/20260602-115735` / `WORKSHOP-MODINFO-BACKUP/20260602-115707`.
- 0.2.2 second-motor evidence is `GAME-SMOKE/20260602-122848`: `DTMAPI.SecondMotorMod` registered `dtmapi.second_motor`, the official-local key item was given, generated native `ItemMotorKey.OnUse` summoned the DTMAPI clone on `farm_type1-平地`, ride-on/ride-off events fired, original motor was restored/summoned, speed multiplier `2` produced base/effective max speed `25/50`, and endurance progress stayed `1` across summon/ride/dismount. A retained failed run `GAME-SMOKE/20260602-112141` is the disabled-location negative proof: the key hook intercepted `dtmapi_second_motor_key` in an in-house/disabled room and failed with `reason=in-house`.
- 0.2.3 SecondMotor current evidence is `GAME-SMOKE/20260603-051204` plus title UI smoke `GAME-SMOKE/20260603-052444`: the official-local package is `DTMAPI 异色飞行摩托`, the item title is `飞行摩托钥匙（异色）`, `IMailDeliveryApi` called native `DolocAPI.SendItemAsEmail` with `sent=True`, `pendingMail=1`, the smoke first generated an original `motor_key` and verified the native `ItemMotorKey.OnUse` path, then generated `dtmapi_second_motor_key` for the DTMAPI route. The dual-visible probe now logs `originalVisible=True originalRoom=farm_type1-平地 secondVisible=True secondRoom=farm_type1-平地 dualVisible=True`, followed by second-motor ride-on/ride-off, original motor snapshot restore, original summon OK, 2x speed, unchanged endurance, clean exit, and no fatal popup. Title screenshot `DTMAPI-evidence/UI-004/20260603-052523/title-settings-config-second-motor.png` shows the DTMAPI status page. Historical run `GAME-SMOKE/20260603-050208` retained the earlier `dualVisible=False` blocker and should be cited only as the pre-fix failure.
- 0.2.3 SecondMotor appearance-fix evidence is `GAME-SMOKE/20260603-082749`: installer output says global official vehicle example sprite assets were skipped and stale `Content/DTMAPI/official-vehicle-example` assets removed; the installed `DTMAPI_SecondMotor` package has no `sprite_vehicle_motor*` files under `Content/DTMAPI`; GameBridge logs `Vehicle.SecondMotorAppearance = experimental`, `appearanceIsolated=True`, `originalScopedTint=0/10`, `secondScopedTint=5/10`, and `appearance=instance-scoped-tint hex=#8CE6FF renderers=5/10 skippedDriver=5`, followed by dual-visible, ride-on/off, original summon OK, `VehicleSecondMotor=true`, clean exit, and no leftover `DolocTown.exe`.
- 0.2.4 SecondMotor edge-transition evidence is `GAME-SMOKE/20260603-202948`: GameBridge patches `DolocAPI.EnterRoom`, logs `Vehicle.SecondMotorMapTransition` target capture/apply for `city_郊区-上游丘陵1`, and the final edge probe records `changedRoom=True`, `secondStillRiding=True`, `secondInCurrentRoom=True`, `distanceToDestination=0.839`, `nearDestination=True`, `originalRoomAfterTransition=farm_type1-平地`, `originalVisibleAfterTransition=False`, `originalAtNewEntry=False`, and `noStuck=True`. Result has `VehicleSecondMotor=true`, `ProcessExited=true`, no fatal popup, and no leftover `DolocTown.exe`. Retained failed attempts before the fix prove the rejected paths: recovery-settle timing, original singleton at the new entry, and stale rider position through native `DolocAPI.AgentPosition`.
- DirectExe vehicle smokes `GAME-SMOKE/20260602-105301` and `GAME-SMOKE/20260602-111609` hit `Fatal error: Another instance is already running` after the game/Steam spawned a second process. Use Steam launch for normal game smoke; keep DirectExe only for explicit harness regression checks.
- The in-save debug console is intentionally a reflected Unity Canvas host plus ordinary DTMAPI mod hotkey, not the old F8/F10 diagnostics overlay. It should stay closed on title screens and block gameplay hotkeys while open.
- The time debug button must advance weather-period boundaries, not 08:00. Current evidence is `GAME-SMOKE/20260602-021349`: `06:10 -> 18:00`, `18:00 -> next-day 00:00`, and `00:00 -> 06:00`, all through `ArchiveDataHandle.PassTimeNoControl`.
- Direct EXE smoke can reach a different failure from Steam launch: `GAME-SMOKE/20260601-131105` loaded the 0.2.1 Y console package but exited before save load with `InvalidOperationException: Steamworks is not initialized` inside the official `ModManager.ReloadMods` path. Treat this as a DirectExe/Steamworks harness blocker; the passing 0.2.1 smokes use Steam launch.
- Do not combine `-AutoOpenAnimalPanel` with ActionSpeed/OneAction gameplay exercise flags in one run unless the smoke harness explicitly closes the animal panel first. Retained interrupted evidence `GAME-SMOKE/20260601-133839` shows `Animal viewer UI evidence OK` followed by repeated `Waiting for NormalGameState before action-speed interaction smoke`; the clean split runs are `GAME-SMOKE/20260601-135144` for animal UI and `GAME-SMOKE/20260601-135239` for ActionSpeed/OneAction.
- Animal hidden-produce rendering must enumerate configured husbandry definitions and fill missing progress as `0`; the current 0.2.3 visual proof is `GAME-SMOKE/20260603-040354/DTMAPI-evidence/ANIMAL-001/20260603-040437/animal-viewer-ui-delayed.png`, showing the native-style `羊毛脂 0/100` row without the old `隐藏产物` label. Earlier `20260602-001819` and `20260602-002157` animal attempts showed stale duplicated text and should not be cited as final visual proof.
- Decal equipment such as `resin_collector` must be created or selected through the game's decal host/slot path; ordinary `CreateEquipment(..., null, -1)` is expected to fail with `建造贴纸设备需要传入decalHost和decalSlot参数`.
- ActionSpeed harvest classification cannot rely only on static/current equipment scanner state. Wild vegetation harvest is reached through `InteractableManagerEx` and a `VegetationRenderer` wrapper, and stale scanner selections must be cleared before collecting water/vegetation evidence.
- ActionSpeed ordinary food/drink is verified as animation speed only; right-click continuous use is intentionally restricted to bottled water. Current gameplay evidence is `GAME-SMOKE/20260603-041950`, which verifies `BottledWaterRightClick`, no-key `AutoFillBottle` through native item paths, and the 0.2.3 strong auto-fill cooldown difference (`0.08` vs ordinary `0.1` in the Debug smoke profile).
- AutoFishing 0.2.3 player toast policy limits visible feedback to F6 on/off and movement cancel; no-water/no-rod/auto-cast states are log/HookStatus evidence. Current third-save smoke `GAME-SMOKE/20260603-042437` verifies external F6, movement cancel through W, native auto-cast, Pull animation speed, wait-phase InstantBite, and skip-minigame routing to `AgentStateFishingPull`. Current 0.2.4 smoke `GAME-SMOKE/20260603-173435` verifies the separate skip=false minigame auto-complete route through `FishingGameScrollBar.UpdateGame`. Earlier `GAME-SMOKE/20260603-030142` remains a partial proof for suppressed no-water/no-rod/cast toast policy.
- OneAction vegetation/dandelion must not be treated as a `DungeonResourceRenderer` resource. It uses `VegetationRenderer.OnFell -> Vegetation.CheckToolConstraints`, so DTMAPI records it as a native exception path and verifies wrong/correct tools without applying one-action damage.
- Hook regressions after Doloc Town build updates.

## Rule

Before changing runtime lifecycle, shutdown, BepInEx, Harmony patches, event dispatch, input, config menu, Workshop loading, or mod loading, check this folder and update it after the attempt.
