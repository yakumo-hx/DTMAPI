# DTMAPI Public API Stability Matrix

Status date: 2026-06-16

This matrix is the public API stability contract. It is intentionally stricter than the smoke-evidence ledger: a smoke run can prove that a code path executed, but it does not by itself make an API stable for ordinary mods.

## Status Vocabulary

| Status | Meaning | Compatibility Promise |
| --- | --- | --- |
| Stable | Public contract is DTMAPI-owned or has proven native owner coverage. Breaking changes require migration notes, compatibility handling, and a new update record. | Ordinary mods may depend on it. |
| StableCandidate | Contract shape is likely correct, but it still lacks one or more promotion gates such as two real mods, manual QA, or regression matrix coverage. | Ordinary mods may experiment, but should expect final polish changes. |
| Experimental | API is usable for test mods or carefully scoped real mods, but native ownership, lifecycle, conflict behavior, or failure modes are not fully proven. | Ordinary mods should treat it as version-sensitive. |
| Diagnostic | Debug, smoke, report, console, or developer-support surface. It may call powerful native paths, but it is not a stable gameplay API. | Ordinary gameplay mods should not depend on it. |
| Proposed | Design direction or desired API surface with no current stable implementation guarantee. | Do not depend on it. |
| Failed | Existing implementation or evidence is known insufficient or contradicted by manual QA. | Do not use as completion evidence or stable API proof. |
| Retired | Public surface or hook route existed in an experimental branch but was intentionally removed from active runtime/API code. | Do not depend on it; treat old evidence as archive/research only. |

`Blocked` is a qualifier, not a status. A row may be `Experimental` and explicitly blocked for native runtime creation until its GameBridge adapter is proven.

## Stable Promotion Criteria

An API may move to `Stable` only after all relevant gates are satisfied:

1. It is backed by a clear DTMAPI owner or a reviewed Doloc Town native responsibility owner.
2. It exposes stable DTMAPI contracts and does not expose raw Unity, Harmony, BepInEx, or copied decompiled game types.
3. At least two real mods, not only a smoke helper, use the API successfully.
4. Game evidence proves the API in the appropriate real gameplay path, usually the third local save unless a task states otherwise.
5. Visual, UI, input, camera, save/load, lifecycle, machine, vehicle, or other player-visible APIs have manual QA or screenshot/video evidence that covers the actual user-facing failure mode.
6. The debug regression matrix has a row for the API's main success path and high-risk failure path.
7. Clean exit evidence exists when the API participates in runtime/gameplay behavior: no leftover `DolocTown.exe` and no Steam waiting-for-exit regression.
8. Known failures, rejected hypotheses, and rollback notes are recorded in the relevant debug, review, hook-map, update, or API contract document.

Static screenshot smoke is allowed as supporting visual evidence, but it is not completion evidence for a visual/UI/camera API unless manual QA or an equivalent targeted visual review covers the same failure mode.

## Native Owner Domain Library Boundary

Future API domains that are not yet represented by a public matrix row should first be checked against `docs/reviews/api/native-owner-domains/INDEX.md`. That library records native owner candidates, official Workshop content/beauty boundaries, and blocked/gap status for broad gameplay/content requests. It is not a stability contract: a report may justify a proposed DTO or GameBridge adapter, but it does not promote an API to `Stable` or `StableCandidate` without the promotion criteria above.

## Internal Product UI Boundary

The title-page DTMAPI Manager UI is an internal product UI over existing Diagnostic and Experimental runtime data. `DtmManagerViewModel`, Manager row formatters, Manager report/export state, and Status Copy Summary are not `DTMAPI.Abstractions` public mod APIs. Their presence in the first-party Bootstrap assembly does not promote `IDtmDiagnosticsApi`, `IUiHelper`, CameraView, FishingAutomation, SaveSlots, or any GameBridge feature API to `Stable` or `StableCandidate`.

Manager UI smoke evidence may prove that support rows, summary text, and report export are visible in the title settings UI. It must not be cited by itself as native-owner proof for gameplay, save/load, camera, fishing, vehicle, equipment, machine, or content APIs.

## Stability Matrix

| Area | API / Contract | Status | Evidence Scope And Current Boundary |
| --- | --- | --- | --- |
| Framework | `DtmMod`, `IManifest`, `IManifestDependency`, `IDtmHelper` | Stable | DTMAPI-owned mod entry and helper container contracts. `IDtmHelper` remains Stable because the container shape is stable, but each child helper surface still follows its own `DtmApiStatus`; helper access does not promote Experimental/Diagnostic child APIs to Stable. No Doloc native owner is implied. Manifest/dependency hardening is tracked separately by Core runtime tests. Current version baseline is `0.5.1-alpha`; unit tests verify `MinimumDTMApiVersion=0.5.1-alpha` and legacy `0.4.2`/`0.3.1` requirements load while future `0.5.2-alpha` requirements are blocked. Runtime smoke `GAME-SMOKE/20260610-121420` and `GAME-SMOKE/20260610-121631` remain retained alpha dependency-normalization evidence for the policy; the 2026-06-12 bottom-layer implementation updates BepInEx numeric bootstrap metadata to `0.5.1.0` and records fresh Release build/test plus DirectExe/Steam third-save validation in update `20260612-0012`. |
| Framework | `MinimumDTMApiVersion` compatibility policy | Stable | Current policy review `docs/reviews/api/2026/20260610-dtmapi-version-compatibility-policy.md` keeps DTMAPI API compatibility numeric-only: prerelease/build suffixes are trimmed before `System.Version` comparison, so `0.5.1-alpha` is the current dev baseline label for the numeric `0.5.1` surface rather than a strict SemVer prerelease blocker. BepInEx plugin metadata remains numeric through `DtmApiRuntime.BinaryVersion = 0.5.1.0`; strict SemVer or an `AllowPrerelease` manifest property is deferred to a future compatibility design. |
| Framework | `IManifest.EntryType` | StableCandidate | Public read-only exposure of the already-parsed manifest `EntryType` value. Verified by unit test through the `IManifest` interface on 2026-06-09; runtime entry-type selection already uses the same field internally. This is a small public-surface addition and should stay StableCandidate until the next API review package confirms compatibility. |
| Framework | `IMonitor.Log`, `LogOnce`, `LogException` | Stable | DTMAPI-owned logging contract used across runtime and mods. |
| Framework | `IConfigHelper.ReadConfig<T>`, `WriteConfig<T>`, `GetConfigPath` | Stable | DTMAPI-owned config file contract. Stability depends on preserving bad-JSON recovery and atomic write behavior introduced by Core runtime hardening. |
| Framework | `IConfigHelper.RegisterMigration<T>` | StableCandidate | Contract exists, but needs more real migration cases before stable promotion. |
| Framework | `IModRegistry.IsLoaded`, `Get`, `GetAll`, `GetApi<T>`, helper-bound `RegisterApi<T>` | StableCandidate | DTMAPI-owned registry. `RegisterApi<T>` must remain helper owner-bound so ordinary mods cannot spoof another manifest owner. Promote after the owner-bound contract is released and used by two real mods. |
| Framework | `ITranslationHelper.Language`, `Get` | StableCandidate | Used by migrated mods for Chinese/English config text. Needs broader locale/manual QA before `Stable`. |
| Events | `IGameLoopEvents.GameLaunched` | StableCandidate | DTMAPI Core dispatch after mod entry. Promotion needs regression matrix coverage for load order and failure isolation. |
| Events | `IGameLoopEvents.UpdateTicked`, `OneSecondUpdateTicked` | Experimental | High-frequency events are fragile. Core now has consecutive-failure circuit breakers, but timing, UI blocking, and fallback tick behavior still require regression rows before stable promotion. |
| Events | `IGameLoopEvents.ReturnedToTitle` | Experimental | Lifecycle event exists, but title/save boundary semantics must stay tied to game evidence and clean-exit checks. |
| Events | `ISaveEvents.SaveLoaded`, `SaveSaving`, `SaveSaved` | Experimental | Reaches important native save/load paths, but save transaction semantics, sidecar writes, failed saves, and reload boundaries remain high-risk. |
| Input | `IInputEvents.ButtonPressed`, `ButtonReleased`; `IInputHelper.RegisterButton`, `IsDown`, `WasPressed`, `Suppress` | Experimental | Works for migrated hotkeys. `IInputHelper.Suppress` is currently one-frame DTMAPI helper state cleared by `Input.ClearFrame` with `WasPressed`; it is not consumed by Doloc Town native input hooks and does not provide full native input isolation. Do not mark stable until native/UI conflict policy is proven. |
| Config UI | `IDtmConfigMenuApi` registration/query APIs (`Register`, `Add*`, `SetDisplayName`, `GetKeybindConflicts`), `DtmColorPreset`; page/item state models used by DTMAPI runtime UI | StableCandidate | Ordinary mods may register config controls and query keybind conflicts only. Page enumeration, page lookup, begin/save/reset/cancel, pending preview, and official-enable lock control are internal `IConfigMenuRuntime` responsibilities for DTMAPI Core/UI, not ordinary mod API. The 2026-06-12 title settings UI implementation adds internal deterministic paging for the enabled-mod list and per-page item list, plus Manager Mods/Errors/Warnings/Hooks/Features paging; follow-up evidence `GAME-SMOKE/20260612-204350` restores the title entry to an internal compact icon button. The attempted title-UI-owned `HomePageTextMenu` single-column guard was removed in `20260612-0017` after manual feedback that it affected pause-menu horizontal icons. The 2026-06-13 `UI.NativeLayoutDiagnostics` follow-ups are internal GameBridge repairs/diagnostics only: the latest `20260613-0008` fix normalizes `GridLayoutGroup.constraintCount` writes only for the active official homepage text menu and active pause `MenuUI`, with title evidence `GAME-SMOKE/20260613-122550` and pause evidence `GAME-SMOKE/20260613-122313`. This exposes no new public edit APIs and does not change the `IDtmConfigMenuApi` contract. Stable promotion still needs real-mod manual UI QA, config save/cancel/reset evidence, scrolling/visibility/conflict regression rows, and official enablement lock checks. |
| UI | `IUiHelper.OpenDtmApiStatusPage`, `OpenModListPage`, `OpenConfigPage`, `OpenErrorPage`, `OpenHookStatusPage` | Experimental | Player-facing DTMAPI UI helpers. Manual QA is required for layout, input blocking, and stale-state/flicker cases before promotion. |
| Diagnostics | `IDiagnosticsHelper.GetErrors`, `ExportLogs`, `GetLatestLogPath`, `IDiagnosticsEvents.LogExported` | StableCandidate | DTMAPI-owned reporting/log-export surface. `GetLatestLogPath()` remains the current-run `DTMAPI/logs/latest.log`; since 2026-06-16 runtime startup rotates a non-empty previous latest log to retained `latest-*.log` history, and `ExportLogs()` includes the 10 newest retained history files under `DTMAPI-history/`. This is a support/export behavior clarification, not a new public API member. Promote after report contents and failure paths are covered in regression matrix. |
| Diagnostics | `IDiagnosticsHelper.GetHookStatuses`, `GetWarnings`, `RecordEvidence`, `IDtmWarningInfo`, `IDiagnosticsEvents.HookStatusChanged` | Diagnostic | Useful for status pages, smoke harness, and evidence capture. Warning records are structured diagnostics for non-blocking problems such as unverifiable `MinimumGameVersion` and optional dependency version mismatches; report summaries and runtime snapshots include them. `GetErrors()` and `GetWarnings()` expose the current retained diagnostics window, currently capped internally at 1000 entries per kind; exported report summaries note internal trimming when it occurs. Hook status text is not native gameplay proof and must not be cited as completion evidence by itself. |
| Diagnostics | `IDtmDiagnosticsApi.GetSnapshot`, `IDtmDiagnosticsSnapshot`, `IDtmLoadedModInfo`, `IDtmModStatusInfo`, `IDtmFeatureStatusInfo` | Diagnostic | Read-only runtime diagnostics snapshot registered under the `DTMAPI` runtime API owner. It exposes loaded mod rows, discovered/loaded/disabled/error mod status rows, structured `IDtmModStatusInfo.StatusCode`, retained errors, retained warnings, hook statuses, structured GameBridge feature statuses, latest log path, and latest report path without parsing logs or promoting gameplay APIs. `IDtmFeatureStatusInfo.FailureCount` remains a cumulative count; consecutive failure and recovery timestamp context is reported through the existing feature-status details string rather than new public snapshot members. Unit tests verify `loaded`, `disabled`, `missing-dependency`, `dependency-cycle`, `entry-dll-error`, `code-load-error`, `api-too-new`, and `unknown-error` status codes while preserving `Status` and `Reason` text; optional dependency version mismatches now remain `loaded` with warnings instead of becoming error rows. Latest diagnostics cap evidence `GAME-SMOKE/20260610-115014` verifies `Feature.Camera`, `Smoke.DiagnosticsSnapshot=verified`, `mods=14`, `modStatusCodes=loaded=14`, `errors=0`, `warnings=0`, `features=8`, and matching report `LatestReportPath`; `GAME-SMOKE/20260610-115225` verifies the same snapshot path for `Feature.ActionSpeed`. Unit coverage also verifies 1005 errors/warnings are capped to a 1000-entry retained window and exported reports include a `DiagnosticsTrimmed` summary. Latest feature failure recovery evidence `GAME-SMOKE/20260610-164830` and `GAME-SMOKE/20260610-165043` verifies Camera and ActionSpeed routes still pass after feature status details gained cumulative/consecutive/recovery semantics; earlier failure status throttle evidence `GAME-SMOKE/20260610-133933`, `GAME-SMOKE/20260610-134145`, `GAME-SMOKE/20260610-134259`, and `GAME-SMOKE/20260610-134406` verifies diagnostics feature snapshots stay current while `Feature.<Id>` hook-status publication is throttled. This remains Diagnostic, not Stable. |
| Diagnostics | Diagnostics report aggregate counters | Diagnostic | Internal/report-only repeated diagnostics aggregation. No new public API or snapshot member is exposed. Unit coverage records 1005 repeated errors and warnings, keeps `GetErrors()` / `GetWarnings()` capped to the latest 1000 entries, and verifies exported summaries include `DiagnosticsAggregates` rows with total counts and latest details. Latest smoke evidence `GAME-SMOKE/20260610-142415` and `GAME-SMOKE/20260610-142626` verifies Camera and ActionSpeed diagnostics snapshot paths still report loaded mods, hooks, features, warnings/errors, and matching `LatestReportPath` values under the unchanged public surface. |
| Audio | `IAudioReplacementApi`, `AudioReplacementOptions`, `AudioReplacementState` | Experimental | Event-level Wwise replacement surface registered by GameBridge for carefully scoped mods. Current native-owner review targets wild paper boxes only: `DungeonResourceModelPaperBox.OnInteract` posts `SoundEvents.PLAY_RESOURCE_PAPER_BOX`, which flows through `WwiseSoundManager.InternalPostSoundEvent`. Runtime registration currently allowlists only the reviewed `PLAY_RESOURCE_PAPER_BOX` event, rejects duplicate suppressing replacements, and fails open if the hook is unavailable, a local WAV is missing/pending/failed, the event carries unsupported emitter/callback semantics, local playback cannot be verified, or playback does not start. The public API accepts owner manifests, reviewed native sound-event string IDs, local audio paths, volume, cooldown, and fail-open suppression policy without exposing raw Wwise, Unity, Harmony, or decompiled Doloc Town types. It remains Experimental and blocked from promotion because replacement is global per reviewed Wwise event, playback uses a DTMAPI 2D `AudioSource` outside Wwise bus/mixing/3D emitter semantics, owner unload cleanup and broader multi-mod arbitration are not yet proven, and tenth-save cardboard smoke for `Yuuka.DTMAPI.ManboCardboardAudio` is still failing: `GAME-SMOKE/20260617-052549` had zero-metadata callback clips, and `GAME-SMOKE/20260617-053452` reached ready preload but `AudioSource.Play()` did not enter a verified playing state. |
| Workshop / Content | `IWorkshopHelper.GetOfficialMods`, `GetDtmApiMods`, `IsOfficialEnablementManaged`, `GetEnablementHint`, `IWorkshopEvents.ModListChanged` | Experimental | Reads official/local/Workshop-capable manifests and respects official enablement. Needs stronger Steam/official UI regression coverage before promotion. |
| Content Query | `IContentQueryHelper.FindAssets`, `GetKnownContentTypes`, `TryReadTextAsset`, `GetIndexedItems`, `GetIndexedItem`, `IContentItemInfo` | Experimental | Read-only official/Workshop source index. It must remain separate from taking ownership of official content loading. |
| Content Pipeline | Content pack edits, ContentPatcher-style actions, update checks, compatibility DB, reflection helper, console command author API | Proposed | Direction exists in planning docs, but no stable public contract is promised here. |
| Migrated Gameplay | `IActionCompletionApi` | Experimental | `ActionCompletionFeature` registers the API through `ActionCompletionService`, with shared `ToolColliderHitHookBridge` owning the `ToolCollider.HandleTools` Prefix/Postfix route and `ActionCompletionHookBridge` consuming the shared postfix ready state for `Actions.OneActionComplete`; `AgentStateLifecycleHookBridge` owns the shared interact-exit callback used for fuel/feed completion. Latest callback-isolation evidence is `GAME-SMOKE/20260610-163813`, which verifies OneAction resource/wrong-tool/fuel/feed/vegetation paths after ActionCompletion and OilCoalDrop gained independent safe wrappers inside the shared ToolCollider postfix route; Oil route evidence `GAME-SMOKE/20260610-163928` verifies `NewContentOilCoalDrop=Passed` on the same route. Earlier shared ToolCollider owner evidence remains `GAME-SMOKE/20260610-140419`, which verifies `Feature.ActionCompletion = ready`, `Feature.OilCoalDrop = ready`, unchanged `Actions.OneActionComplete = verified` and `Actions.OneActionFuelFeed = verified`, clean exit, and no fatal popup after ActionCompletion stopped installing the ToolCollider hooks directly; `GAME-SMOKE/20260610-140641` verifies the same shared prefix/postfix route still publishes `Resources.OilCoalDrop = experimental`, `OilMod.MiningDrop = experimental`, and `Smoke.NewContentOilCoalDrop = verified`. Lifecycle-isolation evidence remains `GAME-SMOKE/20260610-012400`; native-helper evidence remains `GAME-SMOKE/20260609-174124`. Resource classification, fuel/feed post-interact handling, shared ToolCollider dispatch, and vegetation exception paths remain GameBridge-owned and version-sensitive, so this stays Experimental. |
| Migrated Gameplay | `IActionSpeedApi` | Experimental | Used by ActionSpeedMod with real gameplay smoke evidence. Latest final diagnostics report-export evidence is `GAME-SMOKE/20260611-031721`, which verifies `ActionSpeedTool=Passed`, `ActionSpeedConfigApply=Passed`, `ActionSpeedInteraction=Passed`, `Feature.ActionSpeed = ready`, `Smoke.DiagnosticsSnapshot=verified`, `DiagnosticsReportExport=Passed`, matching report `dtmapi-report-20260611-031801.zip`, and clean process/fatal checks; prior diagnostics snapshot evidence remains `GAME-SMOKE/20260610-043830`; feature-status publish-throttle evidence remains `GAME-SMOKE/20260610-041156`; lifecycle-isolation evidence remains `GAME-SMOKE/20260610-012242`; shared-lifecycle evidence remains `GAME-SMOKE/20260609-172903`; feature-status model evidence remains `GAME-SMOKE/20260609-141440`. The 2026-06-16 native-stage rebuild keeps the public DTO unchanged and adds only GameBridge-owned internals: deterministic highest-multiplier provider selection, `AgentControllerState.InteractContinues(float dt)` timer scaling for water-container/interact-held paths, native selected-equipment lookup for plant-item actions, native-owner summaries for `ItemBottle.DrawWater*`, `PlantBasin.Plant/Fertilizer/Protect`, `PlantBasinTree.Fertilizer`, `FlowerPot.Plant`, `AnimalRenderer.OnInteract` owner marking for scanner-overlap cases with marker lifetime guarded across generic state-exit restore, `AffectorElectric.OnInteract` for `Sprinkler` / `FarmLight`, and ready `ResinCollector.OnInteract -> BodyController._Interact -> Collect` harvest. Empty resin collectors are excluded from generic `IGatherableEquipment` harvest fallback. Release tests pass, but fresh third-save smoke/manual QA is still pending for low-row plant actions, repeated fertilizer/crop-film failure branches, animal fondle above door and room connector, electric switch actions, and ready resin collector collection. It still patches many interaction/animation paths and needs broader conflict/lifecycle regression coverage, so it remains Experimental. |
| Migrated Gameplay | `IFishingAutomationApi` | Experimental | `FishingAutomationFeature` registers the API through `FishingAutomationService`, with `FishingAutomationHookBridge` owning `Fishing.Automation` hook targets. In 0.5.1-alpha the experimental DTO was intentionally rewritten from migrated-mod booleans to native-stage strategies: `BiteWaitMode` (`NativeWait` / `InstantNativeBite`), `ResultMode` (`AutoCompleteVisibleMiniGame` / `SkipMiniGameNativeResult`), `AnimationMode` (`Normal` / `FastCastPull`), recast delay, movement-cancel, animation multiplier, cast charge target, and verbose logging. The default F6 loop casts through `BodyController.UseFishRod`, waits for the native bite, auto-reels through `AgentStateFishingWait.NextState` or an equivalent native Wait branch when no input edge exists, auto-plays the visible `FishingGameScrollBar` by scoped native input decisions during `FishingGameScrollBar.UpdateGame` (green/stable hold, red/off-note release, yellow/bonus short tap), collects through `AgentStateFishingPull`, and recasts. `InstantBite` changes only wait timing; `SkipMiniGameNativeResult` uses the native skip-game result route and must not force Pull success; `FastCastPull` is limited to Ready/Cast/Pull fishing phase hooks and exposes a bounded `AnimationMultiplier` (`1..4`, default/migration `3`) that scales reachable animator paths, native Ready charge through `AgentStateFishingReady._castTimer.Tick`, visible cast hook physics through `FishRodRenderer.CastHook` (`FishRodHook.Velocity` plus Rigidbody2D gravity), and native `FishRodRenderer.Pull` / `PullCancel` durations. `CastChargeRatio` is experimental, normalized to `0..1`, defaults to `0` no charge, and is implemented by holding/releasing native `DolocUserInput.NormalUseToolInProgress` until `AgentStateFishingReady._castTimer.Progress` reaches the target so `AgentStateFishingReady.NextState` remains the Cast transition owner. AutoFishing game smokes require explicit fifth-save fixture runs (`-SaveSlot 5`) because the fifth save is the real pond/selected-rod test position; global smoke defaults remain third save. Latest fifth-save verification passed on 2026-06-13 for `DefaultLoop` (`GAME-SMOKE/20260613-112900`), `InstantBite` (`20260613-113127`), `SkipMiniGame` (`20260613-113240`), `FastAnimations` (`20260613-113654`), `CombinedInstantSkip` (`20260613-113824`), and `CombinedInstantComplete` (`20260613-113937`) after the smoke gate was fixed to accept already-advanced native Cast/Wait/Battle/Pull phases. Follow-up verification `GAME-SMOKE/20260613-153632` proves `AutoPlayVisibleMiniGame` with stable-hold/release frame counters instead of delayed direct success, user manual QA confirmed red/green/yellow handling succeeds, `GAME-SMOKE/20260613-154252` proves animator/Pull-duration paths but is incomplete for visible cast speed, `GAME-SMOKE/20260613-160354` proves `FastCastHookPhysics` with hook velocity/gravity scaling plus Pull duration `0.147->0.049`, `GAME-SMOKE/20260613-163812` proves Ready `FastReadyCharge` (`castTimer.Progress:0->0.04;extraDt=0.04;powerBar.updated=2`) plus Cast hook physics (`22.366,35.786 -> 67.098,107.357`), Pull duration `0.117->0.039`, and final `readyCharge=True, fastAnimation=True`, and `GAME-SMOKE/20260613-171405` proves configured `CastChargeRatio=0.5` releases through `AgentStateFishingReady.NextState` at `progress=0.54` while FastAnimations still passes. Failed `GAME-SMOKE/20260613-153850` is retained as the fixed missing-multiplier migration bug, failed `GAME-SMOKE/20260613-163257` is retained as the fixed smoke-gate false negative before Ready charge was tracked independently, `GAME-SMOKE/20260613-170514` is retained as a false positive from PowerShell integer clamp truncating `0.5` to `0`, and `GAME-SMOKE/20260613-171104` is retained as the stricter-gate failure that exposed that script clamp bug. The API remains Experimental: multi-owner policy is still single effective owner and no stable public guarantee is made for ordinary mods yet. Unit coverage verifies default strategy normalization, missing-input-edge native reel mirroring, skip precedence/result preservation, stable/delay/bonus minigame input decisions, Ready `_castTimer.Tick(extraDt)` progress refresh, Ready/Cast/Pull animation speed, and configured Ready charge target hold/release. |
| Migrated Gameplay | Fishing native responsibility review | Experimental | Review `docs/reviews/api/2026/20260610-fishing-native-responsibility.md` maps Ready/Cast/Wait/MiniGame/Pull/Cooldown native owners and keeps `IFishingAutomationApi` Experimental. A 2026-06-10 addendum clarifies that the review's old file-layout/register-owner text predates the `FishingAutomationFeature` split; current implementation registers through `FishingAutomationFeature` / `FishingAutomationService` / `FishingAutomationHookBridge`, while the native-owner conclusions still stand. Current proof reaches native `BodyController.UseFishRod`, `AgentStateFishingWait.RollFish`, state overwrite, energy cost, Pull/minigame callbacks, and `FishingGameScrollBar.currentGameStatus`, but F6 enablement, movement cancel, multi-owner policy, smoke force-fish behavior, and animator-speed restore are DTMAPI-owned sidecar responsibilities. The 2026-06-10 `FishingAutomationFeature` split separated API/service/hook ownership while preserving existing smoke/status semantics; future work must deepen native responsibility coverage rather than treat the ownership split as stability promotion. |
| Migrated Gameplay | `IItemTooltipApi` | Experimental | `FishRoeTooltipFeature` registers the API through `FishRoeTooltipService`, with `FishRoeTooltipHookBridge` owning `Items.FishRoeTooltip` item-title/detail patch status. The public FishBreedingAssistant lookup source remains a placeholder in the shareable repo, but as of 2026-06-13 GameBridge no longer lets that placeholder silently drop `鱼卵 (鱼名称)` decoration: after provider lookup misses, it falls back to the native item owner path `ItemFishRoe.fishName -> DolocAPI.QueryItemProto(fishId).Title`. Latest evidence `GAME-SMOKE/20260613-201326` verifies `ExperimentalHooks=Passed`, `Items.FishRoeTooltip = verified`, `Feature.FishRoeTooltip = ready`, `Fish roe native title lookup resolved fish -> 鱼`, `Smoke exercise FishRoeTooltip OK item=fish_roe title=鱼卵 (鱼) detail=`, `Smoke.FishRoeTooltip = verified`, clean exit, and no fatal popup after the smoke-only fallback provider was removed. Earlier smoke-case split evidence remains `GAME-SMOKE/20260610-021014`; feature split evidence remains `GAME-SMOKE/20260609-181533`. The API remains display-only and Experimental: ordinary mods still receive the stable DTO/options surface only, not raw decompiled item proto types or a stable general content-query promise. |
| Migrated Gameplay | `IAnimalViewerApi` | Experimental | `AnimalViewerFeature` registers the API through `AnimalViewerService`, with `AnimalViewerHookBridge` owning `AnimalFullInfoData(Animal)`, `AnimalViewer.Show`, and `AnimalPanel.RefreshViewer` hook installation. Manual QA Batch 1 changed the hidden-produce overlay first-frame path by disabling localization-style components on cloned progress rows before activation, pre-filling while inactive, refreshing again in the same callback, and publishing `Smoke.AnimalViewerFirstFrameFlickerGuard` for same-callback text validation. The 2026-06-12 bottom-layer implementation tightens the lifecycle so the `AnimalViewer.Show` prefix clears stale cloned rows only, then the postfix performs inactive prefill, activation, and same-callback text validation; smoke now also runs a repeated `AnimalPanel.RefreshViewer` / `Select` loop and publishes `Smoke.AnimalViewerRepeatedSwitchFirstFrame`. Latest bottom-layer evidence `GAME-SMOKE/20260612-170819` verifies `AnimalViewerUi=Passed`, `HookProbe=Passed`, `SaveLoaded=Passed`, clean process/fatal checks, `Smoke.AnimalViewerFirstFrameFlickerGuard=verified`, `Smoke.AnimalViewerRepeatedSwitchFirstFrame=verified`, and `moodTitleHits=0`; screenshots/summary are under `ANIMAL-001/20260612-170858`. Batch 1 evidence `GAME-SMOKE/20260611-214845` remains retained for the first same-callback guard. The API remains Experimental because native animal data/viewer ownership is still fragile and repeated-switch visual evidence must stay tied to game smoke/manual QA rather than public stability. |
| Diagnostic APIs | `IDebugConsoleApi`, `IInventoryDebugApi`, `IWeatherDebugApi`, `ITeleportDebugApi`, `IInstantSaveDebugApi`, `ITimeDebugApi`, `IMovementDebugApi`, `IAdvancedDebugApi` | Diagnostic | These are Y-console/debug surfaces. Some call real native methods, but they are for diagnostics and controlled smoke/manual testing, not stable ordinary gameplay APIs. The 2026-06-16 player-log follow-up does not add public members: the reflected debug console host now ignores Y-close while a search input is focused, right-click item give is limited to the clicked cell's `PointerDown` route instead of stale screen-rectangle hit testing, and `IMovementDebugApi` keeps a lightweight debug lease that clears on SaveLoaded/ReturnedToTitle/reset. The 2026-06-17 Y opener-edge fix also adds no public API: it only makes the reflected host ignore the physical Y press that opened the console until that key is released. |
| Native Utility | `IMailDeliveryApi` | Experimental | Uses native item mail delivery with source gating and duplicate guards. Current game ignores custom mail title/content/sender, so it remains template-based and experimental. |
| Vehicle | `IMotorVehicleApi` and MotorVehicle DTOs | Retired | Removed from active `DTMAPI.Abstractions` and `DTMAPI.GameBridge.DolocTown` in the 2026-06-15 second-level cleanup Round 2 after the only real consumer, `SecondMotorMod`, was archived. The old 2026-06-14/15 slot 8/9 smoke history remains research evidence for why Doloc Town's native motor path is singleton-oriented, but it is not current completion proof and no ordinary mod should depend on this API. Future vehicle work must start from a smaller native-owner review and introduce a fresh public contract instead of reviving the retired surface. |
| Machine | `IMachineProductionApi` | Experimental | MineMod now uses the 2026-06-12 electric-only/no-fuel definition: `AllowFuelMode=false`, `AllowElectricMode=true`, `DefaultMode=electric`, fuel capacity and fuel costs normalized to `0`, and stale persisted Mine config fields are migrated so they cannot re-enable hybrid behavior. Runtime still uses native electric component `Launch()` before producing in electric mode and keeps Mine-owned storage/output telemetry. Bottom-layer evidence `GAME-SMOKE/20260612-170708` verifies `electricOnly=True`, `fuel=disabled`, `electricPowerCost=10`, Mine production/storage, official tech-tree UI, placement screenshot, and `Machine.MineVisualContainment=verified` with no non-Mine contamination. Still experimental because it spans recipe, power, storage, renderer, production, visual lifecycle, and pass-time catch-up owners, and ordinary mods must not depend on raw native machine types. |
| Equipment | `IEquipmentSlotsApi` | Experimental | MoreEquipmentSlots evidence covers interactive reflected UI clones, native save transaction sidecar persistence, equip/recover, and recovery checks. 2026-06-14 protected-storage work moves DTMAPI-owned sidecars from global owner files to per-save `protected-items/equipment-slots/slot-<archiveIndex>` storage with archive/player/save-clock guards, legacy global migration, and tail-first orphan recovery policy. Release build/unit passed, and local third-save smoke `GAME-SMOKE/20260614-011222` verifies registration plus `grandmas_button` and attribute-only `straw_hat` equip/recover with clean exit. User manual QA confirms equip-save-reenter does not duplicate, other saves are not polluted, disabling MoreEquipmentSlots returns items to backpack, and extra hats do not alter the character sprite. 2026-06-14 mushroom-hat follow-up keeps the API Experimental but corrects native hat semantics: registered hats no longer require `HatInfo.Skill`, skill-backed hats/passives still use `AgentEquipmentFunction`, and `HatInfo.Defense` is mirrored after `AgentEquipmentManager.ReloadParams` without writing native `hatItem`; `GAME-SMOKE/20260614-082151` verifies `mushroom_hat` equip/recover with `mushroomHatDefence=0->1->0` and unchanged native visual hat. 2026-06-14 shield-hat follow-up recognizes native `ItemFunctionHatShield` / `AgentEquipmentFuncProtoShield` hats as managed extra-slot shields, stores charge in the protected sidecar, routes hits through `BodyController.OnAttacked` only when vanilla `TryGetShieldItem` has no shield, and consumes tail slots without clearing native `hatItem`; stale disabled/out-of-range entries no longer contribute extra hat defense. `GAME-SMOKE/20260614-120739` verifies `box_hat` shield `80->79/80`, `Player.EquipmentSlotsShield = verified`, unchanged `nativeHat=miner_helmet->miner_helmet->miner_helmet`, backpack recovery, clean exit, and no fatal popup. User manual QA also passed official-slot shield priority, extra-slot shield plus two other hats, three extra-slot shield hats, and mushroom-hat defense loading; extra-slot shield hats do not render the official yellow shield bar, which is an accepted visual difference for now. Automated disabled/unsubscribed recovery and mail-overflow smoke remain pending. Same-process official disable can still leave inert extra-slot visuals until full restart, so the API remains Experimental and needs a hot-disable unregister/hide lifecycle before any stability promotion. |
| Saves | `ISaveSlotsApi` | Experimental | `SaveSlotsFeature` registers the API through `SaveSlotsService`, which owns save-slot policy state and throttled runtime refresh of `DolocAPI.gameManager.archiveFileCount` while official `LocalSave`, `DataPersistenceManager`, `GameDataUiState`, and `GameDataPanel` still own archive discovery, rendering, load, save, delete, copy, and restart recognition. In 0.5.1-alpha, `SaveSlotsOptions.SlotCount` is a compatibility/experimental field: enabled requests are normalized to exactly 12 total official slots for the MoreSaves player-facing path, and disabled requests normalize to vanilla 6. The 2026-06-12 implementation keeps the internal `Save.MoreSlotsUiPaging` GameBridge adaptation on `GameDataUiState.Show` and `GameDataPanel.Select`: 6/12-slot layouts are restored to the official panel, while 18+/24+ API/test layouts are paged in 12-slot visible pages and slot selection moves the official panel to the page containing the target absolute index. Smoke now selects slot 1, slot 13, and the last slot for expanded counts and records `visibleSlotsAfterPaging`. Latest native responsibility review `docs/reviews/api/2026/20260610-saveslots-native-responsibility.md` still keeps this API Experimental because save files, load/save/delete/copy, and restart recognition remain official owner responsibilities. Bottom-layer evidence `GAME-SMOKE/20260612-170240`, `20260612-170331`, `20260612-170421`, and `20260612-170512` verifies 6/12/18/24 official save UI layouts with `visibleSlotsAfterPaging=6/12/6/12` and expanded targets `1|13` / `1|13|24`; fixed-12 MoreSaves follow-up evidence `GAME-SMOKE/20260612-203558` verifies `archiveFileCount=12`, `panelSlotCount=12`, `renderedSlots=12`, `visibleSlotsAfterPaging=12`, and `MoreSavesOfficialSaveUiEvidence=Passed`; latest post-title/pause-setter revalidation `GAME-SMOKE/20260613-123521` verifies the same 12-slot official panel fields with clean process/fatal checks; earlier 12-slot evidence is `GAME-SMOKE/20260610-100337`. Still experimental because delete/copy edge cases, disable/restore, restart recognition, and multi-owner conflict evidence remain pending. |
| Inventory | `IChestLocatorEnhancerApi` | Experimental | `ChestLocatorEnhancerFeature` registers the API through `ChestLocatorEnhancerService`, with `ChestLocatorEnhancerHookBridge` owning `ArchiveDataHandle.GetAvailableInventories`. Latest merged-policy evidence is `GAME-SMOKE/20260610-042459`, which verifies `Feature.ChestLocatorEnhancer = ready`, unchanged `Inventory.ChestLocatorEnhancer = verified`, `Smoke.ChestLocatorEnhancer = verified`, native `CountItem/CostItem` shared-inventory behavior, effective policy summary fields, and clean exit. Unit test `ChestLocatorPoliciesMergeEnabledOwners` verifies two enabled owners merge through existing state messages without adding public DTO members. The API remains Experimental because broader room/building/location policy and conflict behavior still need more real-mod coverage. |
| Farming | `IStrongPlantingGunApi` | Experimental | `StrongPlantingGunFeature` registers the API through `StrongPlantingGunService`, with `StrongPlantingGunHookBridge` owning `ItemFarmingGun` constructor/use and `FarmingGunUiState` transfer hook installation. Manual QA Batch 1 narrows the current contract to the verified fixed three-slot seed/film/fertilizer path: `StrongPlantingGunOptions.SlotCount` is accepted for compatibility but normalized to `3`, real range expansion is not supported by the current native use route, and disabled state restores native one-slot behavior while already-expanded visible slots may remain inactive until re-equip/restart. Batch 1 evidence `GAME-SMOKE/20260611-214654` verifies `StrongPlantingGun=Passed`, `HookProbe=Passed`, `SaveLoaded=Passed`, clean process/fatal checks, and report `dtmapi-report-20260611-214731.zip`. Latest smoke-case split evidence is `GAME-SMOKE/20260610-122933`, which verifies `Feature.StrongPlantingGun = ready`, `Farming.StrongPlantingGun = verified`, `Smoke.StrongPlantingGun = verified`, official farming-gun capacity `inventory:3/total:3/line:3`, seed/film/fertilizer actions all applied, third-save HookProbe SaveLoaded, clean exit, and report `dtmapi-report-20260610-123010.zip` after only moving the StrongPlantingGun smoke body into `Smoke/Cases/StrongPlantingGunSmokeCase.cs`. Feature split evidence remains `GAME-SMOKE/20260610-101436`. The API remains Experimental because it still reaches `ItemFarmingGun` construction/use/UI transfer hooks and needs broader real UI transfer/manual coverage before any stability promotion. |
| Farming | `ICropHarvestingApi` | Experimental | `CropHarvestingFeature` registers the API through `CropHarvestingService`, with explicit API calls over reviewed native `PlantBasin.CouldHarvest` / `PlantBasin.Harvest(bool,bool)` responsibility. The hardening pass narrows the contract to crop containers: ordinary PlantBasin crops plus PlantBasin-family vine, mushroom-bag, and bush categories may execute only when basin-level `CouldHarvest` / `IsCropMature` is true and the target still uses the reviewed `PlantBasin.Harvest(bool,bool)` owner. `TreeBasinCrop` means the cocoa-style `PlantBasinTree` crop-container path, not generic tree harvest; it is scan-only/unsupported until a separate `TreeCrop` / `PlantBasinTree` owner review. `GrassForageBasin` and wild forage/grass/tree content are outside this API's execution scope. Target ids are opaque transient handles for immediate same-session follow-up only. No mature targets is a successful no-op rather than API failure. Cleanup smoke `GAME-SMOKE/20260612-101549` verifies the same ordinary `PlantBasinSimple` scan/harvest path with `owner=DTMAPI.Smoke.CropHarvesting`, `CropHarvestingApi=Passed`, `CropHarvestingApiEvidence=Passed`, clean exit, and no fatal popup. Prior hardening smoke `GAME-SMOKE/20260612-072544` verifies `Feature.CropHarvesting=ready`, no-target `noTargetScan={success=True ... mature=0 ... failed=0}`, ordinary `PlantBasinSimple` scan/harvest through `PlantBasin.Harvest(bool,bool)`, and clean exit. First-slice smoke evidence `GAME-SMOKE/20260612-062154` remains the baseline for the original transient target path. A developer-only `CropHarvestingQaMod` fixture and manual checklist `docs/reviews/manual-qa/2026/20260612-0003-crops-harvesting-real-field-manual-qa.md` prepare real-field checks without expanding execution scope; the fixture is now installed only with explicit QA-fixture install mode and defaults its hotkeys to `None` to avoid F-key conflicts. Partial user QA on 2026-06-12 confirms the historical `F9` one-target harvest and `F10` batch harvest bindings put output into the backpack after manual fixture setup. The smoke owner is `DTMAPI.Smoke.CropHarvesting` so smoke status no longer pollutes the author-facing `AutoHarvestMod` sample owner. The API remains Experimental because room traversal, crop-family coverage, multi-owner scheduling, tree-basin/native-owner expansion, and remaining real-field boundary cases are not stable enough for ordinary-mod guarantees. |
| Camera | `ICameraZoomApi` 0.4.2 | Failed / ObsoleteCompatibility | Manual QA has superseded the 0.4.2 smoke claim. Do not cite `GAME-SMOKE/20260607-072228`, the `ZOOM-042` static screenshot set, or "background no longer appears as a small framed rectangle" as completion evidence. Those files are historical/supporting artifacts only. The API is obsolete and redirects compatibility callers to lease-based `ICameraViewApi`; old CameraZoom evidence is not CameraView completion proof. |
| Camera | `ICameraViewApi`, `ICameraViewLease` | Experimental | Lease-based playable camera view contract. DTMAPI arbitrates multiple owner leases by priority and latest update, writes only the playable camera `orthographicSize`, leaves native camera follow/range semantics intact, and does not call `CameraController.RefreshResolution`, `CameraController.SetPosition`, or `DolocAPI.RefreshScanner`. Latest final diagnostics report-export smoke evidence is `GAME-SMOKE/20260611-031502`, which verifies `Zoom=Passed`, `Feature.Camera = ready`, `Smoke.CameraPlayable = verified`, `Smoke.DiagnosticsSnapshot=verified`, `DiagnosticsReportExport=Passed`, report `dtmapi-report-20260611-031644.zip`, and clean process/fatal checks. Manual QA review `docs/reviews/manual-qa/2026/20260611-0001-refactor-manual-qa-code-review.md` confirms user-observed 2x/4x playable movement, centering, native clamp, no jump, no flicker, return-to-title cleanup, no residual 4x, and no black/small-frame abnormality in the tested paths; preserving zoom across building entry/exit is treated as expected design. Native-owner review `docs/reviews/api/2026/20260612-camera-background-native-owner-review.md` confirms background/fog/panorama synchronization is not implemented in this batch: background preset/layer state belongs to `DolocAPI.LoadBackground`, `BackgroundRenderer`, and `BackgroundLayerRenderer`, while fog/weather belongs to `EnvCovariantController` and `DepthFogController`. Keep `ICameraViewApi` Experimental and do not cite playable movement or orthographic-size-only smoke as background-sync completion or Stable promotion evidence. |
| Camera | `IPanoramaCameraApi` | Proposed | Interface draft only. Panorama/background/fog/range compensation is deliberately not mixed into ordinary playable zoom. |
| Custom Entities | Definition/registry portions of `ICustomAnimalApi`, `ICustomMonsterApi`, `ICustomAttackApi`, `ICustomDroneApi` | StableCandidate | DTMAPI Core registry contracts cover namespaced definitions, validation, duplicate checks, owner cleanup, snapshots/status, lifecycle listener isolation, and blocked request DTOs without raw game types. GameBridge status rows use registry-contract naming: `CustomAnimals.RegistryContract`, `CustomMonsters.RegistryContract`, `CustomAttacks.RegistryContract`, and `CustomDrones.RegistryContract`; each remains `configured-blocked` with `StableCandidate registry contract; runtime creation remains blocked`, verified in `GAME-SMOKE/20260610-045414`. Promotion to `Stable` requires two real mods using the definitions and regression rows; current proof is unit/internal smoke, not real mod adoption. |
| Custom Entities | Native runtime creation verbs: animal/monster `RequestSpawn`, attack `SpawnProjectile`/`ExecuteAttack`, drone `RequestSummon`/`Equip`/`SetMode`, native handles, active runtime snapshots, save restoration | Experimental | Blocked. Current behavior intentionally returns `runtime-creation-blocked` / `RuntimeCreationBlocked`; no native animal, monster, projectile, attack, or drone is created. Ordinary mods must not depend on these runtime verbs until family-specific native adapters are verified in game. |
| Custom Entities | Native animal/monster/attack/drone adapters | Proposed | Future work must start from native owners such as animal proto/room/food/produce/save, monster AI/spawn/drop, bullet factory/collision/damage, and drone controller/weapon/equipment/persistence. |

## 2026-06-16 API Notes

- `20260616-0004` does not promote any API. It improves diagnostic/runtime behavior only: GameBridge feature failures now store unwrapped root exceptions first, bootstrap `Update` failure publication is throttled after the first three full errors, and the existing Diagnostics log-export/history behavior from `20260616-0001` remains unchanged. Release build/test plus third-save smokes `GAME-SMOKE/20260616-124555` and `GAME-SMOKE/20260616-120817` verified the follow-up.
- `IDebugConsoleApi` and the Y-console debug APIs remain Diagnostic. The focus-aware `Y` behavior and right-click give route through the actual item-cell Unity `PointerDown` owner are implementation details of the reflected Bootstrap UI host, not a new ordinary-mod input contract.
- `IMovementDebugApi` remains Diagnostic. The new lease is a debug quality-of-life guard over `MotionAbility.SetMoveScaler`; it deliberately clears at save/title boundaries and is not a stable movement-buff API.
- `IAnimalViewerApi` remains Experimental. Clone-session cleanup and lifecycle status `Animals.ViewerRenderingLifecycle` are GameBridge internals for the long-session disappearance report, not new public AnimalViewer DTOs.
- `IActionSpeedApi` remains Experimental. The ActionSpeed bottom-layer branch is build/unit verified for native-owner classification including ready resin collectors and animal owner marking for room-connector overlap, plus marker lifetime across generic state-exit restore, but water-well/planting/animal/resin completion still requires fresh third-save manual smoke before any stability claim.

## 2026-06-15 API Notes

- `IMotorVehicleApi` and its MotorVehicle DTOs are retired/removed from active API code. The 2026-06-14/15 SecondMotor slot 8/9 smoke history is research history only: user manual QA on 2026-06-15 found abnormal light textures, abnormal farm-room textures, and severe cross-map texture pollution, so the active `SecondMotorMod` project and local packages were archived, then the remaining public API, GameBridge hooks, smoke body, and unit coverage were removed in cleanup Round 2. There is no replacement vehicle API in this release. Old experimental mods that reference `IMotorVehicleApi`, `CustomMotorDefinition`, `SecondMotorOptions`, or MotorVehicle result/state/event DTOs must remove those references; a compile or type-load failure for those retired types is expected, not a random loader bug. Do not cite archived SecondMotor smoke evidence as current completion proof or stable multi-vehicle evidence. Future vehicle work must start from a smaller native-owner slice and fresh manual QA.

## 2026-06-13/14 API Notes

- `20260614-0005` does not change `IEquipmentSlotsApi`. It adds a Diagnostic smoke dump of runtime `TbHat`/`TbItem` hat metadata for API review, verifying `hats=33`, `itemHatRows=33`, no orphan mappings, `box_hat` as `ItemFunctionHatShield` / `AgentEquipmentFuncProtoShield`, and `mushroom_hat` as `Defense=1` without `Skill` in `GAME-SMOKE/20260614-091839`. This is evidence for future design decisions only and does not promote EquipmentSlots beyond Experimental.
- `20260614-0006` does not promote `IEquipmentSlotsApi`. Shield hats are handled as internal GameBridge protected-storage behavior over native `ItemFunctionHatShield` / `AgentEquipmentFuncProtoShield` metadata; no raw hat, shield, `BodyController`, or `AgentEquipmentManager` type is exposed publicly. Multi-shield manual QA has passed, but the API remains Experimental until disabled/unsubscribed recovery, hot-disable UI cleanup, and real-hit tail automation coverage are complete.
- `IFishingAutomationApi` remains Experimental. The `20260613-0001` to `20260613-0003` follow-ups changed internal InstantBite timing/visibility and native post-bite routing. The `20260613-0005` rewrite deliberately changes the experimental DTO shape to native-stage strategy enums and rewrites AutoFishing defaults around the fifth-save real pond fixture. This is a breaking experimental API cleanup, not a stability promotion; hook/status IDs remain compatible where practical. Fifth-save smoke `GAME-SMOKE/20260613-094928` proved the new fixture gate and failure reporting while the local fifth save selected `DolocTown.ItemTool`; after the fifth save was updated to hold `carbon_fishrod`, the latest real loop and all current AutoFishing scenarios passed in `GAME-SMOKE/20260613-112900`, `20260613-113127`, `20260613-113240`, `20260613-113654`, `20260613-113824`, and `20260613-113937`.
- `20260613-0014` keeps `IFishingAutomationApi` Experimental while tightening semantics: `AutoCompleteVisibleMiniGame` is no longer a delayed direct `currentGameStatus=Success` write, and `FastCastPull` now has a player-facing `AnimationMultiplier` in `1..4` with migration/default `3`. Fifth-save evidence `GAME-SMOKE/20260613-153632`, manual red/green/yellow QA, hook-physics re-smoke `GAME-SMOKE/20260613-160354`, and Ready/Cast/Pull re-smoke `GAME-SMOKE/20260613-163812` supersede older `visibleSeconds=0.76` direct-success proof and animator-only animation evidence for current completion/animation semantics.
- `20260613-0016` keeps `IFishingAutomationApi` Experimental and adds `FishingAutomationOptions.CastChargeRatio` as a native Ready-stage policy. It does not expose raw Ready/Cast/FishRod types and does not stabilize multi-owner semantics. Fifth-save evidence `GAME-SMOKE/20260613-171405` supersedes the false-positive `GAME-SMOKE/20260613-170514` for configured charge because the final smoke required `target=0.5` in `Smoke.AutoFishingCastCharge`.
- `20260613-0017` does not change `IFishingAutomationApi`. AutoFishing toggle replacement is implemented by the ordinary mod through existing `IDtmConfigMenuApi.AddKeybindOption` and `IInputHelper.RegisterButton` / `UnregisterButton`; `F6` remains the default, explicit `None` disables the mod hotkey, and custom keys are preserved. Fifth-save smoke `GAME-SMOKE/20260613-175055` verified the F7 path from config/logged policy to input dispatch, `reason=hotkey F7`, and the default native loop. This does not promote config-menu keybinds or FishingAutomation beyond their existing status.
- `20260613-0021` does not change `IFishingAutomationApi`. It narrows the internal InstantBite implementation so `AgentStateFishingWait.OnEnter` only prepares the bite and `OnPlay` owns the single mirrored native reel/energy-cost transition. Unit coverage verifies exactly-once `CostEnergy` behavior; fifth-save energy-delta evidence is still pending, so the API remains Experimental.
- `IDtmConfigMenuApi` / title Manager UI stability does not change. `UI.NativeLayoutDiagnostics` is internal GameBridge logging plus narrow native-owner setter normalization for the remaining title/pause two-column layout regression; it is not a public API.

## Failed / Historical Evidence Rules

- CameraZoom 0.3.0 evidence proves only orthographic-size writes and restore. It is historical, not completion proof.
- CameraZoom 0.4.2 evidence is now marked `Failed / ObsoleteCompatibility` after manual QA. Its static screenshots and smoke logs may remain useful for comparison, but they must not be cited as `ICameraViewApi` completion evidence.
- CustomEntity 0.4.0 smoke proves registry and blocked runtime results only. It does not prove native entity creation.
- HookProbe/TestMod evidence can prove event delivery or internal status paths, but it cannot by itself promote a public gameplay API to `Stable`.

## Current Summary

- Stable: DTMAPI-owned framework contracts with low native-state risk.
- StableCandidate: strong DTMAPI-owned or contract-level APIs that still need adoption/manual/regression gates.
- Experimental: most GameBridge-facing gameplay/content/UI/native utility APIs.
- Diagnostic: Y-console, debug, evidence, and hook-status helper APIs.
- Retired: vehicle/MotorVehicle API and hook surfaces removed after the SecondMotor route failed manual QA.
- Proposed: content pipeline, Panorama camera API draft, and custom entity native adapters.
- Failed / ObsoleteCompatibility: CameraZoom 0.4.2 as a completed visual/camera API claim.
