Goal: implement the Hatch AnimalVoice short-SFX replacement infrastructure. Add a content-pack-only `Content/DTMAPI/audio-replacements.json` route where authors provide JSON plus WAV files, not DLLs or copied Wwise/game audio. Keep replacement on the reviewed native path: `Animal.PlayAnimalSound()` supplies animal context, and `WwiseSoundManager.InternalPostSoundEvent` performs fail-open local WAV replacement only when `speciesId + stage + nativeSoundEvent` match. First use case is `DTMAPI_HatchAssets`: replace Hatch child `PLAY_ANIMAL_PET_CHICKEN_CHILD` with `Content/Audio/hatch_pet_young.wav` and Hatch adult `PLAY_ANIMAL_PET_CHICKEN` with `Content/Audio/hatch_pet_adult.wav`; do not affect vanilla chickens. Preserve Manbo paper-box behavior and do not promote `IAudioReplacementApi` or implement BGM/music. Add unit tests, docs/update records, and a slot 7 `-AutoExerciseHatchAnimalVoice` runtime smoke under the shared runtime lock.
