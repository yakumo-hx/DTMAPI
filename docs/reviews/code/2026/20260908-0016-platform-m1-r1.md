# R1 — M1 candidate and precise M2 entry

- Date: `2026-09-08`
- Lifecycle: `complete` (M1 bounded candidate accepted after corrections and reboot evidence; named capability limits retained).
- Owning Update: [0016](../../../updates/2026/20260908-0016-platform-m1-r1.md).
- Subsequent acceptance: [2026-09-09 独立验收与连续实施](20260909-0001-platform-m1-acceptance-continuation.md)重新打开 PN-015/017 的具体反例，保留本次实机事实，并将阻断限定为未证公共行为/产品出口；原任务返修、补证后可直接 R1→M2，独立内部切片按该结论先行。本页以下是原 9 月 8 日复盘，不以其旧批次终点覆盖用户的新授权。
- Evidence: [M1 run](../../../debug/evidence/GAME-SMOKE/20260908-225439-platform-m1/README.md); implementations [0010](../../../updates/2026/20260908-0010-platform-config-correctness.md)–[0015](../../../updates/2026/20260908-0015-platform-author-journey.md). Scope is the accepted M1 chain; no whole-platform redesign or M2 implementation.

## Decision

**2026-09-09 final post-reboot revision — GO for bounded M2 implementation.** [Current evidence](../../../debug/evidence/GAME-SMOKE/20260909-platform-m1-reboot/README.md) completes native new game, controlled failure, cold recovery and a real IO-failure → same-process retry → success → title → owner/QA close → normal exit. The last runner passed. The false-return coordinator defect is repaired and verified by focused Core tests and shipping Mono. Together with corrected A1–A3 public SDK checks, held input, cross/same-slot loads and actual resident/disk mismatch, M1 now meets its factual and author-journey exit within the documented logging/source-location support. Breakpoint support is still unproven.

The missing/malformed-data branch remains limited: native LocalSave can report success with null data, DataPersistenceManager creates partial new-game state and returns false before AfterLoadArchiveData, yet IsDataLoaded can be true. Same-process UI retry stalls before a second LoadGame; cold recovery works. The matching loading-panel completion/state inspection and successful ordinary IO comparison narrow the likely handoff interruption, without proving its exact callback cancellation. Keep these failed runs failed. This is a PN-009 fail-closed input, not a requirement to repair all native save/UI behavior before M2: invalidate before every attempt, require fresh successful native lifecycle evidence, never infer readiness from IsDataLoaded/ticks, and establish new-game and scene-transition boundaries separately. PN-009/PN-020 must prove the new service guarantees; M1 does not grant them. Reuse PN-005/018 internal work and proceed along PN-009→PN-019→PN-007.a→PN-020→R2→PN-007.b under the existing authorization. No new stable target is authorized by this decision.

Restoration correction: previous records verified profile-root `mod_infos.json`, which is not the actual native `SAVE/mod_infos.json`. Exact prior native enablement restoration is unsupported; this run correctly records and preserves the actual SAVE baseline. Archive/sidecar immutability remains verified. Older conclusions below are historical and are narrowed by this revision.

The remainder records the initial September 8 review and intermediate September 9 state. Its old NO-GO/pending statements are historical and superseded by the final revision above; the independent acceptance Review is unchanged.

## Reviewed results

| Boundary | Evidence-backed conclusion | Limit |
| --- | --- | --- |
| Config | Serializer output preserves special text; read IO errors propagate; malformed bytes must be backed up before defaults; atomic replacement failure preserves original. Focused failure tests passed and the external sample read/wrote/reloaded its text. | Existing migration Action semantics intentionally unchanged; no new configuration contract. |
| Build | One internal plan and actual IDE→CLI delegation produced equal input/DLL/PDB; SDK160 checks actual references, SDK180 rejects unsupported inputs, SDK161/PE closure remain. Target 0.5.5 frozen payload unchanged. | Windows SDK backend; multi-project/resources belong to PN-022. IDE output selection is the documented SDK bin/dtmapi-author configuration directory. |
| Official Local | Arbitrary authors installed, native-enabled, loaded, updated/restarted and withdrawn. Corrected native info shape survives discovery without receipt drift. Cold exe lease blocks real concurrent launch; journal schema 4 binds official root, historical readers retained. | First candidate failed and required authorized non-save asset restoration. Final corrected package did not. Runtime code remains restart-required. |
| Source and resident identity | Authenticated snapshot separates selected/official/owner state, disk SHA/MVID, resident MVID; final new process loaded the new MVID. Identical unloaded disk copy is not treated as resident in focused tests. | MVID equality is not a memory hash or arbitrary binary attestation. In-process disk replacement negative case not demonstrated on shipping Mono. |
| Diagnostics | Entry stack exact line; event owner/name/message plus matched source identifies the deliberate error; next tick continues. Public fix/update/restart works. | Event stack points to callback end line 35 instead of throw line 32. PDB source checksum/sequence points confirm correct source, so documentation must preserve precision limits. Debugger attach and breakpoint unproven. |
| Lifecycle | Single native load on thread 1: SaveLoaded before LoadGame return and before CurrentRoom availability; first observed later native update has all sampled objects. Native official disable removes sample event/input registrations and reports resident code needs restart. | Does not prove ready across different saves, same-save reload, new game or failure. Initial ReturnedToTitle may occur at boot; event name alone is not a prior-save identity. |
| Reports and restoration | Local report inventory/token scan, retained diagnostics; 30 archives/5 sidecars immutable; test assets restored and shared lock released. | Reports contain paths/history and historical crash memory dumps; scan is not a privacy guarantee. No upload or published-support claim. |

Code review followed changed ConfigService/JsonFile, BuildPlan/ProjectBuildInputs/compiler/template, official deployment/recovery/lease/readers, SymbolInspector and snapshot/Hook callback changes. It reused established native-owner analysis and checked only LoadGame/AfterLoadArchiveData/NormalGameState fields required for the observation. The discovered native metadata drift and rollback lock failure were corrected in PN-004 with regression coverage. The final tests also corrected the stale legacy-deploy-paused assertion; historical local source selection is still rejected.

## Bounded continuation on existing cards

1. **PN-016 / PN-008 input blocker:** establish physical or supported held-key input with F8 and native Escape in the current game. Mouse and OS close work; repeated synthetic F8/Escape did not. A user input request was sent and received no result before safe exit. Do not label this a Mod input bug or count an invisible invocation as PASS. Reacquire the shared runtime lock, re-stage the recorded candidate and check current save/sidecar prerequisites before a new run.
2. **PN-016 lifecycle:** use public external trace for A→title→B and same-slot reload, recording sequence/thread/existing internal generation/object presence; no native save. Exercise new game and controlled native load failure only through the already established disposable fixture/isolation workflow. Record failure before/after native event and owner cleanup; simulated NotifySaveLoaded is insufficient.
3. **PN-017:** keep demonstrated source trace and exact artifact pair instructions. For old resident/new disk, use an isolated controlled setup that can actually demonstrate the distinction without weakening cold update protection. Breakpoint investigation can remain a named technical limit; a listening port is not evidence of debugger protocol/support.
4. **PN-008:** reuse unchanged build/transaction/negative tests and the proven update/withdraw chain. Repeat only input/lifecycle slices or paths invalidated by corrective code. Update this R1 decision when its named evidence changes, without creating a new general audit.

## M2 input that is safe to carry forward

2026-09-09 continuation revises the gaps above: A1–A3 production fixes and actual public subprocess checks passed; held-key title input, A→title→B/same-slot reload and real resident/disk mismatch now have [shipping Mono evidence](../../../debug/evidence/GAME-SMOKE/20260909-platform-m1-continuation/README.md). New game/native load failure remain pending after bounded disposable-fixture attempts and repeated window-capture failure. M1 product decision remains NO-GO. The later explicit bounded-continuation authorization allows PN-005 ABI/owner and PN-018 path/global IO internal slices while this external obstacle remains; public readiness/context/target freeze gates are unchanged.

- PN-005 keeps frozen helper/consumer ABI and introduces optional services without adding members to old implementer interfaces. The existing ABI harness and actual frozen consumers remain required; no new interface was produced here.
- PN-009 keeps Bootstrap's existing single Runtime frame authority and the native-frame drain. A proposed public context needs explicit unavailable/readiness and epoch invalidation through load failure/title/reload. SaveLoaded must not be its sole ready predicate. Until step 2 is proved, no public WorldReady, universal epoch or scheduler cancellation guarantee is accepted.
- PN-018 own-file/global data must remain owner-scoped and separate from save-bound data. PN-019 must preserve old config migration semantics until its own versioned design/tests; the M1 fix is not that migration design.
- PN-007 candidate/freeze order and all later target/version gates stay unchanged. PN-020 consumes a real new candidate only after the preceding M2 work; M1 did not generate one.

These constrain the existing plan and do not bypass M1's product gate. At the initial 2026-09-08 review no M2 implementation had occurred. The later explicitly authorized PN-005/PN-018 internal implementations and Core/frozen-ABI results are now owned by Updates 20260909-0002/0003; Mono/product gates remain pending. No Workshop upload, new stable API claim or branch/worktree switch occurred.
